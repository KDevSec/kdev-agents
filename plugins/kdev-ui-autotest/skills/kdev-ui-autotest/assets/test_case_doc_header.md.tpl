# <模块名> 功能测试用例 v1.0

**所属模块**：<业务模块>
**对应需求**：<spec 路径>
**UI 元素权威源**：[recon/menu_list.md](../recon/menu_list.md)（YYYY-MM-DD 实测采集，与本文档冲突时以菜单清单为准）
**用途**：本文档将用于生成 Playwright + pytest 自动化脚本。每条用例的「测试步骤」均按 UI 元素粒度（菜单 / 按钮 / 输入框 / 下拉 / 弹窗）描述，「预期结果」给出可机械化断言的状态。
**测试账号**：<账号> / <密码>

## 测试环境与导航约定（YYYY-MM-DD 实测）

| 项 | 值 |
|----|----|
| 测试环境 URL | <BASE_URL> |
| 登录账号 / 密码 | <USER> / <PWD> |
| 登录按钮文案 | `登 录`（带空格） |
| 登录后默认落地路由 | <落地 URL，如 /index> |
| 顶部 toast 选择器 | `.el-message:has-text("xxx")` |
| 二次确认按钮 | `.el-message-box__btns button:has-text("确 定")` |
| 通用按钮文案空格 | RuoYi/Element-Plus 中文按钮文案常带空格：`登 录` / `确 定` / `取 消` / `保 存` / `搜 索`。下文用例为可读性写作【确定】【取消】【保存】【登录】。**自动化定位用 `:has-text("确 定")` 把空格写进字面量**——实测 `:text-matches("确\\s*定")` count=0，Playwright text engine 不识 ASCII 空格（详见 element-plus-pitfalls.md §坑 10） |

### <一级模块名> N 个子页面菜单导航与路由

| 页面 | 左侧菜单路径 | 路由 | 工具栏按钮 | 顶部 Tab |
|----|----|----|----|----|
| <子页 1> | <一级> → <子页 1> | <route> | <按钮列表>（注明与 spec 差异） | <Tab 列表 或 —> |
| <子页 2> | <一级> → <子页 2> | <route> | <按钮列表> | <Tab 列表> |
| ... | | | | |

### 关键弹窗标题与按钮

| 触发动作 | 实际对话框标题 | 底部按钮 |
|----|----|----|
| <子页> → `新增` | <实际弹窗标题>（注明与 spec 差异，如「不是『新增 XXX』」） | `确 定` / `取 消` |
| <子页> → 行内 `编辑` | <实际弹窗标题> | `确 定` / `取 消` |

### 通用步骤宏（用于自动化脚本统一替换）

- **STEP_LOGIN**：访问 `<BASE_URL>` → 在登录页输入 `<USER>` / `<PWD>`（如出现验证码则填任意 4 位）→ 点击 `登 录` → 等待 `URL == <落地路由>`
- **STEP_NAV_<子页代号>**：点击左侧菜单 `<一级>` → `<子页名>` → 等待面包屑 `<面包屑>` 可见且 `URL == <route>`
- ...（每个子页一条）

> 后续测试步骤中如出现 `点击左侧菜单【<一级>】-【<子页>】`，等价于对应的 STEP_NAV 宏。
> 「测试数据：账号 <USER> / 密码 <PWD>」在所有用例中默认使用，仅在覆盖 STEP_LOGIN 流程的用例中显式声明。

## 需求点索引

| 需求编号 | 需求点名称 | 用例数 |
|----------|-----------|------:|
| ...      | ...       | ...   |

---

【测试用例信息】
- 用例编号：TC-XXX-001
- 用例名称：...
- 需求编号：...
（按项目既有用例格式继续）
