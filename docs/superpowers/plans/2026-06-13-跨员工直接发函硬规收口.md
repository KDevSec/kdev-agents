# 跨员工直接发函硬规收口（v1.5 硬规 2/7）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把「跨员工直接发函」从设计稿（合稿 §10.1）+ 散落的 reviewer prose，收口成通用编排 runtime 层的**显式 v1.5 硬规 2/7**——在 `kdev-flow-driver` 落一节统一硬规、caller 编排补对称边界 prose、加 pytest 硬校验钉住三条不变量。

**Architecture:** **不建新发函原语**（YAGNI——现集群只有两条真实发函路径：caller→reviewer 评审 gate 发函 §2.5c、上游→下游交付 handoff §2.4ter；无任意「员工 A 主动发函员工 B」的调用方，建了没人调，同 spec §2.2 缓建逻辑）。本期 = 把硬规**写显式 + 测试钉死**三不变量：① who→whom（编排↔编排直接发函不经 CEO=硬规2；业务/评审能力不直接对外、跨员工走编排=硬规4/5）② 发函=结构化 request（caller+caps+target+产物指针，走 B 轨文件交接，非自由对话/非内联回灌）③ 边界=评审给建议非拦截，🔴 经双重条件强制 FAIL 但走 caller 有界回流+escalate，**处置权+入账都在 caller**。守 G-008：编排仍走通用 driver，reviewer callee 不复用 driver、不 own flow。

**Tech Stack:** Markdown prose（plugin skill/agents/orchestration）+ pytest（`plugins/kdev-team/tests/`，纯文本断言，无运行时依赖，沿用 test_reviewer_*.py 风格）。

---

## File Structure（改动点）

| 文件 | 动作 | 责任 |
|---|---|---|
| `plugins/kdev-team/tests/test_dispatch_hard_rule.py` | **新建** | 硬校验跨员工发函硬规三不变量落在 runtime 层 + caller 对称 prose（新文件而非塞进 test_cross_employee_handoff.py——后者管「交付 handoff 数据交接」，本测试管「发函硬规」，责任分清）|
| `plugins/kdev-team/skills/kdev-flow-driver/SKILL.md` | **改**（§2.4ter 后插一节）| runtime 契约权威落点：§2.4quater 统一硬规 prose |
| `plugins/kdev-team/skills/kdev-flow-driver/references/node-agent-routing.md` | **改**（reviewer 发函 dispatch 段加指针）| 交叉链接到 §2.4quater，防散落漂移 |
| `plugins/kdev-team/staff.yml` | **改**（头注释加一行）| staff 约定层：callee=被发函、不 own flow 的语义钉一句 |
| `plugins/kdev-team/agents/dev-engineer-orchestrator.md` | **改**（Principles 加一行）| caller 侧对称边界：发函=结构化、处置权在本编排 |
| `plugins/kdev-team/agents/req-architect-orchestrator.md` | **改**（Principles 加一行）| 同上 |

---

## Task 1: 跨员工发函硬规校验测试（红）

**Files:**
- Create: `plugins/kdev-team/tests/test_dispatch_hard_rule.py`

- [ ] **Step 1: Write the failing test**

写入 `plugins/kdev-team/tests/test_dispatch_hard_rule.py`：

```python
"""跨员工直接发函硬规（v1.5 硬规 2/7）落在 runtime 层 + caller 对称 prose 的硬校验。

钉三不变量：① who→whom（编排↔编排不经 CEO=硬规2；能力不直接对外、跨员工走编排=硬规4/5）
② 发函=结构化 request（非自由对话，走 B 轨文件交接）③ 边界=建议非拦截、处置权+入账在 caller。
守 G-008：编排走通用 driver，reviewer callee 不复用 driver/不 own flow。
"""
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
SKILL = ROOT / "skills/kdev-flow-driver/SKILL.md"
NAR = ROOT / "skills/kdev-flow-driver/references/node-agent-routing.md"
STAFF = ROOT / "staff.yml"
DEV_ORCH = ROOT / "agents/dev-engineer-orchestrator.md"
REQ_ORCH = ROOT / "agents/req-architect-orchestrator.md"

SECTION_ANCHOR = "2.4quater"


def _t(p):
    return p.read_text(encoding="utf-8")


def _section():
    """取 SKILL §2.4quater 整段（到下一节 §2.5 前）。"""
    t = _t(SKILL)
    assert SECTION_ANCHOR in t, "SKILL 缺 §2.4quater 跨员工发函硬规节"
    start = t.index(SECTION_ANCHOR)
    end = t.index("### 2.5", start)
    return t[start:end]


def test_skill_has_consolidated_dispatch_hard_rule_section():
    """SKILL 有统一的「跨员工直接发函硬规」节，标题点名 v1.5 硬规 2/7。"""
    sec = _section()
    assert "跨员工" in sec and "发函" in sec and "硬规" in sec
    assert "2/7" in sec  # 显式 v1.5 硬规编号


def test_invariant_1_who_to_whom():
    """不变量①：编排↔编排不经 CEO（硬规2）；能力不直接对外、跨员工走编排（硬规4/5）。"""
    sec = _section()
    assert "硬规 2" in sec and "硬规 4" in sec and "硬规 5" in sec
    assert "不经 CEO" in sec
    assert "编排" in sec and ("不直接对外" in sec or "不能能力直连" in sec)
    # 发函只 dispatch 对方 orchestrator，caller 不直派对方 cap
    assert "orchestrator" in sec
    assert ("不直接派" in sec) or ("不直接派对方" in sec) or ("不直接 dispatch" in sec)


def test_invariant_2_structured_not_freeform():
    """不变量②：发函=结构化 request（caller+caps+产物指针），走 B 轨，非自由对话。"""
    sec = _section()
    assert "结构化" in sec
    assert "request" in sec.lower()
    assert ("非自由对话" in sec) or ("不是" in sec and "自然语言" in sec)
    assert "B 轨" in sec
    # 结构化字段三件：谁发给谁 + 评什么 + 产物指针
    assert "caller" in sec
    assert ("caps" in sec) or ("target_paths" in sec)
    assert ("产物指针" in sec) or ("standards_refs" in sec) or ("artifacts" in sec)


def test_invariant_3_advice_not_block_caller_decides():
    """不变量③：评审给建议非拦截；处置权+入账在 caller；🔴经双重条件走有界回流。"""
    sec = _section()
    assert ("建议" in sec) and ("非拦截" in sec or "不直接命令" in sec or "不自己 halt" in sec)
    assert "caller 自主判断" in sec or "自主判断" in sec
    assert "处置权" in sec
    assert "record-gate" in sec  # 入账归 caller
    assert "双重通过条件" in sec or "双重条件" in sec
    assert "有界回流" in sec


def test_section_guards_g008_callee_not_own_flow():
    """守 G-008：编排走通用 kdev-flow-driver；callee 不复用 driver、不 own flow-state。"""
    sec = _section()
    assert "G-008" in sec
    assert "kdev-flow-driver" in sec
    assert "不复用" in sec
    assert "flow-state" in sec or "不 own flow" in sec or "不持有" in sec


def test_section_links_canonical_hard_rule_declaration():
    """硬规节回链 canonical 声明（概念合稿 §10.1），防 runtime 与设计稿漂移。"""
    sec = _section()
    assert "10.1" in sec  # 合稿 §10.1 通信硬规
    assert "合稿" in sec or "概念模型" in sec


def test_node_routing_points_to_hard_rule_section():
    """node-agent-routing reviewer 发函段交叉指向 §2.4quater（防硬规散落漂移）。"""
    t = _t(NAR)
    assert SECTION_ANCHOR in t


def test_staff_yml_notes_callee_dispatch_semantics():
    """staff.yml 头注释钉一句：callee=被发函、不 own flow。"""
    t = _t(STAFF)
    head = t[: t.index("employees:")]
    assert "发函" in head
    assert "callee" in head
    assert "不 own flow" in head or "不持有 flow" in head or "无自有 flow" in head


def test_caller_orchestrators_have_dispatch_boundary_prose():
    """两 caller 编排 Principles 有对称发函边界：结构化请求 + 处置权在本编排。"""
    for orch in (DEV_ORCH, REQ_ORCH):
        t = _t(orch)
        assert "结构化" in t, f"{orch.name} 缺『结构化请求』边界"
        assert "处置权" in t, f"{orch.name} 缺『处置权在本编排』边界"
        assert "2.4quater" in t, f"{orch.name} 未指向硬规节"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd plugins/kdev-team && python3 -m pytest tests/test_dispatch_hard_rule.py -v`
Expected: FAIL — 9 个用例全红（SKILL 无 §2.4quater；`_section()` 的 `assert SECTION_ANCHOR in t` 即抛 AssertionError）。

- [ ] **Step 3: Commit the failing test**

```bash
git add plugins/kdev-team/tests/test_dispatch_hard_rule.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "test(kdev-team): 跨员工发函硬规 2/7 三不变量校验(红)"
```

---

## Task 2: kdev-flow-driver SKILL §2.4quater 统一硬规节（绿主体）

**Files:**
- Modify: `plugins/kdev-team/skills/kdev-flow-driver/SKILL.md`（在 §2.4ter 段末、`### 2.5 gate 节点 → 判断` 之前插入）

- [ ] **Step 1: 插入 §2.4quater 整节**

在 `### 2.5 gate 节点 → 判断`（约 L238）这一行之前，插入：

````markdown
### 2.4quater 跨员工直接发函硬规（v1.5 硬规 2/7）⭐

§2.4ter 给的是「**怎么发函**」（机制：B 轨 handoff write/read）。本段钉死「**谁能给谁发 / 发的是什么 / 对方说了算不算**」——把[概念模型合稿 §10.1 通信硬规](../../../../docs/framework/01-design/2026-06-10-02-KDev数字员工-概念模型与员工能力-合稿-v1.0.md) 的硬规 2/4/5 收口成**编排 runtime 契约**，全集群发函（caller→reviewer 评审、上游→下游交付）共守。canonical 声明在合稿 §10.1，本段是其运行时落地。

**① 谁能给谁发函（who→whom）：**
- 🔴 **员工编排能力之间直接发函、不经 CEO**（硬规 2）：员工 A 编排 → 员工 B 编排（3 跳：A 编排→B 编排→B 能力→回流）。CEO 只在 4 场景介入（阶段聚合 / 异常升级 / 用户问询 / 评审 3 次循环升级），日常协作不掺和。
- 🔴 **业务/评审能力不直接对外**（硬规 4/5）：能力（`dev-engineer-frontend`、`reviewer-code`…）只跟自家编排对话；跨员工协作**必须走编排能力**，不能能力直连别家能力。发函**只 dispatch 对方的 `<employee>-orchestrator`**（评审专家即 `kdev-team:reviewer-orchestrator`），由它内部 fan-out 自家 cap——**caller 不直接派对方的 cap agent**。
- 评审专家是 **callee**：被 caller review gate 发函调用，**只回函给请评审的那个 caller 编排**，从不反向命令 caller、不跨员工另联、不 halt caller flow。

**② 发函 = 结构化请求，非自由对话：**
- 发函落**结构化 request 文件**（`handoffs/reviewer/<gate>.request.json` 或交付 manifest），字段 = **谁发给谁**（`caller`）+ **评什么/要什么**（`caps` / `target_paths` / `gate_input`）+ **产物指针**（`standards_refs` / `artifacts` / `thresholds`）。**不是**往主会话糊一段自然语言指令。
- 走 **B 轨**（`run_in_background` + 文件交接），**不读 subagent 内联返回**——执行甩后台、决策留主控（避免结果回灌刷屏，MQ-1）。回函同样落结构化文件（`<gate>.handoff.json` / handoff status），caller `handoff-read` 取字段。

**③ 边界：评审给建议、caller 自主判断（非硬拦截）：**
- 评审专家**只产出**评分表 + 🔴/🟡/⚪ 分级**建议** + verdict，**不直接命令** caller 改什么。🟡/⚪ 由 caller 编排 **自主判断**修 or tech-debt 化。
- 🔴 阻断**有牙齿但经 gate**：经**双重通过条件**（`total≥阈值 AND 🔴=0`）强制该能力 FAIL（总分再高也不放行），但兑现路径是 **caller flow 的有界回流 + escalate**（kdev-core 执行），评审专家**不自己 halt/command** caller。
- **处置权 + 入账都在 caller**：caller 读 verdict → 自主决定回流修哪些 → 自己调 `record-gate --by reviewer-expert` 入账（评审专家不碰 kdev-core 状态机）。3 次不过 → `status=blocked` 升 CEO → 用户拍板，escalate 不 force-accept。

**守 [G-008]**：编排**仍走通用 `kdev-flow-driver`**——caller（flow-owner）用本 skill 驱动自己的 node-table，到 review gate 按 §2.5c 发函；评审专家（callee）**不复用本 driver、不持有自己的 `flow-state`、不写 `features/<slug>/` 流程账**，只产评分表寄生 caller flow 贡献一个 verdict。别给 callee 造假 node-table（spec §2.1）。

````

- [ ] **Step 2: Run test to verify the SKILL-side cases pass**

Run: `cd plugins/kdev-team && python3 -m pytest tests/test_dispatch_hard_rule.py -v -k "skill or invariant or section"`
Expected: PASS — `test_skill_has_consolidated_dispatch_hard_rule_section`、3 个 `test_invariant_*`、`test_section_guards_g008_callee_not_own_flow`、`test_section_links_canonical_hard_rule_declaration` 全绿。（`test_node_routing` / `test_staff` / `test_caller_orchestrators` 仍红——Task 3 补。）

- [ ] **Step 3: Commit**

```bash
git add plugins/kdev-team/skills/kdev-flow-driver/SKILL.md
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-flow-driver): §2.4quater 跨员工直接发函硬规 2/7 收口(runtime 契约)"
```

---

## Task 3: 交叉指针 + staff 约定 + caller 对称边界 prose（绿收尾）

**Files:**
- Modify: `plugins/kdev-team/skills/kdev-flow-driver/references/node-agent-routing.md`（reviewer 发函 dispatch 段开头）
- Modify: `plugins/kdev-team/staff.yml`（头注释，`employees:` 之前）
- Modify: `plugins/kdev-team/agents/dev-engineer-orchestrator.md`（Principles，L16 后）
- Modify: `plugins/kdev-team/agents/req-architect-orchestrator.md`（Principles，L16 后）

- [ ] **Step 1: node-agent-routing 加交叉指针**

在 `# reviewer 发函 dispatch（review gate 发函评审专家）` 标题段的首段（现 L124「适用于**两员工**…本段定义**发函时的上下文构造**。」）末尾追加一句：

```markdown

> **发函硬规（who→whom / 结构化非自由对话 / 建议非拦截·caller 自主判断）见 `SKILL.md` §2.4quater 跨员工直接发函硬规（v1.5 硬规 2/7）。** 本段只管「上下文怎么构造」，硬规边界以 §2.4quater 为准，勿在两处各写一份免漂移。
```

- [ ] **Step 2: staff.yml 头注释钉 callee 语义**

在 `staff.yml` 第 1 行注释块里（`employees:` 之前），把现有 callee 说明补成（在第 2 行 `# 守 spec §0.5 命名规范…` 之后插一行）：

```yaml
# callee 员工（kind: callee，如 reviewer）= 被 caller 编排发函调用、不 own flow（不复用 kdev-flow-driver、
#   不持有 flow-state）、只产评分表回函；跨员工发函硬规（who→whom/结构化/建议非拦截）见 kdev-flow-driver SKILL §2.4quater。
```

- [ ] **Step 3: dev-engineer-orchestrator 加对称边界**

在 `dev-engineer-orchestrator.md` 的 Principles，`- 业务能力只对自家编排（硬规5），不外联其他员工。`（L16）之后插一行：

```markdown
- **发函边界（硬规 2/4/5，详见 kdev-flow-driver §2.4quater）**：发函评审专家=**结构化请求**（写 `request.json`：caller+caps+target+产物指针，走 B 轨，非自由对话），只 dispatch `kdev-team:reviewer-orchestrator`（不直接派对方 cap）；评审专家只给评分表+分级建议，**处置权在本编排**——🟡/⚪ 自主判断修 or tech-debt，🔴 经双重通过条件 FAIL 走有界回流，入账自己调 `record-gate --by reviewer-expert`。
```

- [ ] **Step 4: req-architect-orchestrator 加对称边界**

在 `req-architect-orchestrator.md` 的 Principles，`- 业务能力只对自家编排（硬规5），不外联其他员工。`（L16）之后插一行（同款，措辞对齐 design-flow）：

```markdown
- **发函边界（硬规 2/4/5，详见 kdev-flow-driver §2.4quater）**：发函评审专家=**结构化请求**（写 `request.json`：caller+caps+target+产物指针，走 B 轨，非自由对话），只 dispatch `kdev-team:reviewer-orchestrator`（不直接派对方 cap）；评审专家只给评分表+分级建议，**处置权在本编排**——🟡/⚪ 自主判断修 or tech-debt，🔴 经双重通过条件 FAIL 走有界回流，入账自己调 `record-gate --by reviewer-expert`。
```

- [ ] **Step 5: Run full hard-rule test + full kdev-team suite**

Run: `cd plugins/kdev-team && python3 -m pytest tests/test_dispatch_hard_rule.py -v && python3 -m pytest tests/ -q`
Expected: `test_dispatch_hard_rule.py` 9/9 PASS；全量套件 52+9 = 61 passed（无回归）。

- [ ] **Step 6: Commit**

```bash
git add plugins/kdev-team/skills/kdev-flow-driver/references/node-agent-routing.md plugins/kdev-team/staff.yml plugins/kdev-team/agents/dev-engineer-orchestrator.md plugins/kdev-team/agents/req-architect-orchestrator.md
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-team): 发函硬规 2/7 交叉指针 + staff callee 约定 + caller 对称边界 prose"
```

---

## Task 4: bump version + CHANGELOG + roadmap 回写

**Files:**
- Modify: `plugins/kdev-team/.claude-plugin/plugin.json`（version）
- Modify: `.claude-plugin/marketplace.json`（kdev-team version，若有镜像字段）
- Modify: `plugins/kdev-team/CHANGELOG.md`
- Modify: roadmap §1.5.8 / 阶段3 行（`docs/framework/01-design/2026-06-06-01-数字员工整体设计路线图roadmap-v1.0.md`）

- [ ] **Step 1: bump kdev-team version**

读 `plugins/kdev-team/.claude-plugin/plugin.json` 现版本（v0.6.0）→ bump 到 `0.7.0`（新增硬规收口 = minor）。同步 `.claude-plugin/marketplace.json` 里 kdev-team 的 version 字段（若存在镜像）。

- [ ] **Step 2: CHANGELOG 追条目**

`plugins/kdev-team/CHANGELOG.md` 顶部加：

```markdown
## 0.7.0 — 跨员工直接发函硬规收口（v1.5 硬规 2/7）

- kdev-flow-driver SKILL 新增 §2.4quater：把「跨员工直接发函」从设计稿收口成 runtime 契约——三不变量（who→whom 不经 CEO·硬规2/4/5 / 发函=结构化 request 非自由对话·B 轨 / 评审给建议非拦截·处置权+入账在 caller）。
- node-agent-routing reviewer 发函段 + staff.yml 头注释 + dev/req 两 caller 编排 Principles 补对称指针/边界 prose（防散落漂移）。
- 新增 test_dispatch_hard_rule.py（9 用例）硬校验三不变量 + G-008 + canonical 回链。
- 守 G-008：编排仍走通用 kdev-flow-driver，reviewer callee 不复用 driver、不 own flow（无新发函原语，YAGNI）。
- 附：评审专家 reviewer 活体接线轻 dogfood 跑通一条真实 g-sr-review（验证记录见 docs/superpowers/specs/2026-06-13-评审专家reviewer-dogfood验证.md）。
```

- [ ] **Step 3: roadmap 回写**

把 roadmap §1.5.8 / 阶段3 里「跨员工直接发函硬规 | ⏳（v1.5 硬规 2/7）」一行改为 `✅`（done），注明落点 = kdev-flow-driver §2.4quater + test_dispatch_hard_rule.py，并标「review gate 实测通（g-sr-review dogfood）」。

- [ ] **Step 4: Commit**

```bash
git add plugins/kdev-team/.claude-plugin/plugin.json .claude-plugin/marketplace.json plugins/kdev-team/CHANGELOG.md docs/framework/01-design/2026-06-06-01-数字员工整体设计路线图roadmap-v1.0.md docs/superpowers/specs/2026-06-13-评审专家reviewer-dogfood验证.md docs/superpowers/plans/2026-06-13-跨员工直接发函硬规收口.md
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "chore(kdev-team): bump 0.7.0 — 发函硬规 2/7 收口 + dogfood 验证留证 + roadmap 回写"
```

> **G-004 提示**：bump 后须提示用户**刷新 marketplace + 重启 session** 才能激活 v0.7.0（否则 cache stale）。本期 #2 改动是 SKILL/agents/staff prose + 测试，dogfood #1 已在 v0.6.0 激活态下跑过，不依赖 v0.7.0 激活。

---

## 非目标（防镀金 / YAGNI）

- **不建任意「员工 A 主动发函员工 B」的 node-table 原语**：现无调用方（只有 review gate 发函 + 交付 handoff 两条实路径），建了没人调，违 YAGNI + spec §2.2 缓建逻辑。硬规声明「编排↔编排可直接发函」是**约束**，不是要现在就造通用调度器。
- **不动 kdev-core 引擎**：发函/回函全复用 B 轨 handoff（§2.4ter）+ record-gate，零新引擎。
- **不碰 mode-2 / 记忆 Step 他评**：Q-017 已摘除移交蒸馏 audit stage，与本硬规解耦。
- **不重写 reviewer-orchestrator 边界 prose**：L14-15 已是 callee 侧权威，本期只补 caller 侧对称 + 收口到通用 driver 一处统一声明。

## Self-Review

- **Spec coverage**：任务 #2「核对已落 + 补显式硬规（who→whom / 结构化 / 建议非拦截·caller 自主判断）落 kdev-flow-driver / staff / agent prose，守 G-008」→ Task 1（测试钉三不变量）+ Task 2（SKILL §2.4quater 三不变量+G-008+canonical 回链）+ Task 3（node-routing 指针 + staff 约定 + caller 对称 prose）全覆盖。bump+回写 → Task 4。✅
- **Placeholder scan**：无 TBD/TODO；每段 prose、每条测试断言均给全文。✅
- **一致性**：测试断言的关键串（`2.4quater` / `硬规 2`/`4`/`5` / `不经 CEO` / `结构化` / `request` / `非自由对话` / `B 轨` / `caller` / `caps`/`target_paths` / `产物指针`/`standards_refs`/`artifacts` / `建议`+`非拦截`/`不直接命令` / `自主判断` / `处置权` / `record-gate` / `双重通过条件` / `有界回流` / `G-008` / `kdev-flow-driver` / `不复用` / `flow-state` / `10.1` / `合稿` / `不直接派`）逐条对应 Task 2/3 prose 里的字面串，已交叉核对。staff 测试取 `employees:` 之前的头注释、caller 测试查 `结构化`+`处置权`+`2.4quater`——Task 3 prose 均含。✅
