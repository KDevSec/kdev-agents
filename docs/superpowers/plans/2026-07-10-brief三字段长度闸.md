# brief 三字段长度闸 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 kdev-memory SessionStart brief 加长度闸，把 `current_step` / `pending_decisions` / `unresolved_gotchas` 三个 verbatim 无界注入字段钳到阈值内，防撑爆每会话开局上下文。

**Architecture:** 新增纯函数模块 `brief_clamp.py`（clamp + WARN 文案），config 层加分字段阈值读取，`session-start-brief.py` 在读三字段后单点接入 clamp + 原始长度判 WARN（覆盖 normal/compact/resume 三档），文档层约束 recorder/SKILL 减缓源头膨胀。

**Tech Stack:** Python 3.7+ stdlib only（无 yaml 依赖）、pytest。

**Spec:** [docs/superpowers/specs/2026-07-10-brief三字段长度闸-design.md](../specs/2026-07-10-brief三字段长度闸-design.md)

## Global Constraints

- **stdlib only**：不引入 `yaml` 或任何第三方依赖；config 沿用 `memory_config.py` 的 line-parser。
- **Python 3.7+ 兼容**：类型注解用 `from __future__ import annotations`；`List`/`Tuple` 从 `typing` 导（匹配现有 lib 风格）。
- **commit 身份铁规**：每次 commit 必须用 `git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "..."`（项目 `block-unattributed-commit` hook 硬校验，**不加** `Co-Authored-By` 或任何 AI 署名 trailer；`-c` 的 `key=value` 不加引号）。
- **执行前开分支**：当前在 `main`，实施前先开 feature 分支（如 `feat/brief-field-clamp`）。
- **plugin version bump（G-004）**：改了 hook/agent，收尾 Task 5 必须 bump `plugins/kdev-memory/.claude-plugin/plugin.json` `0.20.0 → 0.21.0`，否则用户侧 marketplace cache stale。
- **测试命令**：`python3 -m pytest plugins/kdev-memory/tests/<file> -v`。

---

### Task 1: brief_clamp.py — clamp_field + format_bloat_hint

**Files:**
- Create: `plugins/kdev-memory/hooks/lib/brief_clamp.py`
- Test: `plugins/kdev-memory/tests/test_brief_clamp.py`

**Interfaces:**
- Consumes: 无（纯函数，无项目依赖）。
- Produces:
  - `clamp_field(value: str, limit: int) -> str` — `len(value) <= limit` 或 `limit <= 0` 时原样返回；否则 `value[:limit] + "…⟨+N 字符已折叠，完整见 .kdev/memory/当前状态.md⟩"`。
  - `format_bloat_hint(bloat: List[Tuple[str, int, int]]) -> str` — `bloat=[(field_name, orig_len, limit)]`；空 → `""`；否则一条以 `  - 📈 frontmatter 字段膨胀：` 开头的 P1 hint 行。

- [ ] **Step 1: Write the failing test**

Create `plugins/kdev-memory/tests/test_brief_clamp.py`:

```python
"""test brief_clamp：clamp_field 头部保留+尾部指针 + format_bloat_hint（brief 三字段长度闸）。"""
from __future__ import annotations

import sys
from pathlib import Path

LIB = Path(__file__).parent.parent / "hooks" / "lib"
sys.path.insert(0, str(LIB))

from brief_clamp import clamp_field, format_bloat_hint


def test_clamp_under_limit_unchanged():
    assert clamp_field("abc", 10) == "abc"


def test_clamp_at_limit_unchanged():
    assert clamp_field("abcde", 5) == "abcde"


def test_clamp_over_limit_truncates_with_pointer():
    out = clamp_field("a" * 100, 10)
    assert out.startswith("a" * 10)
    assert "已折叠" in out
    assert "当前状态.md" in out
    assert "+90" in out  # 100 - 10


def test_clamp_empty_string():
    assert clamp_field("", 10) == ""


def test_clamp_limit_zero_is_unlimited():
    assert clamp_field("a" * 100, 0) == "a" * 100


def test_clamp_limit_negative_is_unlimited():
    assert clamp_field("a" * 100, -5) == "a" * 100


def test_clamp_utf8_counts_code_points():
    # 中文每字 1 code point；limit=3 → 保留前 3 个汉字，折叠 3 个
    out = clamp_field("一二三四五六", 3)
    assert out.startswith("一二三")
    assert "已折叠" in out
    assert "+3" in out


def test_clamp_emoji_returns_valid_str():
    out = clamp_field("😀" * 10, 4)
    assert out.startswith("😀" * 4)
    assert isinstance(out, str)


def test_format_bloat_hint_empty():
    assert format_bloat_hint([]) == ""


def test_format_bloat_hint_single():
    hint = format_bloat_hint([("pending_decisions", 2456, 1200)])
    assert "pending_decisions" in hint
    assert "2456" in hint
    assert "归档" in hint
    assert hint.startswith("  - 📈")


def test_format_bloat_hint_multi():
    hint = format_bloat_hint([("current_step", 900, 400), ("pending_decisions", 2500, 1200)])
    assert "current_step" in hint
    assert "pending_decisions" in hint
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m pytest plugins/kdev-memory/tests/test_brief_clamp.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'brief_clamp'`

- [ ] **Step 3: Write minimal implementation**

Create `plugins/kdev-memory/hooks/lib/brief_clamp.py`:

```python
"""kdev-memory brief 三字段长度闸（防 verbatim 无界 frontmatter 撑爆开局上下文）。

见 docs/superpowers/specs/2026-07-10-brief三字段长度闸-design.md。
"""
from __future__ import annotations

from typing import List, Tuple


def clamp_field(value: str, limit: int) -> str:
    """超 limit 则头部保留 + 尾部折叠指针。

    - Python str 按 code point 切片，UTF-8 不裂多字节。
    - limit <= 0 视为不限（no-op），防误配 0 把字段全砍。
    """
    if limit <= 0 or len(value) <= limit:
        return value
    folded = len(value) - limit
    return value[:limit] + f"…⟨+{folded} 字符已折叠，完整见 .kdev/memory/当前状态.md⟩"


def format_bloat_hint(bloat: List[Tuple[str, int, int]]) -> str:
    """bloat = [(field_name, orig_len, limit), ...]。空 → ""；否则一条 P1 hint 行。

    调用方保证 limit > 0（见 session-start-brief 接入点），故 orig // lim 不除零。
    """
    if not bloat:
        return ""
    items = "；".join(
        f"{name} 已 {orig} 字符（超阈值 {lim} 的 {orig // lim}×）"
        for name, orig, lim in bloat
    )
    return (
        f"  - 📈 frontmatter 字段膨胀：{items} —— "
        f"建议把旧条目归档进 每日汇总/，current_step/pending 只留短指针"
        f"（详见 dev-note 上下文消耗诊断）"
    )
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m pytest plugins/kdev-memory/tests/test_brief_clamp.py -v`
Expected: PASS（11 passed）

- [ ] **Step 5: Commit**

```bash
git add plugins/kdev-memory/hooks/lib/brief_clamp.py plugins/kdev-memory/tests/test_brief_clamp.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-memory): brief_clamp 长度闸纯函数——clamp_field 头部保留+尾部指针 + format_bloat_hint"
```

---

### Task 2: memory_config.py — read_brief_field_limits

**Files:**
- Modify: `plugins/kdev-memory/hooks/lib/memory_config.py`（在 `read_distill_thresholds` 后新增函数 + 3 个默认常量）
- Test: `plugins/kdev-memory/tests/test_brief_field_limits.py`

**Interfaces:**
- Consumes: `_read_config`、`_read_int_default`（memory_config.py 已有）。
- Produces: `read_brief_field_limits(kdev_dir=".kdev/memory") -> dict` → `{"current_step": int, "pending_decisions": int, "unresolved_gotchas": int}`。默认 400 / 1200 / 800。

- [ ] **Step 1: Write the failing test**

Create `plugins/kdev-memory/tests/test_brief_field_limits.py`:

```python
"""test memory_config.read_brief_field_limits（brief 三字段长度闸阈值）。"""
from __future__ import annotations

import sys
from pathlib import Path

LIB = Path(__file__).parent.parent / "hooks" / "lib"
sys.path.insert(0, str(LIB))

from memory_config import read_brief_field_limits


def test_defaults_when_no_config(tmp_path):
    limits = read_brief_field_limits(tmp_path)  # 无 config.yaml
    assert limits["current_step"] == 400
    assert limits["pending_decisions"] == 1200
    assert limits["unresolved_gotchas"] == 800


def test_config_override(tmp_path):
    (tmp_path / "config.yaml").write_text(
        "brief.limit_current_step: 100\n"
        "brief.limit_pending_decisions: 500\n"
        "brief.limit_unresolved_gotchas: 300\n",
        encoding="utf-8",
    )
    limits = read_brief_field_limits(tmp_path)
    assert limits["current_step"] == 100
    assert limits["pending_decisions"] == 500
    assert limits["unresolved_gotchas"] == 300


def test_invalid_value_fails_open_to_default(tmp_path):
    (tmp_path / "config.yaml").write_text(
        "brief.limit_current_step: notanumber\n", encoding="utf-8")
    limits = read_brief_field_limits(tmp_path)
    assert limits["current_step"] == 400  # fail-open


def test_underscore_key_variant(tmp_path):
    (tmp_path / "config.yaml").write_text(
        "brief_limit_pending_decisions: 999\n", encoding="utf-8")
    limits = read_brief_field_limits(tmp_path)
    assert limits["pending_decisions"] == 999
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m pytest plugins/kdev-memory/tests/test_brief_field_limits.py -v`
Expected: FAIL with `ImportError: cannot import name 'read_brief_field_limits'`

- [ ] **Step 3: Write minimal implementation**

In `plugins/kdev-memory/hooks/lib/memory_config.py`, add after the `read_distill_thresholds` function (before `def main()`):

```python
DEFAULT_BRIEF_LIMIT_CURRENT_STEP = 400
DEFAULT_BRIEF_LIMIT_PENDING = 1200
DEFAULT_BRIEF_LIMIT_UNRESOLVED = 800


def read_brief_field_limits(kdev_dir: Path | str = ".kdev/memory") -> dict:
    """读 brief 三字段长度闸阈值（current_step / pending_decisions / unresolved_gotchas）。

    未配置 / 非法 → 各自默认（fail-open）。兼容 dot-key 与下划线 flat-key。
    """
    config = _read_config(kdev_dir)
    return {
        "current_step": _read_int_default(
            config, ("brief.limit_current_step", "brief_limit_current_step"),
            DEFAULT_BRIEF_LIMIT_CURRENT_STEP),
        "pending_decisions": _read_int_default(
            config, ("brief.limit_pending_decisions", "brief_limit_pending_decisions"),
            DEFAULT_BRIEF_LIMIT_PENDING),
        "unresolved_gotchas": _read_int_default(
            config, ("brief.limit_unresolved_gotchas", "brief_limit_unresolved_gotchas"),
            DEFAULT_BRIEF_LIMIT_UNRESOLVED),
    }
```

Also add these keys to the module docstring's 字段 list (after the `brief.verbosity` line):

```
- brief.limit_current_step / brief.limit_pending_decisions / brief.limit_unresolved_gotchas —— brief 三字段长度闸阈值（默认 400 / 1200 / 800）
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m pytest plugins/kdev-memory/tests/test_brief_field_limits.py -v`
Expected: PASS（4 passed）

- [ ] **Step 5: Commit**

```bash
git add plugins/kdev-memory/hooks/lib/memory_config.py plugins/kdev-memory/tests/test_brief_field_limits.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-memory): memory_config 加 read_brief_field_limits——分字段阈值 400/1200/800 + fail-open"
```

---

### Task 3: session-start-brief.py 接线 clamp + WARN

**Files:**
- Modify: `plugins/kdev-memory/hooks/session-start-brief.py`（import + `main()` clamp/WARN 块 + `_build_brief` 加 `field_bloat_hint` 参数 + P1 区 + compact 分支 + brief_kwargs）
- Test: `plugins/kdev-memory/tests/test_brief_verbosity.py`（扩展 2 例）

**Interfaces:**
- Consumes: `clamp_field`、`format_bloat_hint`（Task 1）；`read_brief_field_limits`（Task 2）。
- Produces: `_build_brief(..., field_bloat_hint: str = "")` 新增末位关键字参数。

- [ ] **Step 1: Write the failing test**

Append to `plugins/kdev-memory/tests/test_brief_verbosity.py`:

```python
def test_brief_clamps_long_pending_without_warn(tmp_path):
    """pending 1500 字符（>1200 clamp，<2400 无 WARN）→ 折叠指针出现、无膨胀 WARN。"""
    repo = _init(tmp_path, config_text="rating.mode: model-only\n")
    mem = repo / ".kdev" / "memory"
    (mem / "当前状态.md").write_text(
        f"---\nphase: t\npending_decisions: [{'x' * 1500}]\n---\n", encoding="utf-8")
    ctx = _ctx(repo)
    assert "已折叠" in ctx        # clamp 触发
    assert "字段膨胀" not in ctx  # WARN 未触发


def test_brief_warns_on_bloated_pending(tmp_path):
    """pending 2500 字符（>2400）→ 折叠指针 + P1 膨胀 WARN 同现。"""
    repo = _init(tmp_path, config_text="rating.mode: model-only\n")
    mem = repo / ".kdev" / "memory"
    (mem / "当前状态.md").write_text(
        f"---\nphase: t\npending_decisions: [{'x' * 2500}]\n---\n", encoding="utf-8")
    ctx = _ctx(repo)
    assert "已折叠" in ctx      # clamp 触发
    assert "字段膨胀" in ctx    # WARN 触发
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m pytest plugins/kdev-memory/tests/test_brief_verbosity.py -v -k "clamp or bloat"`
Expected: FAIL —— `assert "已折叠" in ctx` fails（当前无 clamp，长 pending 原样注入）。

- [ ] **Step 3: Write minimal implementation**

**3a.** Modify the memory_config import line (`session-start-brief.py:42`):

```python
from memory_config import read_rating_mode, rating_mode_configured, read_brief_verbosity, read_brief_field_limits  # noqa: E402
```

Add a new import right after it:

```python
from brief_clamp import clamp_field, format_bloat_hint  # noqa: E402
```

**3b.** In `main()`, immediately after the six `state_* = read_state_field(...)` lines (currently `:557-562`), insert:

```python

    # 三字段长度闸：clamp 注入值 + 以原始长度判 WARN（brief 三字段长度闸 spec）
    _limits = read_brief_field_limits(kdev_dir)
    _triples = [("current_step", state_step, _limits["current_step"]),
                ("pending_decisions", state_pending, _limits["pending_decisions"]),
                ("unresolved_gotchas", state_unresolved, _limits["unresolved_gotchas"])]
    _bloat = [(n, len(v), lim) for n, v, lim in _triples if lim > 0 and len(v) > lim * 2]
    field_bloat_hint = format_bloat_hint(_bloat)
    state_step = clamp_field(state_step, _limits["current_step"])
    state_pending = clamp_field(state_pending, _limits["pending_decisions"])
    state_unresolved = clamp_field(state_unresolved, _limits["unresolved_gotchas"])
```

**3c.** In `_build_brief` signature, add a parameter after `verbosity: str = "normal",` (currently `:376`):

```python
    verbosity: str = "normal",
    field_bloat_hint: str = "",
) -> str:
```

**3d.** In `_build_brief`, P1 block assembly (currently `:393`), insert the bloat hint before `if missing_past:`:

```python
    # P1: 跨天汇总缺失 / CLAUDE.md 漂移 / 历史半残 / 沉淀提醒 + 字段膨胀 WARN
    if field_bloat_hint:
        p1_lines.append(field_bloat_hint)
    if missing_past:
```

**3e.** In `_build_brief`, the `verbosity == "compact"` early-return branch, after the `state_pending` append (currently `:447-448`):

```python
            if state_pending:
                cparts.append(f"- pending_decisions: {state_pending}")
            if field_bloat_hint:
                cparts.append(field_bloat_hint)
```

**3f.** In `main()`, `brief_kwargs` dict, add to the last line (currently `:596`):

```python
        fallback_hint=_fallback_upgrade_hint(kdev_dir, today), verbosity=verbosity,
        field_bloat_hint=field_bloat_hint,
    )
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m pytest plugins/kdev-memory/tests/test_brief_verbosity.py -v`
Expected: PASS（原 5 例 + 新 2 例 = 7 passed）

- [ ] **Step 5: Commit**

```bash
git add plugins/kdev-memory/hooks/session-start-brief.py plugins/kdev-memory/tests/test_brief_verbosity.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-memory): SessionStart brief 三字段接入 clamp+WARN——单点覆盖 normal/compact/resume 三档"
```

---

### Task 4: 文档约束 — recorder + SKILL 短指针铁规

**Files:**
- Modify: `plugins/kdev-memory/agents/kdev-step-recorder.md`（`:65` 后加 bullet）
- Modify: `plugins/kdev-memory/skills/kdev-memory/SKILL.md`（`:182` 句尾追加）

**Interfaces:** 无（纯文档，无测试）。

- [ ] **Step 1: 改 recorder.md**

In `plugins/kdev-memory/agents/kdev-step-recorder.md`, find the line (`:65`):

```
- v0.17: `current_step` 值为时间戳 Step ID（如 `Step 20260613-101432-ly1989abc`），不再是 `<slug>-N`。
```

Append a new bullet immediately after it:

```
- **短指针铁规（brief 长度闸）**：`current_step` 只写**最新** Step ID（+可选 ≤ 一句状态），**严禁** `|【上一里程碑】` 式把旧里程碑叙事拼接进同一字段——该字段被 SessionStart brief 每会话全量注入，无界拼接会撑爆开局上下文（见 docs/superpowers/specs/2026-07-10-brief三字段长度闸-design.md）。
```

- [ ] **Step 2: 改 SKILL.md**

In `plugins/kdev-memory/skills/kdev-memory/SKILL.md`, find the line (`:182`) ending with `**每次完成 Step 都要顺手改** \`current_step\` + \`last_updated\`。` and append to that same line:

```
 `current_step` 是**短指针**（只留最新 Step ID + 可选一句状态，禁止无界拼接旧里程碑）；`pending_decisions` / `unresolved_gotchas` 超阈值时把旧条目归档进 `每日汇总/`——三字段都被 SessionStart brief 每会话注入，长度闸会截断超限部分（见 2026-07-10-brief三字段长度闸 spec）。
```

- [ ] **Step 3: 验证无破坏**

Run: `python3 -m pytest plugins/kdev-memory/tests/test_claude_md_lint.py -v`
Expected: PASS（文档改动不应破坏 lint；若该测试不涉及这两文件则仍 PASS）

- [ ] **Step 4: Commit**

```bash
git add plugins/kdev-memory/agents/kdev-step-recorder.md plugins/kdev-memory/skills/kdev-memory/SKILL.md
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "docs(kdev-memory): recorder+SKILL 补 current_step 短指针铁规——禁止里程碑无界拼接（配合 brief 长度闸）"
```

---

### Task 5: 收尾 — R-009 回写 + G-004 version bump

**Files:**
- Modify: `docs/skills/kdev-memory/dev-notes/kdev-memory上下文消耗诊断-2026-07-10.md`（§7.4 第 2 条回写 + 加锚）
- Modify: `plugins/kdev-memory/.claude-plugin/plugin.json`（version bump）

**Interfaces:** 无。

- [ ] **Step 1: 回写 dev-note §7.4-2（R-009）**

In `docs/skills/kdev-memory/dev-notes/kdev-memory上下文消耗诊断-2026-07-10.md`, find the §7.4 list item 2:

```
2. **§5.1-2 闸位前移**：写入侧（recorder / step 落盘）设阈值 WARN 或拒绝拼接，比注入侧更根治；注入侧长度闸作最后兜底。
```

Replace it with:

```
2. **§5.1-2 闸位（已被 spec 修正）**：⚠️ 已被 [2026-07-10-brief三字段长度闸 spec](../../superpowers/specs/2026-07-10-brief三字段长度闸-design.md) §1 修订（R-009）——原设想「写入侧闸位前移更根治」**不成立**：三字段无 Python 写入路径（全靠 LLM 手写进 md），写入侧无确定性拦截点，只能 WARN 检测 + 文档约束；**注入侧长度闸才是唯一代码里确定生效的拦截点**。以 spec 为准。
```

- [ ] **Step 2: version bump（G-004）**

In `plugins/kdev-memory/.claude-plugin/plugin.json`:

```json
  "version": "0.21.0",
```

（从 `"version": "0.20.0",` 改）

- [ ] **Step 3: 跑全套 kdev-memory 测试确认全绿**

Run: `python3 -m pytest plugins/kdev-memory/tests/ -q`
Expected: PASS（含新增 test_brief_clamp.py、test_brief_field_limits.py、扩展的 test_brief_verbosity.py，及全部既有回归）

- [ ] **Step 4: Commit**

```bash
git add docs/skills/kdev-memory/dev-notes/kdev-memory上下文消耗诊断-2026-07-10.md plugins/kdev-memory/.claude-plugin/plugin.json
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "chore(kdev-memory): bump 0.21.0——brief 三字段长度闸 + dev-note §7.4 R-009 回写（写入侧无拦截点修正）"
```

---

## 完成标准

- 5 个 task 全部 commit；`python3 -m pytest plugins/kdev-memory/tests/ -q` 全绿。
- 健康项目（字段未超阈值）brief 行为字节级不变（现有 5 例回归绿即证）。
- G-004 version 已 bump 0.21.0；R-009 回写清单（dev-note §7.4-2 + SKILL.md）已消。
- **实施后需用户侧刷 marketplace 激活 0.21.0**（G-004，plan 外，属交付提醒）。
