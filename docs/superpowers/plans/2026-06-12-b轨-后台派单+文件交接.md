# B 轨：编排派单 run_in_background 化 + 文件交接 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 kdev-flow-driver 驱动循环里业务员工的前台同步派单改成 `run_in_background:true` + 约定路径 JSON 文件交接，压掉"派单刷屏主会话"主痛点；搭车修 MQ-1（recorder 返回机器块 → 一句人话）。

**Architecture:** 三层改动——(1) kdev-core 新增 `write_handoff_status`/`read_handoff_status` + CLI `handoff-write`/`handoff-read`（交接协议的代码 + 真读写单测，落在 `handoff_dir` 旁，即 test_handoffs.py 当初注的"协议留 P-B"位置）；(2) kdev-team 的 kdev-flow-driver SKILL §2.4 派单从前台同步改后台 + 文件交接，gate 判断仍留主循环（守 §1.5.6 B 轨边界：执行甩后台，决策留主控）；(3) kdev-memory 的 kdev-step-recorder 收尾返回一句人话（MQ-1）。三插件各 bump version + CHANGELOG（G-004）。

**Tech Stack:** Python 3.12（kdev-core lib + argparse CLI）、pytest（kdev-core 真读写单测 + kdev-team/kdev-memory 散文不变量测试）、Claude Code `Agent({run_in_background:true})` + completion 通知（驱动循环胶水，散文承载，靠 dogfood 验证）。

---

## 关键约束（贯穿全程，违反即返工）

- **只改派单方式**：不碰 namespaced（fd3e059 已修）、不碰 gate 决策位置（仍主控）、不碰 node-table.yml、不碰 agent persona `.md` 定义。让 agent 写交接文件的指令走**派单 prompt**（SKILL §4 模板），不改 persona。
- **gate 同步问真人仍在主循环**：§2.5 gate 段**绝不**加 `run_in_background`——这正是 A 方案做不了、B 保留的价值。有一条 kdev-team 测试硬守这条。
- **文件交接落 `.kdev/features/<slug>/handoffs/<员工>/`**（P-Core-FF 既有约定，kdev-core `handoff_dir` 已建目录），**不自创新目录**。
- **可观测**：每个后台 agent 完成/失败靠 completion 通知 + 交接文件 `status` 双信号；`handoff-read` 找不到文件/格式坏 = 显式 `FlowStateError`，当作该节点未完成，不静默跳过。
- **守 Q-008**：编排=主控驱动；SKILL 仍是驱动循环胶水，不把编排下放给 subagent。
- **§1.5.6 "B 轨" ≠ §1.5.2 "P-B"**：两者都带字母 B 但不是一回事。本计划只回写 §1.5.6 + §1.5.7 MQ-1，**不碰 P-B（跨员工 handoff）相关行**。

## 数据契约（交接状态文件）

路径：`.kdev/features/<slug>/handoffs/<员工 canonical-id>/<node_id>.handoff.json`

```json
{
  "node_id": "n3-plan",
  "employee": "dev-engineer",
  "status": "done",
  "summary": "PLAN.md 写完，3 增量 + TDD 序列",
  "artifacts": ["delivery/PLAN.md"],
  "gate_input": {"build": "pass"},
  "reason": null
}
```

- `status` ∈ `done | blocked | needs_context`
- `summary`：一句人话（必填）
- `artifacts`：产物路径列表（可空）
- `gate_input`：给下一个 gate 的结构化信号，可为 `null`
- `reason`：`status != done` 时必填，否则 `null`

## 文件结构（改哪些文件、各自职责）

| 文件 | 动作 | 职责 |
|---|---|---|
| `plugins/kdev-core/kdev_core/flow_state.py` | Modify | 加 `HANDOFF_STATUSES` + `_handoff_status_path` + `write_handoff_status` + `read_handoff_status`（落在 `handoff_dir` 旁，L390 附近） |
| `plugins/kdev-core/tests/test_handoffs.py` | Modify | 加交接状态真读写 + 校验单测 |
| `plugins/kdev-core/kdev_core/cli.py` | Modify | 加 `cmd_handoff_write`/`cmd_handoff_read` + 两个 subparser（仿 `handoff-path` L313） |
| `plugins/kdev-core/tests/test_new_cli.py` | Modify | 加 CLI round-trip 单测 |
| `plugins/kdev-core/.claude-plugin/plugin.json` | Modify | version 0.2.0 → 0.3.0 |
| `plugins/kdev-core/CHANGELOG.md` | Modify | 0.3.0 条目 |
| `plugins/kdev-team/skills/kdev-flow-driver/SKILL.md` | Modify | §2.4 后台派单改造 + 新增 §2.4bis 文件交接协议 + §4 模板加写交接指令 |
| `plugins/kdev-team/skills/kdev-flow-driver/references/node-agent-routing.md` | Modify | 顶部加一行交接文件交叉引用（不改路由映射本体） |
| `plugins/kdev-team/tests/test_bg_dispatch.py` | Create | 散文不变量：派单含 run_in_background + 交接协议 + gate 不被后台化 |
| `plugins/kdev-team/.claude-plugin/plugin.json` | Modify | version 0.3.1 → 0.4.0 |
| `plugins/kdev-team/CHANGELOG.md` | Modify | 0.4.0 条目 |
| `plugins/kdev-memory/agents/kdev-step-recorder.md` | Modify | Return format 成功分支 → 一句人话（MQ-1） |
| `plugins/kdev-memory/tests/test_recorder_return_oneline.py` | Create | 散文不变量：成功返回无机器块、是一句人话 |
| `plugins/kdev-memory/.claude-plugin/plugin.json` | Modify | version 0.15.0 → 0.15.1 |
| `plugins/kdev-memory/CHANGELOG.md` | Modify | 0.15.1 条目 |
| `docs/framework/01-design/2026-06-06-01-数字员工整体设计路线图roadmap-v1.0.md` | Modify | §1.5.6 B done + §1.5.7 MQ-1 销账 |

**工作目录**：本计划在 worktree `.claude/worktrees/feat+b-track-bg-dispatch`（branch `worktree-feat+b-track-bg-dispatch`，已含 fd3e059 prereq；`.kdev` symlink 已通）。所有相对路径以 worktree 根为基。

**测试运行**：`export PYTHONPATH=plugins/kdev-core:plugins/kdev-memory/hooks/lib`（kdev-team 测试自带 conftest 注 kdev-core）。

**AI commit 身份**（每个 commit 步统一用）：
```bash
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "<msg>"
```
（key=value 不加引号；无 Co-Authored-By；不要 push。）

---

## Task 1: kdev-core 交接状态读写函数

**Files:**
- Modify: `plugins/kdev-core/kdev_core/flow_state.py`（在 `handoff_dir` 之后，约 L398）
- Test: `plugins/kdev-core/tests/test_handoffs.py`

- [ ] **Step 1: 写失败测试**

在 `test_handoffs.py` 末尾追加：

```python
import pytest
from kdev_core.flow_state import write_handoff_status, read_handoff_status, FlowStateError


def test_write_read_handoff_status_roundtrip(tmp_workspace):
    p = write_handoff_status(
        tmp_workspace, "user-auth", "dev-engineer", "n3-plan",
        "done", "PLAN.md 写完",
        artifacts=["delivery/PLAN.md"], gate_input={"build": "pass"})
    assert p.name == "n3-plan.handoff.json"
    assert p.parent.name == "dev-engineer"
    data = read_handoff_status(tmp_workspace, "user-auth", "dev-engineer", "n3-plan")
    assert data["node_id"] == "n3-plan"
    assert data["employee"] == "dev-engineer"
    assert data["status"] == "done"
    assert data["summary"] == "PLAN.md 写完"
    assert data["artifacts"] == ["delivery/PLAN.md"]
    assert data["gate_input"] == {"build": "pass"}
    assert data["reason"] is None


def test_write_handoff_status_rejects_bad_status(tmp_workspace):
    with pytest.raises(FlowStateError):
        write_handoff_status(tmp_workspace, "s", "e", "n", "finished", "x")


def test_write_handoff_status_requires_reason_when_not_done(tmp_workspace):
    with pytest.raises(FlowStateError):
        write_handoff_status(tmp_workspace, "s", "e", "n", "blocked", "stuck")
    write_handoff_status(tmp_workspace, "s", "e", "n", "blocked", "stuck",
                         reason="env missing")
    assert read_handoff_status(tmp_workspace, "s", "e", "n")["reason"] == "env missing"


def test_write_handoff_status_requires_summary(tmp_workspace):
    with pytest.raises(FlowStateError):
        write_handoff_status(tmp_workspace, "s", "e", "n", "done", "")


def test_read_handoff_status_missing_raises(tmp_workspace):
    with pytest.raises(FlowStateError):
        read_handoff_status(tmp_workspace, "nope", "e", "n")


def test_read_handoff_status_malformed_raises(tmp_workspace):
    from kdev_core.flow_state import handoff_dir
    d = handoff_dir(tmp_workspace, "s", "e")
    (d / "n.handoff.json").write_text("{not json", encoding="utf-8")
    with pytest.raises(FlowStateError):
        read_handoff_status(tmp_workspace, "s", "e", "n")
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd <worktree根> && PYTHONPATH=plugins/kdev-core python3 -m pytest plugins/kdev-core/tests/test_handoffs.py -q`
Expected: FAIL（`ImportError: cannot import name 'write_handoff_status'`）

- [ ] **Step 3: 实现函数**

在 `flow_state.py` 的 `handoff_dir`（L390-397）之后追加（注意 `json`/`_feature_dir`/`FlowStateError` 文件顶部已有）：

```python
HANDOFF_STATUSES = ("done", "blocked", "needs_context")


def _handoff_status_path(workspace, slug, employee, node_id) -> Path:
    return _feature_dir(workspace, slug) / "handoffs" / employee / f"{node_id}.handoff.json"


def write_handoff_status(workspace, slug, employee, node_id, status, summary,
                         artifacts=None, gate_input=None, reason=None):
    """落一份结构化交接状态到 handoffs/<employee>/<node_id>.handoff.json。

    业务 agent 收尾调它（经 CLI），主循环靠 read_handoff_status 读，
    替代把大段 result 回灌主会话（B 轨压刷屏核心）。返回写入的 Path。
    """
    if status not in HANDOFF_STATUSES:
        raise FlowStateError(
            f"invalid handoff status {status!r}; must be one of {HANDOFF_STATUSES}")
    if not node_id:
        raise FlowStateError("node_id required")
    if not summary:
        raise FlowStateError("summary required (one-line human signal)")
    if status != "done" and not reason:
        raise FlowStateError(f"reason required when status={status!r}")
    handoff_dir(workspace, slug, employee)  # mkdir -p（幂等）
    path = _handoff_status_path(workspace, slug, employee, node_id)
    payload = {
        "node_id": node_id,
        "employee": employee,
        "status": status,
        "summary": summary,
        "artifacts": list(artifacts or []),
        "gate_input": gate_input,
        "reason": reason,
    }
    path.write_text(json.dumps(payload, ensure_ascii=False, indent=2) + "\n",
                    encoding="utf-8")
    return path


def read_handoff_status(workspace, slug, employee, node_id):
    """读 + 校验交接状态文件，返回 dict。

    缺文件/坏 JSON/缺必填键 → FlowStateError，让"后台 agent 没写/写坏"
    显式冒泡（主循环当作该节点未完成），不静默当成功。
    """
    path = _handoff_status_path(workspace, slug, employee, node_id)
    if not path.exists():
        raise FlowStateError(f"handoff status not found: {path}")
    try:
        data = json.loads(path.read_text(encoding="utf-8"))
    except (ValueError, OSError) as exc:
        raise FlowStateError(f"handoff status unreadable: {path}: {exc}") from exc
    for key in ("node_id", "employee", "status", "summary"):
        if key not in data:
            raise FlowStateError(f"handoff status missing required key {key!r}: {path}")
    if data["status"] not in HANDOFF_STATUSES:
        raise FlowStateError(f"handoff status invalid status {data['status']!r}: {path}")
    return data
```

- [ ] **Step 4: 运行测试确认通过**

Run: `PYTHONPATH=plugins/kdev-core python3 -m pytest plugins/kdev-core/tests/test_handoffs.py -q`
Expected: PASS（2 旧 + 6 新 = 8 passed）

- [ ] **Step 5: Commit**

```bash
git add plugins/kdev-core/kdev_core/flow_state.py plugins/kdev-core/tests/test_handoffs.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-core): 交接状态读写 write/read_handoff_status（B 轨文件交接协议）"
```

---

## Task 2: kdev-core CLI handoff-write / handoff-read + bump

**Files:**
- Modify: `plugins/kdev-core/kdev_core/cli.py`（cmd 函数区 + build_parser subparser 区）
- Test: `plugins/kdev-core/tests/test_new_cli.py`
- Modify: `plugins/kdev-core/.claude-plugin/plugin.json`、`plugins/kdev-core/CHANGELOG.md`

- [ ] **Step 1: 写失败测试**

在 `test_new_cli.py` 末尾追加（`json` 已在文件顶部 import）：

```python
def test_cli_handoff_write_read_roundtrip(tmp_workspace, run_cli):
    out = run_cli(["handoff-write", "coding-flow", "f",
                   "--employee", "dev-engineer", "--node", "n3-plan",
                   "--status", "done", "--summary", "PLAN done",
                   "--artifact", "delivery/PLAN.md",
                   "--gate-input", '{"build":"pass"}']).strip()
    assert out.endswith("/n3-plan.handoff.json")
    read = run_cli(["handoff-read", "coding-flow", "f",
                    "--employee", "dev-engineer", "--node", "n3-plan"])
    data = json.loads(read)
    assert data["status"] == "done"
    assert data["summary"] == "PLAN done"
    assert data["artifacts"] == ["delivery/PLAN.md"]
    assert data["gate_input"] == {"build": "pass"}
    assert data["reason"] is None
```

- [ ] **Step 2: 运行测试确认失败**

Run: `PYTHONPATH=plugins/kdev-core python3 -m pytest plugins/kdev-core/tests/test_new_cli.py::test_cli_handoff_write_read_roundtrip -q`
Expected: FAIL（`invalid choice: 'handoff-write'`，argparse 报错 → rc≠0）

- [ ] **Step 3: 实现 CLI**

在 `cli.py` 的 `cmd_handoff_path`（L147-150）之后追加两个 cmd 函数：

```python
def cmd_handoff_write(args):
    gi = json.loads(args.gate_input) if args.gate_input else None
    p = flow_state.write_handoff_status(
        args.workspace, args.slug, args.employee, args.node, args.status,
        args.summary, artifacts=args.artifact, gate_input=gi, reason=args.reason)
    print(str(p))
    return 0


def cmd_handoff_read(args):
    data = flow_state.read_handoff_status(
        args.workspace, args.slug, args.employee, args.node)
    print(json.dumps(data, ensure_ascii=False, indent=2))
    return 0
```

在 `build_parser()` 里 `handoff-path` subparser（L313-315）之后追加：

```python
    phw = _common(sub, "handoff-write")
    phw.add_argument("--employee", required=True)
    phw.add_argument("--node", required=True)
    phw.add_argument("--status", required=True,
                     choices=["done", "blocked", "needs_context"])
    phw.add_argument("--summary", required=True)
    phw.add_argument("--artifact", action="append", default=[])
    phw.add_argument("--gate-input", default=None, dest="gate_input")
    phw.add_argument("--reason", default=None)
    phw.set_defaults(func=cmd_handoff_write)

    phr = _common(sub, "handoff-read")
    phr.add_argument("--employee", required=True)
    phr.add_argument("--node", required=True)
    phr.set_defaults(func=cmd_handoff_read)
```

（`json` 在 cli.py 顶部已 import；若没有则补 `import json`。先 grep 确认：`grep -n "^import json" plugins/kdev-core/kdev_core/cli.py`。）

- [ ] **Step 4: 运行测试确认通过 + 全量回归**

Run: `PYTHONPATH=plugins/kdev-core python3 -m pytest plugins/kdev-core/tests/ -q`
Expected: PASS（126 旧 + 1 CLI 新 + Task1 的 6 = 133 passed）

- [ ] **Step 5: bump kdev-core + CHANGELOG**

`plugins/kdev-core/.claude-plugin/plugin.json`：`"version": "0.2.0"` → `"0.3.0"`。

`plugins/kdev-core/CHANGELOG.md` 顶部插入（紧跟首个 `# ...` 标题行后）：

```markdown
## 0.3.0 — 2026-06-12

**B 轨文件交接协议：交接状态读写（落 handoff_dir 旁，承 test_handoffs.py「协议留 P-B」预留）**

- `flow_state.write_handoff_status` / `read_handoff_status`：结构化交接状态落 `.kdev/features/<slug>/handoffs/<员工>/<node_id>.handoff.json`；schema = node_id/employee/status(done|blocked|needs_context)/summary/artifacts/gate_input/reason。读端缺文件/坏 JSON/缺键 → FlowStateError（显式冒泡，不静默）。
- CLI 新增 `handoff-write` / `handoff-read`（仿 `handoff-path`）：业务 agent 收尾 write、主循环 read，替代大段 result 回灌主会话。
- 给 kdev-flow-driver B 轨（run_in_background 派单 + 文件交接）用；R2/R3 纯函数零改。
- 测试：test_handoffs.py +6（真读写 + 校验），test_new_cli.py +1（CLI round-trip）。
```

- [ ] **Step 6: Commit**

```bash
git add plugins/kdev-core/kdev_core/cli.py plugins/kdev-core/tests/test_new_cli.py plugins/kdev-core/.claude-plugin/plugin.json plugins/kdev-core/CHANGELOG.md
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-core): handoff-write/read CLI + bump v0.3.0（B 轨文件交接 CLI 入口）"
```

---

## Task 3: kdev-flow-driver SKILL 后台派单改造 + 文件交接协议 + bump

**Files:**
- Modify: `plugins/kdev-team/skills/kdev-flow-driver/SKILL.md`（§2.4 + 新增 §2.4bis + §4 模板）
- Modify: `plugins/kdev-team/skills/kdev-flow-driver/references/node-agent-routing.md`（顶部交叉引用一行）
- Create: `plugins/kdev-team/tests/test_bg_dispatch.py`
- Modify: `plugins/kdev-team/.claude-plugin/plugin.json`、`plugins/kdev-team/CHANGELOG.md`

- [ ] **Step 1: 写失败测试**

新建 `plugins/kdev-team/tests/test_bg_dispatch.py`：

```python
from pathlib import Path

SKILL = (Path(__file__).resolve().parents[1]
         / "skills/kdev-flow-driver/SKILL.md")


def _skill_text():
    return SKILL.read_text(encoding="utf-8")


def test_dispatch_uses_run_in_background():
    """§2.4 业务派单已改 run_in_background（B 轨核心）。"""
    assert "run_in_background" in _skill_text()


def test_dispatch_wires_handoff_protocol():
    """SKILL 接入文件交接：写/读 CLI + .handoff.json 命名。"""
    t = _skill_text()
    assert "handoff-write" in t
    assert "handoff-read" in t
    assert ".handoff.json" in t


def test_main_loop_reads_handoff_not_inline_return():
    """主循环靠完成通知 + 读交接文件，而非读 subagent 内联返回。"""
    t = _skill_text()
    assert "完成通知" in t or "completion" in t


def test_gate_section_not_backgrounded():
    """gate 判断仍在主循环——§2.5 gate 段绝不出现 run_in_background。"""
    t = _skill_text()
    start = t.index("### 2.5")
    end = t.index("### 2.6")
    assert "run_in_background" not in t[start:end]
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd <worktree根> && python3 -m pytest plugins/kdev-team/tests/test_bg_dispatch.py -q`
Expected: FAIL（`test_dispatch_uses_run_in_background` 等失败——SKILL 还没改）

- [ ] **Step 3: 改 SKILL §2.4 派单步骤**

把 `SKILL.md` §2.4（L142-165）整段**替换**为下面内容（保留小节标题 `### 2.4 action 节点 → 派业务 agent`）：

````markdown
### 2.4 action 节点 → 派业务 agent（后台 + 文件交接）

从 `next_actions` 得到下一步节点 id，但当前 action 节点本身需要先完成工作。派单逻辑（B 轨：执行甩后台、决策留主控）：

1. **读 Node→Agent 路由表**（见 `references/node-agent-routing.md`）确定当前节点对应哪个 `subagent_type`
2. **算交接目录**：
   ```bash
   python3 -m kdev_core handoff-path $FLOW <slug> --employee <canonical-id> --workspace <workspace>
   # → .kdev/features/<slug>/handoffs/<员工>/
   ```
3. **构造派单上下文**（§4 模板）：prompt 里必含——任务描述、当前节点目标、前序产物路径、工作目录、相关约束（UED/原型图等），**末尾必含「完成后写交接文件」指令**（见 §2.4bis + §4 模板）
4. **后台派 agent**（🔴 `subagent_type` 必须是插件全名 `kdev-team:<agent-id>`，裸 id not-found——见 `references/node-agent-routing.md` 顶部「派单标识」；🔴 `run_in_background: true` 是 B 轨核心）：
   ```
   Agent({
     subagent_type: "kdev-team:dev-engineer-env",
     run_in_background: true,
     prompt: "<构造的上下文 + 写交接文件指令>"
   })
   ```
   `run_in_background: true` 让 subagent 的工具调用、内联渲染、大段 result 全在后台 sidechain，**不灌主会话**——这是 B 轨压掉"派单刷屏"的核心。runtime_model 按 staff.yml（当前阶段1统一 opus）。
5. **等完成通知 → 读交接文件**（不读 subagent 内联返回）：
   - 后台 agent 完成时主循环收到 **completion 通知**。
   - 读结构化状态：
     ```bash
     python3 -m kdev_core handoff-read $FLOW <slug> --employee <id> --node <current_node> --workspace <workspace>
     # → {node_id, employee, status, summary, artifacts, gate_input, reason}
     ```
   - `status=done` → 取 `artifacts` / `gate_input` 作下一步（gate）输入。
   - `status=blocked` / `needs_context` → **不 advance**，按 BLOCKED 处理（§2.3，或带 `reason` 升人）。
   - ⚠️ **可观测**：completion 通知 + 交接文件 `status` 双信号；`handoff-read` 报 `handoff status not found / unreadable`（FlowStateError）= 该节点没干完，**不静默 advance**——按未完成处理（重派或升人）。
6. **推进到下一节点**（仅 `status=done`）：
   ```bash
   python3 -m kdev_core advance $FLOW <slug> <to_node> --table $NT \
     --reason "<node_name> 完成" --workspace <workspace>
   ```
   `to_node` 取 `next_actions[0].to_node`（action 节点通常只有一个 next）

### 2.4bis 文件交接协议（B 轨核心，最小够用）

业务 agent **不把产出大段 result 回灌主会话**，而是写一个结构化状态文件，主循环读它。

- **路径**：`.kdev/features/<slug>/handoffs/<员工 canonical-id>/<node_id>.handoff.json`（目录由 kdev-core `handoff_dir` 约定，P-Core-FF 已建；**不自创新目录**）。
- **schema**（kdev-core `write_handoff_status` 落、`read_handoff_status` 校验）：

  | 字段 | 含义 |
  |---|---|
  | `node_id` | 当前节点 id |
  | `employee` | 员工 canonical id |
  | `status` | `done` / `blocked` / `needs_context` |
  | `summary` | 一句人话（必填） |
  | `artifacts` | 产物路径列表（可空） |
  | `gate_input` | 给下一 gate 的结构化信号，可 `null` |
  | `reason` | `status != done` 时必填 |

- **agent 怎么写**：派单 prompt 末尾注入指令（见 §4 模板），让 agent 收尾调 `kdev_core handoff-write ...`。
- **主循环怎么读**：`handoff-read`（见 §2.4 step 5）。
- **为什么文件而非 result 回灌**：result 回灌刷屏主会话（连 recorder 的机器块都被反馈嫌吵，见 MQ-1）；文件交接 = 主循环只读它要的几个字段，编排叙述清爽。**gate 判断仍在主循环**（B 轨守界：执行甩后台、决策留主控）。
````

- [ ] **Step 4: 改 SKILL §4 上下文构造模板**

在 §4（`## 4. 上下文构造模板`）的现有 prompt 模板代码块之后，追加一段「完成后写交接文件」指令（这段会被主控拼进每次派单 prompt 的末尾）：

````markdown
派 agent 时，在上面模板**末尾追加**下面这段「写交接文件」指令（B 轨文件交接，REQUIRED）：

```
## 完成后写交接文件（REQUIRED）

完成本节点工作后，你【必须】把结构化状态写到交接文件（**不要**把大段产出回灌——产出落 artifacts，主控只读交接文件）：

PYTHONPATH=<FRAMEWORK_REPO>/plugins/kdev-core python3 -m kdev_core handoff-write <flow> <slug> \
  --employee <你的 canonical id> --node <当前 node_id> \
  --status done \
  --summary "<一句话：这步干完了什么>" \
  --artifact <产物路径> [--artifact <更多产物>] \
  [--gate-input '<给下一 gate 的 JSON，如 {"build":"pass","lint":"clean"}>'] \
  --workspace <workspace>

若未完成 / 被阻塞：把 --status 改成 blocked（或 needs_context）并加 --reason "<为什么>"。

你的 final message 只回一句话确认（如"n3-plan 完成，详见交接文件"）——详情在交接文件 + artifacts 里，**不要长篇回灌**。
```

（主控构造 prompt 时把 `<FRAMEWORK_REPO>` / `<flow>` / `<slug>` / `<node_id>` / `<canonical id>` / `<workspace>` 全部替换成实际值再派单。）
````

- [ ] **Step 5: node-agent-routing.md 加交叉引用**

在 `references/node-agent-routing.md` 顶部「派单标识」小节（L7 区块）之后，加一小段（**不改路由映射表本体**）：

```markdown
## 🔵 派单方式：后台 + 文件交接（B 轨）

业务派单一律 `run_in_background: true`，agent 收尾把状态写到
`.kdev/features/<slug>/handoffs/<员工>/<node_id>.handoff.json`（`kdev_core handoff-write`），
主控靠 completion 通知 + `handoff-read` 拿结果，**不读 subagent 内联返回**。
协议 schema + 派单步骤见 `SKILL.md` §2.4 / §2.4bis / §4。
（本表的产物落位规则——dev-engineer 落 `delivery/`、req-architect 落 `handoffs/`——不变；交接**状态文件**统一落 `handoffs/<员工>/`，内部用 `artifacts` 指向真实产物路径。）
```

- [ ] **Step 6: 运行测试确认通过**

Run: `python3 -m pytest plugins/kdev-team/tests/ -q`
Expected: PASS（22 旧 + 4 新 = 26 passed）

- [ ] **Step 7: bump kdev-team + CHANGELOG**

`plugins/kdev-team/.claude-plugin/plugin.json`：`"version": "0.3.1"` → `"0.4.0"`。

`plugins/kdev-team/CHANGELOG.md` 顶部插入（紧跟 `# Changelog` 后）：

```markdown
## 0.4.0 — 2026-06-12

**B 轨：编排派单 run_in_background 化 + 文件交接（§1.5.6 B done）**

- `kdev-flow-driver/SKILL.md` §2.4 业务派单：前台同步 `Agent({...})` → `run_in_background: true` + 约定路径 JSON 文件交接，去掉内联渲染 + 大段 result 回灌（~80% 混杂），压"派单刷屏主会话"主痛点。
- 新增 §2.4bis 文件交接协议（schema + 路径 + 读写 CLI），复用 kdev-step-recorder 已验 fire-and-forget 思路 + kdev-core `handoff-write`/`handoff-read`（v0.3.0）。
- §4 上下文模板加「完成后写交接文件」指令（注入派单 prompt 末尾；**不改 agent persona 定义**）。
- 主循环靠 completion 通知 + `handoff-read` 拿"干完没 + 产物路径 + gate 输入"；交接文件缺/坏 = 显式失败不静默 advance。
- **gate 判断仍在主循环**（§2.5 不后台化）——守 §1.5.6 B 轨边界：执行甩后台、决策留主控。
- 测试：新增 `test_bg_dispatch.py`（4 散文不变量，含「gate 不被后台化」硬守）。
- ⚠️ G-004：用户须刷 marketplace（`/plugin` 更新/重装）+ 重启 session 才生效。
```

- [ ] **Step 8: Commit**

```bash
git add plugins/kdev-team/skills/kdev-flow-driver/SKILL.md plugins/kdev-team/skills/kdev-flow-driver/references/node-agent-routing.md plugins/kdev-team/tests/test_bg_dispatch.py plugins/kdev-team/.claude-plugin/plugin.json plugins/kdev-team/CHANGELOG.md
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-team): kdev-flow-driver 后台派单 + 文件交接 + bump v0.4.0（§1.5.6 B 轨）"
```

---

## Task 4: MQ-1 — kdev-step-recorder 收尾返回一句人话 + bump

**Files:**
- Modify: `plugins/kdev-memory/agents/kdev-step-recorder.md`（Return format L216-234 + Constraints L243）
- Create: `plugins/kdev-memory/tests/test_recorder_return_oneline.py`
- Modify: `plugins/kdev-memory/.claude-plugin/plugin.json`、`plugins/kdev-memory/CHANGELOG.md`

- [ ] **Step 1: 写失败测试**

新建 `plugins/kdev-memory/tests/test_recorder_return_oneline.py`：

```python
from pathlib import Path

RECORDER = (Path(__file__).resolve().parents[1]
            / "agents/kdev-step-recorder.md")


def _success_block():
    t = RECORDER.read_text(encoding="utf-8")
    start = t.index("## Return format")
    section = t[start:]
    # 成功分支 = "On reject" 之前那段
    return section.split("On reject")[0]


def test_success_return_drops_machine_block():
    """MQ-1：成功不再回 STATUS/MINTED_ID/COUNTER 机器块。"""
    s = _success_block()
    assert "MINTED_ID:" not in s
    assert "COUNTER:" not in s


def test_success_return_is_one_line_human():
    """成功返回一句人话确认（含'执行日志'落点）。"""
    s = _success_block()
    assert "执行日志" in s


def test_reject_branch_still_structured():
    """拒绝分支保留结构化（主会话需照此修正重派）。"""
    t = RECORDER.read_text(encoding="utf-8")
    assert "STATUS: NEEDS_CONTEXT" in t
    assert "RULE_VIOLATED" in t
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python3 -m pytest plugins/kdev-memory/tests/test_recorder_return_oneline.py -q`
Expected: FAIL（`test_success_return_drops_machine_block`——当前成功块含 MINTED_ID）

- [ ] **Step 3: 改 recorder.md Return format**

把 `kdev-step-recorder.md` 的 `## Return format`（L216-234）整段**替换**为：

````markdown
## Return format

On success（**只回一句人话确认**——详情已写进 执行日志.md 文件本身，不回机器块；这是 MQ-1：机器块回灌主会话被反馈"突兀"）:

> 已落 Step <ID> → 执行日志.md（<scope> scope）；当前状态已同步、pending-commits 已清。

例：`已落 Step main-70 → 执行日志.md（shared scope）；当前状态已同步、pending 已清。`

（员工 scope 时省略"当前状态已同步"——员工 Step 不动主线 当前状态.md。）

On reject（保留结构化——主会话需照此修正后重派）:
```
STATUS: NEEDS_CONTEXT
RULE_VIOLATED: <number>
DETAIL: <one-line explanation>
SUGGESTED_FIX: <what main session should add/change>
```
````

并把 Constraints 末行（L243）：
```
- 返回精简：成功时只回上述 6 行审计字段，**不要**把写入的 4 段内容再贴回 stdout（主会话已知意图，详情在文件里）。
```
替换为：
```
- 返回精简：成功时**只回一句人话确认**（MQ-1），不回机器审计块、不贴 4 段内容（主会话已知意图，详情在文件里）。
```

- [ ] **Step 4: 运行测试确认通过 + 全量回归**

Run: `PYTHONPATH=plugins/kdev-memory/hooks/lib python3 -m pytest plugins/kdev-memory/tests/ -q`
Expected: PASS（323 旧 + 3 新 = 326 passed；recorder e2e 测的是 lib 契约，不碰 Return format 散文，不受影响）

- [ ] **Step 5: bump kdev-memory + CHANGELOG**

`plugins/kdev-memory/.claude-plugin/plugin.json`：`"version": "0.15.0"` → `"0.15.1"`。

`plugins/kdev-memory/CHANGELOG.md` 顶部插入（紧跟 `# kdev-memory CHANGELOG` 后）：

```markdown
## [0.15.1] — 2026-06-12

**MQ-1：kdev-step-recorder 收尾返回 6 行机器块 → 一句人话确认。**

### 🔄 变更

- `agents/kdev-step-recorder.md` Return format：成功分支从 `STATUS/MINTED_ID/COUNTER/SCOPE/RATING_MODE/TARGET` 6 行审计块，改为一句人话确认（如"已落 Step main-70 → 执行日志.md；当前状态已同步、pending 已清"）。fire-and-forget subagent 的 final text 即返回值、会回显主会话——机器块被反馈"突兀"（§1.5.7 MQ-1）。拒绝分支保留结构化（主会话需照此修正重派）。
- 直接配合 §1.5.6 B 轨：B 轨复用 fire-and-forget 批量派单，recorder 机器块回灌会抵消 B 轨"压刷屏"收益。
- 测试：新增 `test_recorder_return_oneline.py`（成功无机器块 + 一句人话 + 拒绝仍结构化）。

### 🧱 约束

- 不碰 lib 契约（mint/写盘/clear pending 不变，e2e 测试不受影响）；不碰 scope/评分模式逻辑。
- ⚠️ G-004：bump `0.15.0 → 0.15.1`，用户须刷 marketplace + 重启 session 才生效。
```

- [ ] **Step 6: Commit**

```bash
git add plugins/kdev-memory/agents/kdev-step-recorder.md plugins/kdev-memory/tests/test_recorder_return_oneline.py plugins/kdev-memory/.claude-plugin/plugin.json plugins/kdev-memory/CHANGELOG.md
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "fix(kdev-memory): recorder 收尾返回一句人话 + bump v0.15.1（MQ-1）"
```

---

## Task 5: 验证 —— 全量测试绿 + 手动 dogfood（压刷屏 + 主循环读后台结果）

**Files:** 无（验证任务，产出证据写进本计划末尾或 commit message）

- [ ] **Step 1: 三插件全量回归**

Run:
```bash
export PYTHONPATH=plugins/kdev-core:plugins/kdev-memory/hooks/lib
python3 -m pytest plugins/kdev-core/tests/ plugins/kdev-team/tests/ plugins/kdev-memory/tests/ -q
```
Expected: 全绿（kdev-core 133 + kdev-team 26 + kdev-memory 326）

- [ ] **Step 2: 手动 dogfood —— 后台派单 + 文件交接 round-trip**

目的：活体验证"派单不再刷屏主会话 + 主循环正确读到后台结果"。用 `general-purpose` agent（不依赖 kdev-team marketplace 激活，G-004 未刷也能跑），忠实复刻 B 轨机制（后台 + 交接文件）。

主控（本会话）执行：

1. 选 scratch slug（不污染真 flow）：`SLUG=dogfood-btrack`，`WS=<worktree根>`。
2. 后台派一个 agent，prompt 让它做一点"工作"后调 handoff-write：
   ```
   Agent({
     subagent_type: "general-purpose",
     model: "sonnet",
     run_in_background: true,
     description: "dogfood B-track handoff",
     prompt: "你在 <WS> 工作。做一点示意工作（echo 一行），然后调：
       PYTHONPATH=<WS>/plugins/kdev-core python3 -m kdev_core handoff-write coding-flow dogfood-btrack \
         --employee dev-engineer --node n3-plan --status done \
         --summary 'dogfood：PLAN 占位已写' --artifact delivery/PLAN.md \
         --gate-input '{\"build\":\"pass\"}' --workspace <WS>
       最后只回一句话确认，不要长篇回灌。"
   })
   ```
3. **观察主会话**：派单后只见「一次 Agent 调用 + 一次 completion 通知」，**不见** subagent 的逐条工具内联渲染 / 大段 result 回灌 → 证明压刷屏。
4. completion 后主控读交接文件：
   ```bash
   PYTHONPATH=plugins/kdev-core python3 -m kdev_core handoff-read coding-flow dogfood-btrack \
     --employee dev-engineer --node n3-plan --workspace .
   ```
   Expected: 打印 `{"node_id":"n3-plan", "status":"done", "summary":"dogfood：PLAN 占位已写", "artifacts":["delivery/PLAN.md"], "gate_input":{"build":"pass"}, ...}`
5. 反向验失败可观测：读一个不存在的节点：
   ```bash
   PYTHONPATH=plugins/kdev-core python3 -m kdev_core handoff-read coding-flow dogfood-btrack \
     --employee dev-engineer --node n99-nope --workspace . ; echo "rc=$?"
   ```
   Expected: `FlowStateError: handoff status not found` + rc≠0 → 证明"没干完=显式失败不静默"。
6. 清理 scratch：`rm -rf .kdev/features/dogfood-btrack`（`.kdev` 是 symlink → 删的是主仓 scratch，确认 slug 名独特无误后再删）。

- [ ] **Step 3: 记录 dogfood 证据**

把 Step 2 的观察（"主会话只见 1 派单 + 1 通知，无内联刷屏" + handoff-read 输出 + 失败 rc≠0）三条结论，作为本步 dogfood 证据，写进 Task 6 的 step-recorder dispatch summary 的 `key_facts`/`key_decisions`。

---

## Task 6: 回写 roadmap（§1.5.6 B done + §1.5.7 MQ-1 销账）+ G-004 提醒

**Files:**
- Modify: `docs/framework/01-design/2026-06-06-01-数字员工整体设计路线图roadmap-v1.0.md`

- [ ] **Step 1: §1.5.6 B 轨标 done**

把 §1.5.6 表里 B 行（L155）的「收益兑现」列从 `**立即（L2 协同就够用）**` 改为 `✅ **done（2026-06-12，分支 feat/b-track-bg-dispatch）**`，并在「决策：先 B 后 A」段（L158）「B 现在做」一条（L159）末尾追加一句：

```
✅ **B done（2026-06-12）**：kdev-flow-driver §2.4 业务派单 → `run_in_background:true` + `.kdev/features/<slug>/handoffs/<员工>/<node_id>.handoff.json` 文件交接（kdev-core v0.3.0 `handoff-write/read` + 校验；kdev-team v0.4.0 SKILL 改造，gate 仍主循环）；MQ-1 同步修（recorder 一句人话）。dogfood 验证主会话只见 1 派单+1 通知、无内联刷屏，主循环 `handoff-read` 拿到后台结果。三插件测试 kdev-core 133 / kdev-team 26 / kdev-memory 326 全绿。
```

- [ ] **Step 2: §1.5.7 MQ-1 销账**

把 §1.5.7 表 MQ-1 行（L172）「状态」列从 `⏳ 待修（...）` 改为：

```
✅ **已修（2026-06-12，kdev-memory v0.15.1，随 B 轨同会话）**：recorder 成功收尾改一句人话确认（"已落 Step …→执行日志.md；当前状态已同步、pending 已清"），拒绝分支保留结构化。
```

并把 L175 的脚注：
```
> 取证勘误：... MQ-1/MQ-2 本身留作 backlog，不在本次 push。
```
改为：
```
> 取证勘误：... MQ-1 已于 2026-06-12 随 B 轨修复（kdev-memory v0.15.1）；MQ-2（自评 confabulate / 第三方他评）仍留 backlog 随 M3。
```

- [ ] **Step 3: Commit**

```bash
git add "docs/framework/01-design/2026-06-06-01-数字员工整体设计路线图roadmap-v1.0.md"
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "docs(roadmap): §1.5.6 B 轨 done + §1.5.7 MQ-1 销账"
```

- [ ] **Step 4: G-004 提醒用户刷 marketplace**

向用户输出提醒（不是代码步）：本次 bump 三插件——kdev-core 0.3.0 / kdev-team 0.4.0 / kdev-memory 0.15.1。按 G-004，需用户 `/plugin` 更新或重装这三个插件 + 重启 session，新派单方式 / recorder 一句话返回才生效。

---

## Self-Review（写完计划后自查）

**Spec 覆盖**：
- 范围 1（§2.4 前台同步 → run_in_background + 文件交接，gate 留主循环）→ Task 3 ✓
- 范围 2（文件交接协议最小够用，复用 step-recorder 模式 + handoffs/ 约定）→ Task 1/2（代码+CLI）+ Task 3 §2.4bis（协议文档）✓
- 范围 3（MQ-1 recorder 一句人话）→ Task 4 ✓
- 范围 4（bump kdev-team + kdev-memory）→ Task 3 step 7 + Task 4 step 5 ✓；额外 bump kdev-core（用户已确认 3 插件）→ Task 2 step 5 ✓
- 硬约束（不碰 namespaced/gate 位置/node-table/agent 定义）→ Task 3 改 SKILL prose + 派单 prompt，不碰 persona；test_bg_dispatch 硬守 gate 不后台化 ✓
- 测试（派单构造 + 文件交接读写单测 + dogfood）→ Task 1/2（kdev-core 真读写）+ Task 3（kdev-team 派单不变量）+ Task 5（dogfood）✓
- 回写 roadmap + G-004 提醒 → Task 6 ✓

**Placeholder 扫描**：所有 code/命令步均有完整内容，无 TBD/TODO。SKILL/CHANGELOG/roadmap 编辑给出完整替换文本。✓

**类型一致**：`write_handoff_status(workspace, slug, employee, node_id, status, summary, artifacts, gate_input, reason)` 在 Task 1 定义、Task 2 CLI 调用、Task 5 dogfood 调用签名一致；JSON 字段（node_id/employee/status/summary/artifacts/gate_input/reason）在 schema、函数、CLI、测试、SKILL §2.4bis 表全一致。✓

**风险点**：dogfood（Task 5）依赖 `run_in_background` agent 在本 session 可派；若不可用，退化为「直接 CLI handoff-write 写一份 + handoff-read 读」证明文件交接读写闭环，并在证据里注明"后台派单部分受 session 能力限制改 CLI 直证"。
