# 阶段2 P-0：员工集中到 kdev-team Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新建 `plugins/kdev-team/` 数字员工集群插件，把阶段1 塞进 kdev-coding-flow 的 7 个 persona + node-table 搬成 kdev-team 的标准 CC agents + orchestration，kdev-coding-flow 回归纯方法论 skill。

**Architecture:** 员工 = `kdev-team/{agents/<canonical-cap>.md(真 CC frontmatter agent), orchestration/<canonical>.node-table.yml}` + `staff.yml` 花名册；flow-skill(kdev-coding-flow) 只剩方法论被 agent 参考。编排=agent 按 node-table 调度（守 Q-008），flow-skill 不充当编排。本 plan 只做 P-0 结构搬迁（不含 P-C1 记忆 scope / P-A 需求架构师 / P-B handoff）。

**Tech Stack:** Claude Code 插件（plugin.json + marketplace.json 注册 + agents/ 约定）· Python/pytest（结构校验，复用 kdev_core.node_machine 验 node-table）· YAML。

**配套**：spec [docs/superpowers/specs/2026-06-07-阶段2-第二员工+记忆scope-design.md](../specs/2026-06-07-阶段2-第二员工+记忆scope-design.md)（§0.5 命名规范 / §2.4 agent×skill / §3 P-0）。

**Commit 身份硬规**（每个 commit 步骤照用，`-c` 的 key=value **不加引号**，G-001/G-002）：
```
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "..."
```
**不要 push**（用户确认后推）。

**canonical 映射**（阶段1 persona → kdev-team agent id）：
| 源 persona | agent id (= 文件名 = subagent_type) |
|---|---|
| 开发工程师-编排 | `dev-engineer-orchestrator` |
| 环境准备 | `dev-engineer-env` |
| 实施计划 | `dev-engineer-plan` |
| 前端实现 | `dev-engineer-frontend` |
| E2E视觉验收 | `dev-engineer-e2e` |
| 部署上线 | `dev-engineer-deploy` |
| 安全扫描 | `dev-engineer-sec` |

> **G-004 备注（写进 README）**：kdev-team 是新插件，CC 要发现它需 ① marketplace.json 注册（本 plan 做）② **用户侧刷新 marketplace + 重启 session**（agent 不代劳）。dev-engineer-* 作为 subagent_type 可派要等用户刷新生效。

---

## File Structure

**新建 `plugins/kdev-team/`**
- `.claude-plugin/plugin.json` — 插件清单（name=kdev-team, version, description…）
- `agents/dev-engineer-{orchestrator,env,plan,frontend,e2e,deploy,sec}.md` — 7 个 CC frontmatter agent（搬自 personas + 加 frontmatter）
- `orchestration/dev-engineer.node-table.yml` — 编排结构（搬自 kdev-coding-flow）
- `staff.yml` — 花名册（canonical→中文 display + flow-skill 引用 + 运行时模型）
- `tests/{conftest.py, test_agents.py, test_orchestration_config.py}` — 结构 + node-table 一致性校验
- `README.md` — 集群说明 + G-004 刷新备注

**修改**
- `.claude-plugin/marketplace.json` — 加 kdev-team 条目
- `plugins/kdev-coding-flow/skills/kdev-coding-flow/SKILL.md` — 删「接 kdev-core 底座入口」节、改为指向 kdev-team 的一行指针；正文方法论保留
- 删 `plugins/kdev-coding-flow/skills/kdev-coding-flow/{personas/,orchestration/}`
- 删 `plugins/kdev-coding-flow/tests/{test_personas.py,test_orchestration_config.py}`（搬到 kdev-team）；保留 kdev-coding-flow 其它 tests（如 SKILL lint，若有）

---

## Task 1: kdev-team 插件骨架 + marketplace 注册

**Files:**
- Create: `plugins/kdev-team/.claude-plugin/plugin.json`
- Create: `plugins/kdev-team/README.md`
- Modify: `.claude-plugin/marketplace.json`
- Test: `plugins/kdev-team/tests/test_plugin_manifest.py`

- [ ] **Step 1: 写失败测试**（`plugins/kdev-team/tests/test_plugin_manifest.py`）

```python
import json
from pathlib import Path

ROOT = Path(__file__).resolve().parents[3]              # 仓根
PLUGIN = Path(__file__).resolve().parents[1]            # plugins/kdev-team


def test_plugin_json_valid():
    d = json.loads((PLUGIN / ".claude-plugin/plugin.json").read_text(encoding="utf-8"))
    assert d["name"] == "kdev-team"
    assert d["version"]
    assert d["description"]


def test_registered_in_marketplace():
    mk = json.loads((ROOT / ".claude-plugin/marketplace.json").read_text(encoding="utf-8"))
    entry = [p for p in mk["plugins"] if p["name"] == "kdev-team"]
    assert len(entry) == 1
    assert entry[0]["source"] == "./plugins/kdev-team"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd plugins/kdev-team && python3 -m pytest tests/test_plugin_manifest.py -v`
Expected: FAIL（plugin.json / marketplace 条目 不存在）

- [ ] **Step 3: 写 plugin.json**（`plugins/kdev-team/.claude-plugin/plugin.json`，格式参 kdev-memory）

```json
{
  "name": "kdev-team",
  "description": "KDev 数字员工集群：编排 + 业务能力 agent（瘦 persona，按 node-table 调度 kdev-core 引擎）+ per-员工 orchestration node-table + staff.yml 花名册。员工 = canonical id（dev-engineer/req-architect…）+ 中文 display；agent 参考各自 flow-skill(kdev-coding-flow/kdev-design-flow) 方法论 + call 能力 skill。守 Q-008 结构进底座/执行=agent 驱动。",
  "version": "0.1.0",
  "author": { "name": "ly" },
  "license": "MIT",
  "keywords": ["digital-employee", "agent", "orchestration", "staff", "kdev"]
}
```

- [ ] **Step 4: 注册进 marketplace.json**

在 `.claude-plugin/marketplace.json` 的 `plugins` 数组追加（紧跟 kdev-memory 条目风格）：

```json
    {
      "name": "kdev-team",
      "description": "KDev 数字员工集群：编排+业务 agent + per-员工 node-table + staff.yml 花名册（员工集中总目录）",
      "category": "development",
      "source": "./plugins/kdev-team"
    }
```

- [ ] **Step 5: 写 README.md**（`plugins/kdev-team/README.md`）

```markdown
# kdev-team — KDev 数字员工集群

员工集中总目录。每个员工 = `agents/<canonical>-<cap>.md`（瘦 persona，CC subagent）+ `orchestration/<canonical>.node-table.yml`（编排结构）。花名册见 `staff.yml`。

## 模型（守 spec §2.4）
- **编排 = 编排 agent 按 node-table 调度**业务 agent + 驱动 kdev-core CLI；**不是调 flow-skill 串联**。
- **业务 agent** 调能力 skill 干活。
- **flow-skill**（kdev-coding-flow / kdev-design-flow）= 方法论 + skill 调用模板**参考**，非编排器。

## 员工
| canonical id | 中文 | flow-skill 方法论 |
|---|---|---|
| dev-engineer | 开发工程师 | kdev-coding-flow |
| （req-architect 阶段2 P-A 加） | 需求架构师 | kdev-design-flow |

## ⚠️ 激活（G-004）
新插件 / 改 agent 后，CC 按 version 键控 cache：**改动须 bump `plugin.json` version** + **用户侧刷新 marketplace（`/plugin` 更新/重装）+ 重启 session** 才生效。agent 不代劳激活。
```

- [ ] **Step 6: 跑测试确认通过**

Run: `cd plugins/kdev-team && python3 -m pytest tests/test_plugin_manifest.py -v`
Expected: PASS（2 passed）

- [ ] **Step 7: Commit**

```bash
git add plugins/kdev-team/.claude-plugin/plugin.json plugins/kdev-team/README.md plugins/kdev-team/tests/test_plugin_manifest.py .claude-plugin/marketplace.json
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-team): 新建数字员工集群插件骨架 + marketplace 注册"
```

---

## Task 2: 搬 7 个 dev-engineer agent（persona → CC frontmatter agent）

**Files:**
- Create: `plugins/kdev-team/agents/dev-engineer-{orchestrator,env,plan,frontend,e2e,deploy,sec}.md`
- Delete: `plugins/kdev-coding-flow/skills/kdev-coding-flow/personas/*.md`
- Create: `plugins/kdev-team/tests/conftest.py`, `plugins/kdev-team/tests/test_agents.py`

- [ ] **Step 1: 写 conftest + 失败测试**

`plugins/kdev-team/tests/conftest.py`：
```python
import sys
from pathlib import Path
# 复用 kdev-core 引擎（test_orchestration_config 会用）
KDEV_CORE = Path(__file__).resolve().parents[2] / "kdev-core"
sys.path.insert(0, str(KDEV_CORE))
```

`plugins/kdev-team/tests/test_agents.py`：
```python
import re
from pathlib import Path

AGENTS = Path(__file__).resolve().parents[1] / "agents"
DEV_AGENTS = [
    "dev-engineer-orchestrator", "dev-engineer-env", "dev-engineer-plan",
    "dev-engineer-frontend", "dev-engineer-e2e", "dev-engineer-deploy", "dev-engineer-sec",
]
SECTIONS = ["## Identity", "## Principles", "## Critical Actions", "## Capabilities"]


def _frontmatter(text):
    m = re.match(r"^---\n(.*?)\n---\n", text, re.DOTALL)
    return m.group(1) if m else None


def test_all_7_dev_agents_exist():
    names = {p.stem for p in AGENTS.glob("dev-engineer-*.md")}
    for a in DEV_AGENTS:
        assert a in names, f"缺 agent: {a}"


def test_each_agent_has_frontmatter_and_sections():
    for a in DEV_AGENTS:
        text = (AGENTS / f"{a}.md").read_text(encoding="utf-8")
        fm = _frontmatter(text)
        assert fm, f"{a} 缺 YAML frontmatter"
        assert f"name: {a}" in fm, f"{a} frontmatter name 不匹配文件名"
        assert "description:" in fm and "model:" in fm, f"{a} frontmatter 缺 description/model"
        for sec in SECTIONS:
            assert sec in text, f"{a} 缺段落 {sec}"


def test_orchestrator_drives_via_cli_and_node_table():
    text = (AGENTS / "dev-engineer-orchestrator.md").read_text(encoding="utf-8")
    # 编排=按 node-table 调度 + CLI 驱动（守 §2.4）
    assert "node-table" in text
    assert "kdev_core" in text and "record-gate" in text
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd plugins/kdev-team && python3 -m pytest tests/test_agents.py -v`
Expected: FAIL（agents/ 为空）

- [ ] **Step 3: 搬 7 persona + 加 frontmatter**

对每个 persona：`git mv plugins/kdev-coding-flow/skills/kdev-coding-flow/personas/<源>.md plugins/kdev-team/agents/<agent-id>.md`，然后在文件**顶部插入 frontmatter**（body 原样保留）。frontmatter 模板：

```yaml
---
name: <agent-id>
description: <中文角色一句话 + when to use>
model: opus
---
```

逐个 description（中文角色 + 触发）：
- `dev-engineer-orchestrator`：`开发工程师·编排能力 — 读 dev-engineer.node-table.yml 驱动 kdev-core 引擎走 13 节点 coding-flow SOP，按编排在节点派业务 agent、gate 收结构化判定。Use when 主控派开发工程师端到端跑编码 flow。`
- `dev-engineer-env`：`开发工程师·环境准备 — clone/栈与工具链对齐、蒸馏 <repo>/docs/rules.md、产 env.md。Use when coding-flow 节点0 环境对齐。`
- `dev-engineer-plan`：`开发工程师·实施计划 — Gate-A/B 输入 + 写 PLAN.md/implementation-plan。Use when coding-flow 节点3 起实施计划。`
- `dev-engineer-frontend`：`开发工程师·前端实现 — 按 PLAN 改 src（含视觉改造），守 token/8px/字体白名单等。Use when coding-flow 实现节点。`
- `dev-engineer-e2e`：`开发工程师·E2E视觉验收 — build + 视觉 diff + 功能/金丝雀冒烟。Use when coding-flow 验收节点。`
- `dev-engineer-deploy`：`开发工程师·部署上线 — 合并分支 + 起被测环境。Use when coding-flow 合并/部署节点。`
- `dev-engineer-sec`：`开发工程师·安全扫描 — 轻量 security.md 自评（纯视觉改造从简）。Use when coding-flow 安全节点。`

> **dev-engineer-orchestrator 正文补强**（Task 5 会把 SKILL「接底座入口」的 CLI 驱动指令并入这里）：确保 Critical Actions 含 `python -m kdev_core` 的 init/advance/record-gate/resume/complete 调用 + 读 `orchestration/dev-engineer.node-table.yml` + 派 dev-engineer-* 业务 agent。

- [ ] **Step 4: 删空 personas 源目录**

```bash
rmdir plugins/kdev-coding-flow/skills/kdev-coding-flow/personas 2>/dev/null || true
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd plugins/kdev-team && python3 -m pytest tests/test_agents.py -v`
Expected: PASS（3 passed）

- [ ] **Step 6: Commit**

```bash
git add plugins/kdev-team/agents/ plugins/kdev-team/tests/conftest.py plugins/kdev-team/tests/test_agents.py
git add -A plugins/kdev-coding-flow/skills/kdev-coding-flow/personas
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-team): 搬 7 dev-engineer agent（persona→CC frontmatter agent，canonical id）"
```

---

## Task 3: 搬 node-table → kdev-team/orchestration

**Files:**
- Create: `plugins/kdev-team/orchestration/dev-engineer.node-table.yml`（git mv 自 kdev-coding-flow）
- Delete: `plugins/kdev-coding-flow/skills/kdev-coding-flow/orchestration/`
- Create: `plugins/kdev-team/tests/test_orchestration_config.py`

- [ ] **Step 1: 写失败测试**（复用阶段1 校验逻辑，改指新路径）

`plugins/kdev-team/tests/test_orchestration_config.py`：
```python
from pathlib import Path
import yaml
from kdev_core import node_machine

NT = Path(__file__).resolve().parents[1] / "orchestration/dev-engineer.node-table.yml"
GATE_KINDS = {"review", "decision", "acceptance"}


def _load():
    data = yaml.safe_load(NT.read_text(encoding="utf-8"))
    return data, node_machine.load_node_table(data)


def test_node_table_loads_and_has_16_nodes():
    data, table = _load()
    assert len(table["nodes"]) == 16
    assert table["terminal_fail"] == "n-fail"


def test_every_gate_node_has_a_gate_spec():
    data, table = _load()
    specs = data["gate_specs"]
    for nid, n in table["nodes"].items():
        if n["kind"] == "gate":
            assert n["gate"] in specs, f"gate {n['gate']} not in gate_specs"


def test_gate_specs_targets_valid_and_reviewer_bound():
    data, table = _load()
    nodes = set(table["nodes"])
    for gid, spec in data["gate_specs"].items():
        assert spec["kind"] in GATE_KINDS
        targets = (list(spec.get("branches", {}).values())
                   + [spec[k] for k in ("on_pass", "on_reflow") if k in spec])
        for tgt in targets:
            assert tgt in nodes, f"{gid} -> unknown node {tgt}"
        assert spec["reviewer"] in {"self", "reviewer-expert"}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd plugins/kdev-team && python3 -m pytest tests/test_orchestration_config.py -v`
Expected: FAIL（node-table 不在新路径）

- [ ] **Step 3: 搬 node-table**

```bash
mkdir -p plugins/kdev-team/orchestration
git mv plugins/kdev-coding-flow/skills/kdev-coding-flow/orchestration/node-table.yml plugins/kdev-team/orchestration/dev-engineer.node-table.yml
rmdir plugins/kdev-coding-flow/skills/kdev-coding-flow/orchestration 2>/dev/null || true
```
（node-table.yml 内容不改——节点/gate/reviewer 绑定原样；它本就是 dev-engineer 的编排结构。）

- [ ] **Step 4: 跑测试确认通过**

Run: `cd plugins/kdev-team && python3 -m pytest tests/test_orchestration_config.py -v`
Expected: PASS（3 passed）

- [ ] **Step 5: Commit**

```bash
git add plugins/kdev-team/orchestration/ plugins/kdev-team/tests/test_orchestration_config.py
git add -A plugins/kdev-coding-flow/skills/kdev-coding-flow/orchestration
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-team): 搬 dev-engineer.node-table.yml（编排结构）到 kdev-team/orchestration"
```

---

## Task 4: staff.yml 花名册

**Files:**
- Create: `plugins/kdev-team/staff.yml`
- Test: `plugins/kdev-team/tests/test_staff.py`

- [ ] **Step 1: 写失败测试**

`plugins/kdev-team/tests/test_staff.py`：
```python
from pathlib import Path
import yaml

STAFF = Path(__file__).resolve().parents[1] / "staff.yml"
AGENTS = Path(__file__).resolve().parents[1] / "agents"


def test_dev_engineer_entry():
    d = yaml.safe_load(STAFF.read_text(encoding="utf-8"))
    emp = d["employees"]["dev-engineer"]
    assert emp["display"] == "开发工程师"
    assert emp["flow_skill"] == "kdev-coding-flow"
    assert emp["node_table"] == "orchestration/dev-engineer.node-table.yml"
    # 花名册列的每个 agent 文件都在
    for a in emp["agents"]:
        assert (AGENTS / f"{a}.md").exists(), f"花名册引用的 agent 不存在: {a}"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd plugins/kdev-team && python3 -m pytest tests/test_staff.py -v`
Expected: FAIL（staff.yml 不存在）

- [ ] **Step 3: 写 staff.yml**

```yaml
# kdev-team 花名册：canonical id → 中文 display + flow-skill 方法论 + 编排 node-table + 运行时模型
# 守 spec §0.5 命名规范。运行时模型阶段1 暂统一 opus，可改二档。
employees:
  dev-engineer:
    display: 开发工程师
    flow_skill: kdev-coding-flow          # 方法论参考（非编排器）
    node_table: orchestration/dev-engineer.node-table.yml
    runtime_model: opus
    agents:
      - dev-engineer-orchestrator
      - dev-engineer-env
      - dev-engineer-plan
      - dev-engineer-frontend
      - dev-engineer-e2e
      - dev-engineer-deploy
      - dev-engineer-sec
  # req-architect 阶段2 P-A 追加
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd plugins/kdev-team && python3 -m pytest tests/test_staff.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add plugins/kdev-team/staff.yml plugins/kdev-team/tests/test_staff.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-team): staff.yml 花名册（dev-engineer → 开发工程师 + flow-skill + node-table + 模型）"
```

---

## Task 5: kdev-coding-flow 瘦身（SKILL 回归方法论）

**Files:**
- Modify: `plugins/kdev-coding-flow/skills/kdev-coding-flow/SKILL.md`
- Modify: `plugins/kdev-team/agents/dev-engineer-orchestrator.md`（确保编排/CLI 指令在此）
- Test: `plugins/kdev-team/tests/test_skill_slimmed.py`

- [ ] **Step 1: 写失败测试**

`plugins/kdev-team/tests/test_skill_slimmed.py`：
```python
from pathlib import Path

SKILL = (Path(__file__).resolve().parents[2]
         / "kdev-coding-flow/skills/kdev-coding-flow/SKILL.md")
ORCH = Path(__file__).resolve().parents[1] / "agents/dev-engineer-orchestrator.md"


def test_skill_points_to_kdev_team_not_embeds_orchestration():
    text = SKILL.read_text(encoding="utf-8")
    # 方法论保留（13 节点仍在），但编排/persona 落点指向 kdev-team、不再自带 personas/orchestration 驱动块
    assert "kdev-team" in text                         # 指针存在
    assert "skills/kdev-coding-flow/personas" not in text  # 旧 persona 路径清除
    # 编排 CLI 驱动命令不再堆在 SKILL（归 orchestrator agent）
    assert "python -m kdev_core advance" not in text


def test_orchestrator_agent_owns_drive_commands():
    text = ORCH.read_text(encoding="utf-8")
    assert "python -m kdev_core" in text
    assert "dev-engineer.node-table.yml" in text
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd plugins/kdev-team && python3 -m pytest tests/test_skill_slimmed.py -v`
Expected: FAIL（SKILL 仍含「接底座入口」CLI 块 / orchestrator 未提新 node-table 路径）

- [ ] **Step 3: 改 SKILL.md — 替换「接 kdev-core 底座入口」节为指针**

把 SKILL.md 末尾的「## 接 kdev-core 底座入口（v2 编排）」整节，替换为简短指针：

```markdown
## 接 kdev-core 底座（员工编排在 kdev-team）

本 SOP 的 13 节点编排由**开发工程师**员工执行，定义在 `plugins/kdev-team/`：
- 编排 agent `dev-engineer-orchestrator` 读 `kdev-team/orchestration/dev-engineer.node-table.yml`、用 kdev-core CLI 驱动引擎、按节点派 `dev-engineer-*` 业务 agent。
- 本 SKILL = **方法论 + skill 调用要求/模板参考**（agent 参考），**不充当编排器**（守 Q-008：编排=agent 按 node-table 调度）。
```
（其余 13 节点方法论、节点细则、bundle 策略、Auto Mode 适配、implementer 模板等**全部保留**。）

- [ ] **Step 4: 确保 orchestrator agent 含驱动指令**

检查 `plugins/kdev-team/agents/dev-engineer-orchestrator.md` 的 Critical Actions 含：读 `orchestration/dev-engineer.node-table.yml`、`python -m kdev_core init/advance/record-gate/resume/complete`、派 dev-engineer-* 业务 agent、gate reviewer 绑定(self vs reviewer-expert deferred)。缺则从被替换的 SKILL「接底座入口」节内容补入（路径改 dev-engineer.node-table.yml、persona 名改 dev-engineer-* agent id）。

- [ ] **Step 5: 跑测试确认通过 + kdev-coding-flow 自身回归**

Run: `cd plugins/kdev-team && python3 -m pytest tests/test_skill_slimmed.py -v`
Expected: PASS（2 passed）
Run: `cd plugins/kdev-coding-flow && python3 -m pytest -q`（若有其它 tests，确认仍绿）
Expected: 绿（或仅剩 SKILL lint 类）

- [ ] **Step 6: Commit**

```bash
git add plugins/kdev-coding-flow/skills/kdev-coding-flow/SKILL.md plugins/kdev-team/agents/dev-engineer-orchestrator.md plugins/kdev-team/tests/test_skill_slimmed.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "refactor(kdev-coding-flow): SKILL 回归纯方法论（编排指令归 dev-engineer-orchestrator agent），指针指向 kdev-team"
```

---

## Task 6: 清理旧测试 + 全量回归 + 版本

**Files:**
- Delete: `plugins/kdev-coding-flow/tests/{test_personas.py, test_orchestration_config.py}`
- Modify: `plugins/kdev-coding-flow/.claude-plugin/plugin.json`（bump version，触发 cache 刷新）

- [ ] **Step 1: 删 kdev-coding-flow 已搬走的测试**

```bash
git rm plugins/kdev-coding-flow/tests/test_personas.py plugins/kdev-coding-flow/tests/test_orchestration_config.py
```
（这两个测的对象已搬到 kdev-team；kdev-coding-flow 保留其它既有 tests。若删后 tests/ 空，保留 conftest 或留空目录无妨。）

- [ ] **Step 2: bump kdev-coding-flow version**（内容变了，避 G-004 stale）

`plugins/kdev-coding-flow/.claude-plugin/plugin.json` 的 `version` +0.1.0（如 0.x.y → 0.(x+1).0）。若该文件无 version 字段，按 kdev-memory 格式补 `"version": "0.2.0"`（取当前 + 1 minor）。

- [ ] **Step 3: 全量回归（两插件）**

Run: `cd plugins/kdev-team && python3 -m pytest -q`
Expected: 全绿（test_plugin_manifest 2 + test_agents 3 + test_orchestration_config 3 + test_staff 1 + test_skill_slimmed 2 = 11）
Run: `cd plugins/kdev-coding-flow && python3 -m pytest -q`
Expected: 绿（剩余 tests）
Run: `cd plugins/kdev-core && python3 -m pytest -q`
Expected: 76 passed（未受影响）

- [ ] **Step 4: Commit**

```bash
git add -A plugins/kdev-coding-flow/tests plugins/kdev-coding-flow/.claude-plugin/plugin.json
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "chore(kdev-coding-flow): 删已搬走的 persona/orchestration 测试 + bump version（cache 刷新）"
```

- [ ] **Step 5: 运行时验证备注（非自动化）**

P-0 的"`Agent({subagent_type: dev-engineer-frontend})` 可直派"需 **用户侧刷新 marketplace + 重启 session** 后才生效（G-004）。本 plan 完成后在报告中提示用户刷新；agent 不代劳激活。

---

## Self-Review（计划自查）

- **Spec 覆盖**：① kdev-team 骨架+注册=Task 1 ✓ ② 7 agent 搬迁+frontmatter=Task 2 ✓ ③ node-table 搬迁=Task 3 ✓ ④ staff.yml=Task 4 ✓ ⑤ kdev-coding-flow 瘦身+编排归 orchestrator=Task 5 ✓ ⑥ 测试迁移+清理+version=Task 2/3/6 ✓。命名守 §0.5（canonical id 全处）✓。agent×skill 守 §2.4（编排归 orchestrator agent、SKILL 不含驱动块、SKILL=方法论参考）✓。
- **Placeholder 扫描**：无 TBD；frontmatter description 逐个给全；persona/node-table body 原样 git mv（不复述，因保持 verbatim 是迁移要求，非占位）。
- **类型/路径一致**：agent id（dev-engineer-*）跨 Task 2/4/5 一致；node-table 路径 `kdev-team/orchestration/dev-engineer.node-table.yml` 跨 Task 3/4/5 一致；测试 parents[] 层级（kdev-team/tests → parents[1]=kdev-team, parents[2]=plugins, parents[3]=仓根）核对正确。
- **不含**：P-C1（kdev-memory scope）/ P-A（需求架构师）/ P-B（handoff）—— 另起 plan。
