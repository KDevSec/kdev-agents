# 编排路由器 / CEO 总编排 MVP — TDD 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 `/kdev-team <高层目标>` 在现有编排资产上做 LLM 路由 → 渲染一屏可读编排结论 → 人确认 → 主会话顺序链式调现有 `/kdev-flow-driver` 跑通 full-delivery 三段（需求→开发→测试，同 slug 自动接力 + 一道 after-req 停人闸），并由 HUD 渲染链进度/派单流。

**Architecture:** 三段主 loop（plan→confirm→drive）纯主会话 skill（守"子 agent 不能再开子 agent"）。新建声明式 `lifecycles/*.yml` 模板目录 + lint 校验器（against staff.yml / node-table gate_specs / handoff 契约 / 同 slug），确认即冻结结论落 `features/<slug>/delivery-plan.yml`。kdev-core 加 `dispatch` 事件（append-only：start 派单 + done 回填 usage），kdev-hud 读 delivery-plan + dispatch 事件渲染链进度/派单流 + 自包含 hud.html 钻入。

**Tech Stack:** Python 3.12 / pytest 9 / PyYAML（已是 kdev-core 依赖）。纯 stdlib + yaml，无新第三方依赖。引擎/CLI 照现有 argparse + jsonl + 文件契约风格。

## Global Constraints

- **零新引擎、零新原语**：直接复用 `/kdev-flow-driver`、`staff.yml`、per-员工 node-table、`handoff-write/read`、`reviewer-orchestrator`+dispatch-table、kdev-core CLI、HUD。新建仅 §6 四件（lifecycles / `/kdev-team` skill / delivery-plan schema+lint / dispatch 事件机制）。
- **硬约束（子 agent 不能再开子 agent）**：总编排只能主会话 skill 跑；drive 段顺序在主会话调 `/kdev-flow-driver`（它也跑主会话，内部才派 capability agent）；**绝不**把 flow-driver 当 `Agent()` 子 agent 派出去（那样它就无法再派 capability agent）。
- **OUT OF SCOPE（plan 明确不做）**：① per-gate flow-config merge 引擎能力（评审开关 MVP 降级为 `--review-mode` 三档 + 确认屏 per-gate 仅作意图展示，落地靠手改 node-table——诚实债 1）；② delivery-state 断点续跑 cursor（MVP 声明链级状态只活主会话内存、不可恢复——诚实债 2，不得宣称达 L3）；③ 其余 4 模板（design-only / design+build / test-only / build-only）——MVP 只兑现 full-delivery 一模板，绿了其余纯加 YAML。
- **AI commit 身份**：每次 commit 用 `git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit ...`（`-c` 后 key=value **不加引号**；kdev-commit `block-unattributed-commit` hook 硬校验，漏任一项 deny）。
- **Q-002**：跳过用户评分环节。
- **G-004（收尾提醒，非任务步骤）**：动了 plugin 的 hook/agent/version/command/skill → 收尾必须 bump 三插件 version（kdev-core 0.3.0 / kdev-hud 0.1.0 / kdev-team 0.8.1）+ 写 CHANGELOG + 提醒用户刷 marketplace（`/plugin` 更新/重装）+ 重启 session 才生效。
- **FF-5 实为净新**：roadmap 现仅到 FF-4（HUD 数字分落地）。本 MVP 的"派单可视化 + location.hash 钻入"是净新设计，实现 HUD 设计 §2.4「可钻入单员工/单节点详情」的设计愿景；plan 内不引用不存在的 FF-5 条目。
- **数据契约 = 文件格式**：HUD 只读 `features/<slug>/` 文件、零写入、运行时不 import kdev_core（自包含、坏数据降级）。delivery-plan.yml 由 kdev-team 写、HUD 以 guarded `import yaml` 读（缺 PyYAML / 缺文件 / 坏行 → 降级 None）。
- **⚠️ 测试基建约定（pre-flight 修订）**：kdev-core/tests 与 kdev-hud/tests 含 `__init__.py`（pytest prepend 模式 → 模块以 `tests.xxx` 包名导入），**禁止** `from test_xxx import ...` 跨 test 文件裸导入（会 ImportError）。共享夹具一律**在每个用到的 test 文件内联定义**：`GOOD`（delivery-plan 文本）在 test_router_lint / test_router_confirm / test_router_drive 各自定义；`_model_with_delivery()` 在 test_dispatch_dashboard / test_drilldown_html 各自定义。kdev-team/tests 无 `__init__.py` 不要新增（保持现状）。
- **gate_specs 结构（既有，校验器据此校验 + 确认屏据此渲染）**：键 = `{kind: review|decision|acceptance, on_pass/on_reflow (review/acceptance) | branches{} (decision), reviewer: reviewer-expert|self}`；顶层 `max_retries: 3`、`terminal_fail: n-fail`。node-table **无** `human_gates` 概念——`human_gates` 是 delivery-plan 独有的**链级**停人闸（after-req/after-dev/after-test），非节点级。

---

## File Structure

**WP1 — kdev-core（dispatch 事件机制）**
- Modify: `plugins/kdev-core/kdev_core/events.py` — 加 `dispatch_event()` builder（append-only，start/done 两 phase）。
- Modify: `plugins/kdev-core/kdev_core/cli.py` — 加 `dispatch-start` / `dispatch-done` 两子命令。
- Test: `plugins/kdev-core/tests/test_dispatch_events.py`（新建）。

**WP2 — kdev-team（新 `kdev_team` python 包 + lifecycles 模板 + lint）**
- Create: `plugins/kdev-team/kdev_team/__init__.py`、`roster.py`、`lifecycle.py`、`delivery_plan.py`、`lint.py`
- Create: `plugins/kdev-team/lifecycles/full-delivery.yml`（MVP 唯一模板）
- Modify: `plugins/kdev-team/orchestration/{req-architect,dev-engineer}.node-table.yml`、`test-engineer.design.node-table.yml`、`test-engineer.exec.node-table.yml` — 各加顶层 `delivery_node:` 字段（lint 据此解析 handoff 目标，避免与 flow-driver SKILL 文字表漂移）。
- Modify: `plugins/kdev-team/tests/conftest.py` — sys.path 加 kdev-team 根（`from kdev_team import ...`）。
- Test: `plugins/kdev-team/tests/test_router_lifecycle.py`、`test_router_delivery_plan.py`、`test_router_lint.py`、`test_node_delivery_node.py`（新建）。

**WP3 — kdev-team（`/kdev-team` skill：confirm/drive 纯函数 + authoring）**
- Create: `plugins/kdev-team/kdev_team/confirm.py`、`drive.py`
- Create: `plugins/kdev-team/skills/kdev-team/SKILL.md`、`plugins/kdev-team/commands/kdev-team.md`
- Test: `plugins/kdev-team/tests/test_router_confirm.py`、`test_router_drive.py`、`test_router_skill_manifest.py`（新建）。

**WP4 — kdev-hud（读 delivery-plan + dispatch → 渲染 + 钻入）**
- Modify: `plugins/kdev-hud/kdev_hud/datasource.py` — `read_delivery_plan()`、`_dispatch_views()`、扩 `build_feature_view`。
- Modify: `plugins/kdev-hud/kdev_hud/dashboard.py` — 链进度 + 派单流 + 员工忙闲 section + 自包含钻入 JS。
- Test: `plugins/kdev-hud/tests/test_dispatch_datasource.py`、`test_dispatch_dashboard.py`、`test_drilldown_html.py`（新建）。

**集成验收**
- Test: `plugins/kdev-team/tests/test_router_e2e_dataflow.py`（新建）——模拟 full-delivery 数据流：freeze delivery-plan → 写 dispatch 事件 → HUD model 渲染链进度 i/N。

---

## 数据契约（全 WP 共享，先读再实现）

### dispatch 事件（events.jsonl 内，append-only，与 transition/gate 同流）

派单时写 `phase=start`，完成时**追加**（非改写）`phase=done` 回填 usage：

```json
{"ts":"2026-06-17T...","type":"dispatch","phase":"start","actor":"ceo","slug":"user-auth","flow":"design-flow","emp":"req-architect","dispatch_id":"user-auth#1-req-architect","stage_index":1,"handoff_from":null}
{"ts":"2026-06-17T...","type":"dispatch","phase":"done","actor":"ceo","slug":"user-auth","flow":"design-flow","emp":"req-architect","dispatch_id":"user-auth#1-req-architect","status":"done","subagent_tokens":null,"tool_uses":null,"duration_s":null}
```

- `dispatch_id` = `<slug>#<stage_index>-<emp>`，start↔done 配对键。
- usage 三字段（`subagent_tokens`/`tool_uses`/`duration_s`）**可空**：MVP flow-driver 跑主会话（非单一 `Agent()`），整段 usage 无法作为一个 usage 对象取到 → done 默认写 null，CEO 能观测多少填多少（诚实债，schema 已就位、回填靠后续）。

### delivery-plan.yml（`features/<slug>/delivery-plan.yml`，确认冻结写一次、之后不变）

```yaml
template_id: full-delivery
slug: user-auth
goal: "做用户认证功能"
confidence: 0.86
reasoning: "全新功能+无现成SR+安全敏感+需可测交付 → 全交付三段"
stages:
  - {emp: req-architect, flow: design-flow,      on: true, handoff_from: null}
  - {emp: dev-engineer,  flow: coding-flow,      on: true, handoff_from: req-architect@n8-merge}
  - {emp: test-engineer, flow: test-design-flow, on: true, handoff_from: req-architect@n8-merge}
review_overrides:
  dev-engineer: {g-sec-review: reviewer-expert}
human_gates: [after-req]
runner_up: {template_id: design+build, why_not: "含'功能'隐含可交付→需测试段"}
```

### lifecycles/full-delivery.yml（模板 seed，新建）

```yaml
template_id: full-delivery
display: 全交付（需求→开发→测试）
when: "全新功能 + 需可交付落地 + 需测试验收；无现成 SR/设计产物"
stages:
  - {emp: req-architect, flow: design-flow,      handoff_from: null}
  - {emp: dev-engineer,  flow: coding-flow,      handoff_from: req-architect@n8-merge}
  - {emp: test-engineer, flow: test-design-flow, handoff_from: req-architect@n8-merge}
reviews_default: reviewer-expert      # 各 flow 评审默认走专家（node-table gate_specs 已是该值，确认屏据 gate_specs 真实渲染）
human_gates_default: [after-req]       # 默认一道停人闸：需求交付后人确认再进开发
```

---

# WP1 — kdev-core：dispatch 事件机制

### Task 1.1: `dispatch_event()` builder（events.py）

**Files:**
- Modify: `plugins/kdev-core/kdev_core/events.py`（在 `gate_event` 之后追加）
- Test: `plugins/kdev-core/tests/test_dispatch_events.py`（新建）

**Interfaces:**
- Produces: `dispatch_event(*, phase, slug, flow, emp, dispatch_id, stage_index=None, handoff_from=None, status=None, subagent_tokens=None, tool_uses=None, duration_s=None, actor="ceo") -> dict`。复用既有 `append_event(workspace, slug, event)` / `read_events(workspace, slug)`（不改）。

- [ ] **Step 1: 写失败测试**

```python
# plugins/kdev-core/tests/test_dispatch_events.py
from kdev_core import events


def test_dispatch_start_event_shape():
    e = events.dispatch_event(
        phase="start", slug="user-auth", flow="design-flow",
        emp="req-architect", dispatch_id="user-auth#1-req-architect",
        stage_index=1, handoff_from=None)
    assert e["type"] == "dispatch"
    assert e["phase"] == "start"
    assert e["actor"] == "ceo"
    assert e["slug"] == "user-auth"
    assert e["flow"] == "design-flow"
    assert e["emp"] == "req-architect"
    assert e["dispatch_id"] == "user-auth#1-req-architect"
    assert e["stage_index"] == 1
    assert e["handoff_from"] is None
    assert "ts" in e


def test_dispatch_done_event_carries_optional_usage():
    e = events.dispatch_event(
        phase="done", slug="user-auth", flow="coding-flow",
        emp="dev-engineer", dispatch_id="user-auth#2-dev-engineer",
        status="done", subagent_tokens=12345, tool_uses=20, duration_s=88)
    assert e["phase"] == "done"
    assert e["status"] == "done"
    assert e["subagent_tokens"] == 12345
    assert e["tool_uses"] == 20
    assert e["duration_s"] == 88


def test_dispatch_done_usage_defaults_to_none():
    e = events.dispatch_event(
        phase="done", slug="s", flow="design-flow", emp="req-architect",
        dispatch_id="s#1-req-architect", status="done")
    assert e["subagent_tokens"] is None
    assert e["tool_uses"] is None
    assert e["duration_s"] is None


def test_dispatch_events_roundtrip_via_append_read(tmp_path):
    ws = str(tmp_path)
    start = events.dispatch_event(
        phase="start", slug="s", flow="design-flow", emp="req-architect",
        dispatch_id="s#1-req-architect", stage_index=1)
    done = events.dispatch_event(
        phase="done", slug="s", flow="design-flow", emp="req-architect",
        dispatch_id="s#1-req-architect", status="done")
    events.append_event(ws, "s", start)
    events.append_event(ws, "s", done)
    got = events.read_events(ws, "s")
    assert [g["phase"] for g in got] == ["start", "done"]
    assert all(g["type"] == "dispatch" for g in got)
    assert got[0]["dispatch_id"] == got[1]["dispatch_id"]
```

- [ ] **Step 2: 跑测试验证失败**

Run: `python3 -m pytest plugins/kdev-core/tests/test_dispatch_events.py -v`
Expected: FAIL — `AttributeError: module 'kdev_core.events' has no attribute 'dispatch_event'`

- [ ] **Step 3: 写最小实现**

```python
# plugins/kdev-core/kdev_core/events.py —— 追加到文件末尾（gate_event 之后）
def dispatch_event(*, phase, slug, flow, emp, dispatch_id,
                   stage_index=None, handoff_from=None, status=None,
                   subagent_tokens=None, tool_uses=None, duration_s=None,
                   actor="ceo") -> dict:
    """Build a dispatch event line (CEO 派单/回填).

    phase="start"：派单时写（stage_index / handoff_from）。
    phase="done"：完成时**追加**回填（status + 可空 usage：subagent_tokens/tool_uses/duration_s）。
    events.jsonl append-only —— done 不改写 start，靠 dispatch_id 配对。
    """
    return {
        "ts": _now_iso(),
        "type": "dispatch",
        "phase": phase,
        "actor": actor,
        "slug": slug,
        "flow": flow,
        "emp": emp,
        "dispatch_id": dispatch_id,
        "stage_index": stage_index,
        "handoff_from": handoff_from,
        "status": status,
        "subagent_tokens": subagent_tokens,
        "tool_uses": tool_uses,
        "duration_s": duration_s,
    }
```

同时更新 events.py 顶部 docstring「两类事件」段，把 `dispatch（派单）事件留 P-B handoff。` 改为说明 dispatch 已落地（避免 R-009 文档漂移）：

```python
两类事件（P-Core-FF 范围）+ dispatch：
  - transition：R2 流转（来自 phase_history 条目），actor="system"
  - gate：R3 关卡判定（来自 GateResult），actor=GateResult.by
  - dispatch：CEO 总编排派单/回填（start 派单 + done 回填 usage），actor="ceo"
```

- [ ] **Step 4: 跑测试验证通过**

Run: `python3 -m pytest plugins/kdev-core/tests/test_dispatch_events.py -v`
Expected: PASS（4 passed）

- [ ] **Step 5: 跑 kdev-core 全量回归**

Run: `python3 -m pytest plugins/kdev-core/tests -q`
Expected: PASS（139 passed —— 原 135 + 新 4）

- [ ] **Step 6: Commit**

```bash
git add plugins/kdev-core/kdev_core/events.py plugins/kdev-core/tests/test_dispatch_events.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-core): events.jsonl 加 dispatch 事件 builder（start 派单 + done 回填 usage，append-only）"
```

---

### Task 1.2: CLI `dispatch-start` / `dispatch-done` 子命令（cli.py）

**Files:**
- Modify: `plugins/kdev-core/kdev_core/cli.py`（加两 cmd 函数 + 两 subparser）
- Test: `plugins/kdev-core/tests/test_dispatch_events.py`（追加 CLI 用例）

**Interfaces:**
- Consumes: Task 1.1 `events.dispatch_event` / `events.append_event`。
- Produces CLI:
  - `python3 -m kdev_core dispatch-start <flow> <slug> --emp <e> --dispatch-id <id> [--stage-index N] [--handoff-from <e@node>] [--workspace WS]`
  - `python3 -m kdev_core dispatch-done <flow> <slug> --emp <e> --dispatch-id <id> --status <done|blocked> [--subagent-tokens N] [--tool-uses N] [--duration-s N] [--workspace WS]`
  - 两命令各 print 刚写入事件的 JSON（便于 CEO 取回 / 调试）。

- [ ] **Step 1: 写失败测试（追加到 test_dispatch_events.py）**

```python
import json
from kdev_core import cli


def test_cli_dispatch_start_then_done_writes_two_events(tmp_path, capsys):
    ws = str(tmp_path)
    rc = cli.main(["dispatch-start", "design-flow", "auth",
                   "--emp", "req-architect", "--dispatch-id", "auth#1-req-architect",
                   "--stage-index", "1", "--workspace", ws])
    assert rc == 0
    out_start = json.loads(capsys.readouterr().out)
    assert out_start["phase"] == "start" and out_start["emp"] == "req-architect"

    rc = cli.main(["dispatch-done", "design-flow", "auth",
                   "--emp", "req-architect", "--dispatch-id", "auth#1-req-architect",
                   "--status", "done", "--subagent-tokens", "999", "--workspace", ws])
    assert rc == 0
    out_done = json.loads(capsys.readouterr().out)
    assert out_done["phase"] == "done" and out_done["status"] == "done"
    assert out_done["subagent_tokens"] == 999

    evs = events.read_events(ws, "auth")
    assert [e["phase"] for e in evs] == ["start", "done"]
```

- [ ] **Step 2: 跑测试验证失败**

Run: `python3 -m pytest plugins/kdev-core/tests/test_dispatch_events.py::test_cli_dispatch_start_then_done_writes_two_events -v`
Expected: FAIL —— argparse `invalid choice: 'dispatch-start'`

- [ ] **Step 3: 写最小实现**

```python
# plugins/kdev-core/kdev_core/cli.py

# —— 加在 cmd_events 之后 ——
def cmd_dispatch_start(args):
    from kdev_core import events
    e = events.dispatch_event(
        phase="start", slug=args.slug, flow=args.flow, emp=args.emp,
        dispatch_id=args.dispatch_id, stage_index=args.stage_index,
        handoff_from=args.handoff_from)
    events.append_event(args.workspace, args.slug, e)
    print(json.dumps(e, ensure_ascii=False, indent=2))
    return 0


def cmd_dispatch_done(args):
    from kdev_core import events
    e = events.dispatch_event(
        phase="done", slug=args.slug, flow=args.flow, emp=args.emp,
        dispatch_id=args.dispatch_id, status=args.status,
        subagent_tokens=args.subagent_tokens, tool_uses=args.tool_uses,
        duration_s=args.duration_s)
    events.append_event(args.workspace, args.slug, e)
    print(json.dumps(e, ensure_ascii=False, indent=2))
    return 0
```

```python
# —— 在 build_parser() 里，pev（events）subparser 之后加 ——
    pds = _common(sub, "dispatch-start")
    pds.add_argument("--emp", required=True)
    pds.add_argument("--dispatch-id", required=True, dest="dispatch_id")
    pds.add_argument("--stage-index", type=int, default=None, dest="stage_index")
    pds.add_argument("--handoff-from", default=None, dest="handoff_from")
    pds.set_defaults(func=cmd_dispatch_start)

    pdd = _common(sub, "dispatch-done")
    pdd.add_argument("--emp", required=True)
    pdd.add_argument("--dispatch-id", required=True, dest="dispatch_id")
    pdd.add_argument("--status", default="done", choices=["done", "blocked"])
    pdd.add_argument("--subagent-tokens", type=int, default=None, dest="subagent_tokens")
    pdd.add_argument("--tool-uses", type=int, default=None, dest="tool_uses")
    pdd.add_argument("--duration-s", type=int, default=None, dest="duration_s")
    pdd.set_defaults(func=cmd_dispatch_done)
```

- [ ] **Step 4: 跑测试验证通过**

Run: `python3 -m pytest plugins/kdev-core/tests/test_dispatch_events.py -v`
Expected: PASS（5 passed）

- [ ] **Step 5: 跑 kdev-core 全量回归 + smoke**

Run: `python3 -m pytest plugins/kdev-core/tests -q`
Expected: PASS（140 passed）

- [ ] **Step 6: Commit**

```bash
git add plugins/kdev-core/kdev_core/cli.py plugins/kdev-core/tests/test_dispatch_events.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-core): CLI 加 dispatch-start/dispatch-done 写入口（CEO 派单事件落 events.jsonl）"
```

---

# WP2 — kdev-team：kdev_team 包 + lifecycles 模板 + lint 校验器

### Task 2.1: 包脚手架 + conftest sys.path + node-table `delivery_node` 字段

**Files:**
- Create: `plugins/kdev-team/kdev_team/__init__.py`（空，标记包）
- Modify: `plugins/kdev-team/tests/conftest.py`（加 kdev-team 根到 sys.path）
- Modify: `plugins/kdev-team/orchestration/req-architect.node-table.yml`（顶层加 `delivery_node: n8-merge`）
- Modify: `plugins/kdev-team/orchestration/dev-engineer.node-table.yml`（顶层加 `delivery_node: n11-merge`）
- Modify: `plugins/kdev-team/orchestration/test-engineer.design.node-table.yml`（顶层加 `delivery_node: n3-merge`）
- Modify: `plugins/kdev-team/orchestration/test-engineer.exec.node-table.yml`（顶层加 `delivery_node: n2-report`）
- Test: `plugins/kdev-team/tests/test_node_delivery_node.py`（新建）

**Interfaces:**
- Produces: `from kdev_team import ...` 可导入；每个 node-table 顶层有 `delivery_node`（lint Task 2.4 据此解析 handoff 目标）。delivery_node 取值来自 flow-driver SKILL §2.4ter 交付节点表（req→n8-merge / test-design→n3-merge / test-exec→n2-report / dev→n11-merge）。

- [ ] **Step 1: 写失败测试**

```python
# plugins/kdev-team/tests/test_node_delivery_node.py
from pathlib import Path
import yaml

ORCH = Path(__file__).resolve().parents[1] / "orchestration"

EXPECTED = {
    "req-architect.node-table.yml": "n8-merge",
    "dev-engineer.node-table.yml": "n11-merge",
    "test-engineer.design.node-table.yml": "n3-merge",
    "test-engineer.exec.node-table.yml": "n2-report",
}


def test_kdev_team_package_importable():
    import kdev_team  # noqa: F401


def test_each_node_table_declares_delivery_node():
    for fname, expected in EXPECTED.items():
        data = yaml.safe_load((ORCH / fname).read_text(encoding="utf-8"))
        assert data.get("delivery_node") == expected, fname
        # delivery_node 必须是表里真实存在的节点 id
        ids = {n["id"] for n in data["nodes"]}
        assert expected in ids, f"{fname}: {expected} not a real node"
```

- [ ] **Step 2: 跑测试验证失败**

Run: `python3 -m pytest plugins/kdev-team/tests/test_node_delivery_node.py -v`
Expected: FAIL —— `import kdev_team` ModuleNotFoundError + `delivery_node` 为 None

- [ ] **Step 3: 写最小实现**

创建 `plugins/kdev-team/kdev_team/__init__.py`：

```python
"""kdev-team 编排路由器（CEO 总编排）可测逻辑层。

仅放纯函数 / 数据校验（lifecycle 模板加载、delivery-plan schema、lint 校验、
确认屏渲染、drive 序列）。编排副作用（调 flow-driver、写事件）留 SKILL.md 主会话。
"""
```

修改 `plugins/kdev-team/tests/conftest.py`，在末尾追加 kdev-team 根：

```python
import sys
from pathlib import Path
# 复用 kdev-core 引擎（test_orchestration_config 会用）
KDEV_CORE = Path(__file__).resolve().parents[2] / "kdev-core"
sys.path.insert(0, str(KDEV_CORE))
# 本插件 python 包根（from kdev_team import ...）
KDEV_TEAM = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(KDEV_TEAM))
```

各 node-table 顶层加 `delivery_node`（紧跟现有 `flow:` 行后）。例如 req-architect.node-table.yml：

```yaml
flow: design-flow
delivery_node: n8-merge      # 交付节点（flow-driver §2.4ter handoff-write 落点）；lint 解析 handoff 目标据此
max_retries: 3
```

dev-engineer → `delivery_node: n11-merge`；test-engineer.design → `delivery_node: n3-merge`；test-engineer.exec → `delivery_node: n2-report`。

- [ ] **Step 4: 跑测试验证通过 + node-table 回归（确认加字段不破现有）**

Run: `python3 -m pytest plugins/kdev-team/tests/test_node_delivery_node.py plugins/kdev-team/tests/test_orchestration_config.py -v`
Expected: PASS（含原 orchestration_config 用例不受 `delivery_node` 影响）

- [ ] **Step 5: Commit**

```bash
git add plugins/kdev-team/kdev_team/__init__.py plugins/kdev-team/tests/conftest.py plugins/kdev-team/orchestration/*.node-table.yml plugins/kdev-team/tests/test_node_delivery_node.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-team): 建 kdev_team 包脚手架 + node-table 加 delivery_node 字段（lint 解析 handoff 目标的 canonical 数据源）"
```

---

### Task 2.2: lifecycle 模板加载（lifecycle.py + full-delivery.yml）

**Files:**
- Create: `plugins/kdev-team/lifecycles/full-delivery.yml`（见上「数据契约」）
- Create: `plugins/kdev-team/kdev_team/lifecycle.py`
- Test: `plugins/kdev-team/tests/test_router_lifecycle.py`（新建）

**Interfaces:**
- Produces:
  - `lifecycle.lifecycles_dir() -> Path`（`plugins/kdev-team/lifecycles/`）
  - `lifecycle.list_templates() -> list[str]`（按文件名排序的 template_id）
  - `lifecycle.load_template(template_id) -> dict`；未知 id → 抛 `lifecycle.TemplateError`

- [ ] **Step 1: 写失败测试**

```python
# plugins/kdev-team/tests/test_router_lifecycle.py
import pytest
from kdev_team import lifecycle


def test_full_delivery_template_present():
    assert "full-delivery" in lifecycle.list_templates()


def test_load_full_delivery_has_three_stages():
    t = lifecycle.load_template("full-delivery")
    assert t["template_id"] == "full-delivery"
    emps = [s["emp"] for s in t["stages"]]
    assert emps == ["req-architect", "dev-engineer", "test-engineer"]
    flows = [s["flow"] for s in t["stages"]]
    assert flows == ["design-flow", "coding-flow", "test-design-flow"]
    assert t["stages"][1]["handoff_from"] == "req-architect@n8-merge"
    assert t["stages"][2]["handoff_from"] == "req-architect@n8-merge"
    assert t["human_gates_default"] == ["after-req"]


def test_unknown_template_raises():
    with pytest.raises(lifecycle.TemplateError):
        lifecycle.load_template("no-such-template")
```

- [ ] **Step 2: 跑测试验证失败**

Run: `python3 -m pytest plugins/kdev-team/tests/test_router_lifecycle.py -v`
Expected: FAIL —— ModuleNotFoundError: kdev_team.lifecycle

- [ ] **Step 3: 写最小实现**

先建 `plugins/kdev-team/lifecycles/full-delivery.yml`（内容见上「数据契约 § lifecycles/full-delivery.yml」，逐字写入）。

```python
# plugins/kdev-team/kdev_team/lifecycle.py
"""lifecycles/*.yml 命名生命周期模板加载（MVP 仅 full-delivery）。"""
from pathlib import Path

import yaml


class TemplateError(Exception):
    pass


def lifecycles_dir() -> Path:
    return Path(__file__).resolve().parents[1] / "lifecycles"


def list_templates() -> list:
    d = lifecycles_dir()
    if not d.exists():
        return []
    return sorted(p.stem for p in d.glob("*.yml"))


def load_template(template_id) -> dict:
    path = lifecycles_dir() / f"{template_id}.yml"
    if not path.exists():
        raise TemplateError(f"unknown template: {template_id!r}")
    return yaml.safe_load(path.read_text(encoding="utf-8"))
```

- [ ] **Step 4: 跑测试验证通过**

Run: `python3 -m pytest plugins/kdev-team/tests/test_router_lifecycle.py -v`
Expected: PASS（3 passed）

- [ ] **Step 5: Commit**

```bash
git add plugins/kdev-team/lifecycles/full-delivery.yml plugins/kdev-team/kdev_team/lifecycle.py plugins/kdev-team/tests/test_router_lifecycle.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-team): lifecycles/full-delivery.yml 模板 + lifecycle loader"
```

---

### Task 2.3: roster（staff.yml + node-table 解析助手）

**Files:**
- Create: `plugins/kdev-team/kdev_team/roster.py`
- Test: `plugins/kdev-team/tests/test_router_lint.py`（新建，本任务先放 roster 用例）

**Interfaces:**
- Produces（lint / confirm 共用）：
  - `roster.team_root() -> Path`（`plugins/kdev-team/`）
  - `roster.load_staff() -> dict`
  - `roster.employee(emp, staff=None) -> dict | None`
  - `roster.is_flow_owner(emp, staff=None) -> bool`（staff `kind == "flow-owner"`）
  - `roster.flows_for(emp, staff=None) -> list[str]`（`node_table` 单数 → 该表 flow；`node_tables` 复数 → keys）
  - `roster.node_table_data(emp, flow, staff=None) -> dict`；解析不到 → 抛 `roster.RosterError`
  - `roster.gate_specs(emp, flow, staff=None) -> dict`
  - `roster.delivery_node(emp, flow, staff=None) -> str | None`

- [ ] **Step 1: 写失败测试**

```python
# plugins/kdev-team/tests/test_router_lint.py
import pytest
from kdev_team import roster


def test_flow_owner_vs_callee():
    assert roster.is_flow_owner("req-architect") is True
    assert roster.is_flow_owner("dev-engineer") is True
    assert roster.is_flow_owner("reviewer") is False    # callee
    assert roster.is_flow_owner("no-such-emp") is False


def test_flows_for_single_and_multi_flow_owner():
    assert roster.flows_for("req-architect") == ["design-flow"]
    assert roster.flows_for("dev-engineer") == ["coding-flow"]
    assert set(roster.flows_for("test-engineer")) == {"test-design-flow", "test-exec-flow"}


def test_gate_specs_and_delivery_node_resolved_from_node_table():
    gs = roster.gate_specs("req-architect", "design-flow")
    assert gs["g-sr-review"]["reviewer"] == "reviewer-expert"
    assert roster.delivery_node("req-architect", "design-flow") == "n8-merge"
    assert roster.delivery_node("test-engineer", "test-design-flow") == "n3-merge"


def test_unknown_flow_for_employee_raises():
    with pytest.raises(roster.RosterError):
        roster.node_table_data("req-architect", "coding-flow")
```

- [ ] **Step 2: 跑测试验证失败**

Run: `python3 -m pytest plugins/kdev-team/tests/test_router_lint.py -v`
Expected: FAIL —— ModuleNotFoundError: kdev_team.roster

- [ ] **Step 3: 写最小实现**

```python
# plugins/kdev-team/kdev_team/roster.py
"""staff.yml + per-员工 node-table 的解析助手（lint / confirm 共用）。"""
from pathlib import Path

import yaml


class RosterError(Exception):
    pass


def team_root() -> Path:
    return Path(__file__).resolve().parents[1]


def load_staff() -> dict:
    return yaml.safe_load((team_root() / "staff.yml").read_text(encoding="utf-8"))


def _staff(staff):
    return staff if staff is not None else load_staff()


def employee(emp, staff=None):
    return _staff(staff).get("employees", {}).get(emp)


def is_flow_owner(emp, staff=None) -> bool:
    e = employee(emp, staff)
    return bool(e) and e.get("kind") == "flow-owner"


def _node_table_map(e) -> dict:
    """归一化：单数 node_table → {flow: path}；复数 node_tables 原样。"""
    if "node_tables" in e:
        return dict(e["node_tables"])
    if e.get("node_table"):
        # 单数：从表里读 flow 字段作 key
        path = e["node_table"]
        data = yaml.safe_load((team_root() / path).read_text(encoding="utf-8"))
        return {data["flow"]: path}
    return {}


def flows_for(emp, staff=None) -> list:
    e = employee(emp, staff)
    if not e:
        return []
    return sorted(_node_table_map(e).keys())


def node_table_data(emp, flow, staff=None) -> dict:
    e = employee(emp, staff)
    if not e:
        raise RosterError(f"unknown employee: {emp!r}")
    nt = _node_table_map(e)
    if flow not in nt:
        raise RosterError(f"{emp!r} has no flow {flow!r} (has {sorted(nt)})")
    return yaml.safe_load((team_root() / nt[flow]).read_text(encoding="utf-8"))


def gate_specs(emp, flow, staff=None) -> dict:
    return node_table_data(emp, flow, staff).get("gate_specs", {})


def delivery_node(emp, flow, staff=None):
    return node_table_data(emp, flow, staff).get("delivery_node")
```

- [ ] **Step 4: 跑测试验证通过**

Run: `python3 -m pytest plugins/kdev-team/tests/test_router_lint.py -v`
Expected: PASS（4 passed）

- [ ] **Step 5: Commit**

```bash
git add plugins/kdev-team/kdev_team/roster.py plugins/kdev-team/tests/test_router_lint.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-team): roster —— staff.yml + node-table 解析助手（flow-owner/flows/gate_specs/delivery_node）"
```

---

### Task 2.4: delivery-plan schema 解析 + 读写冻结（delivery_plan.py）

**Files:**
- Create: `plugins/kdev-team/kdev_team/delivery_plan.py`
- Test: `plugins/kdev-team/tests/test_router_delivery_plan.py`（新建）

**Interfaces:**
- Produces:
  - `delivery_plan.REQUIRED_KEYS`（frozenset）
  - `delivery_plan.parse(text) -> dict`（yaml.safe_load + 基本结构归一：缺 `review_overrides`→{}，缺 `human_gates`→[]）
  - `delivery_plan.structural_errors(plan) -> list[str]`（仅形状：缺必填键 / stages 非空 list / 每 stage 有 emp+flow+on / confidence 是 0..1 数值）。**语义校验**（emp 真存在、handoff 合法等）在 Task 2.5 lint。
  - `delivery_plan.path(workspace, slug) -> Path`（`<ws>/.kdev/features/<slug>/delivery-plan.yml`）
  - `delivery_plan.write(workspace, plan) -> Path`（mkdir -p + dump yaml）
  - `delivery_plan.read(workspace, slug) -> dict | None`（缺/坏 → None）

- [ ] **Step 1: 写失败测试**

```python
# plugins/kdev-team/tests/test_router_delivery_plan.py
from kdev_team import delivery_plan as dp

VALID = """
template_id: full-delivery
slug: user-auth
goal: "做用户认证"
confidence: 0.86
reasoning: "全新功能"
stages:
  - {emp: req-architect, flow: design-flow, on: true, handoff_from: null}
  - {emp: dev-engineer, flow: coding-flow, on: true, handoff_from: req-architect@n8-merge}
human_gates: [after-req]
"""


def test_parse_fills_defaults():
    plan = dp.parse(VALID)
    assert plan["review_overrides"] == {}        # 缺省补空
    assert plan["human_gates"] == ["after-req"]
    assert plan["slug"] == "user-auth"


def test_structural_errors_empty_for_valid():
    assert dp.structural_errors(dp.parse(VALID)) == []


def test_structural_errors_flag_missing_keys_and_bad_confidence():
    plan = dp.parse(VALID)
    del plan["goal"]
    plan["confidence"] = 1.5
    errs = dp.structural_errors(plan)
    assert any("goal" in e for e in errs)
    assert any("confidence" in e for e in errs)


def test_structural_errors_flag_stage_missing_fields():
    plan = dp.parse(VALID)
    plan["stages"] = [{"emp": "req-architect"}]   # 缺 flow/on
    errs = dp.structural_errors(plan)
    assert any("flow" in e for e in errs)


def test_write_read_roundtrip_creates_dir(tmp_path):
    plan = dp.parse(VALID)
    p = dp.write(str(tmp_path), plan)
    assert p.exists()
    assert p == dp.path(str(tmp_path), "user-auth")
    got = dp.read(str(tmp_path), "user-auth")
    assert got["slug"] == "user-auth"
    assert got["stages"][1]["handoff_from"] == "req-architect@n8-merge"


def test_read_missing_returns_none(tmp_path):
    assert dp.read(str(tmp_path), "nope") is None
```

- [ ] **Step 2: 跑测试验证失败**

Run: `python3 -m pytest plugins/kdev-team/tests/test_router_delivery_plan.py -v`
Expected: FAIL —— ModuleNotFoundError: kdev_team.delivery_plan

- [ ] **Step 3: 写最小实现**

```python
# plugins/kdev-team/kdev_team/delivery_plan.py
"""delivery-plan.yml schema：解析 + 形状校验 + 冻结读写。

语义校验（emp 真存在 / handoff 合法 / gate id 真存在）在 lint.py。
delivery-plan.yml 由 CEO 确认后冻结写一次、之后不改；进度从 dispatch 事件派生。
"""
import numbers
from pathlib import Path

import yaml

REQUIRED_KEYS = frozenset(
    {"template_id", "slug", "goal", "confidence", "stages"})


def parse(text) -> dict:
    plan = yaml.safe_load(text) or {}
    plan.setdefault("review_overrides", {})
    plan.setdefault("human_gates", [])
    return plan


def structural_errors(plan) -> list:
    errs = []
    for k in REQUIRED_KEYS:
        if k not in plan or plan[k] in (None, ""):
            errs.append(f"missing required key: {k}")
    conf = plan.get("confidence")
    if not isinstance(conf, numbers.Real) or isinstance(conf, bool) \
            or not (0.0 <= float(conf) <= 1.0):
        errs.append(f"confidence must be a number in [0,1], got {conf!r}")
    stages = plan.get("stages")
    if not isinstance(stages, list) or not stages:
        errs.append("stages must be a non-empty list")
    else:
        for i, s in enumerate(stages):
            for f in ("emp", "flow", "on"):
                if f not in s:
                    errs.append(f"stage[{i}] missing field: {f}")
    return errs


def path(workspace, slug) -> Path:
    return Path(workspace) / ".kdev" / "features" / slug / "delivery-plan.yml"


def write(workspace, plan) -> Path:
    p = path(workspace, plan["slug"])
    p.parent.mkdir(parents=True, exist_ok=True)
    p.write_text(yaml.safe_dump(plan, allow_unicode=True, sort_keys=False),
                 encoding="utf-8")
    return p


def read(workspace, slug):
    p = path(workspace, slug)
    if not p.exists():
        return None
    try:
        return yaml.safe_load(p.read_text(encoding="utf-8"))
    except (yaml.YAMLError, OSError):
        return None
```

- [ ] **Step 4: 跑测试验证通过**

Run: `python3 -m pytest plugins/kdev-team/tests/test_router_delivery_plan.py -v`
Expected: PASS（6 passed）

- [ ] **Step 5: Commit**

```bash
git add plugins/kdev-team/kdev_team/delivery_plan.py plugins/kdev-team/tests/test_router_delivery_plan.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-team): delivery-plan schema 解析 + 形状校验 + 冻结读写"
```

---

### Task 2.5: lint 语义校验器（lint.py）—— TDD 核心

**Files:**
- Create: `plugins/kdev-team/kdev_team/lint.py`
- Test: `plugins/kdev-team/tests/test_router_lint.py`（追加 lint 用例）

**Interfaces:**
- Consumes: Task 2.3 `roster.*`、Task 2.4 `delivery_plan.structural_errors`、Task 2.2 `lifecycle.list_templates`。
- Produces:
  - `lint.validate(plan, staff=None) -> list[str]`（空 = 合法、可进确认屏；非空 = 拒进，逐条人读）。先跑 `delivery_plan.structural_errors`，结构有错直接返回（不再跑语义，避免 KeyError）。再跑语义：
    1. `template_id` ∈ `lifecycle.list_templates()`
    2. 每个 `on:true` stage：`emp` 是 flow-owner；`flow` ∈ `roster.flows_for(emp)`
    3. `handoff_from`（非 null）格式 `<emp>@<node>`；其 emp 是**更早**的 on:true stage；node == 该上游 emp 在其 flow 的 `roster.delivery_node`
    4. `review_overrides`：key emp ∈ on:true stages；其下 gate id ∈ 该 emp flow 的 `roster.gate_specs`；值 ∈ {reviewer-expert, self}
    5. `human_gates`：取值 ∈ {after-req, after-dev, after-test}
    6. `confidence < 0.6` 时必须有 `runner_up`（含 template_id）

- [ ] **Step 1: 写失败测试（追加到 test_router_lint.py）**

```python
from kdev_team import lint, delivery_plan as dp

GOOD = """
template_id: full-delivery
slug: user-auth
goal: "做用户认证"
confidence: 0.86
reasoning: r
stages:
  - {emp: req-architect, flow: design-flow, on: true, handoff_from: null}
  - {emp: dev-engineer, flow: coding-flow, on: true, handoff_from: req-architect@n8-merge}
  - {emp: test-engineer, flow: test-design-flow, on: true, handoff_from: req-architect@n8-merge}
review_overrides:
  dev-engineer: {g-sec-review: reviewer-expert}
human_gates: [after-req]
"""


def _plan(**over):
    plan = dp.parse(GOOD)
    plan.update(over)
    return plan


def test_valid_plan_passes_lint():
    assert lint.validate(dp.parse(GOOD)) == []


def test_unknown_template_rejected():
    errs = lint.validate(_plan(template_id="bogus"))
    assert any("template" in e for e in errs)


def test_callee_employee_rejected():
    plan = _plan()
    plan["stages"][0]["emp"] = "reviewer"     # callee, 非 flow-owner
    errs = lint.validate(plan)
    assert any("reviewer" in e and "flow-owner" in e for e in errs)


def test_bad_flow_for_employee_rejected():
    plan = _plan()
    plan["stages"][1]["flow"] = "design-flow"   # dev-engineer 没有 design-flow
    assert any("flow" in e for e in lint.validate(plan))


def test_handoff_from_bad_node_rejected():
    plan = _plan()
    plan["stages"][1]["handoff_from"] = "req-architect@n99-bogus"
    errs = lint.validate(plan)
    assert any("handoff_from" in e and "n99-bogus" in e for e in errs)


def test_handoff_from_forward_reference_rejected():
    plan = _plan()
    # dev 引用 test（更晚的 stage）作上游 → 非法
    plan["stages"][1]["handoff_from"] = "test-engineer@n3-merge"
    errs = lint.validate(plan)
    assert any("handoff_from" in e and "earlier" in e for e in errs)


def test_review_override_unknown_gate_rejected():
    plan = _plan()
    plan["review_overrides"] = {"dev-engineer": {"g-nope": "reviewer-expert"}}
    assert any("g-nope" in e for e in lint.validate(plan))


def test_review_override_bad_value_rejected():
    plan = _plan()
    plan["review_overrides"] = {"dev-engineer": {"g-sec-review": "intern"}}
    assert any("intern" in e for e in lint.validate(plan))


def test_unknown_human_gate_rejected():
    assert any("human_gate" in e.lower() for e in lint.validate(_plan(human_gates=["after-lunch"])))


def test_low_confidence_requires_runner_up():
    plan = _plan(confidence=0.4)
    plan.pop("runner_up", None)
    assert any("runner_up" in e for e in lint.validate(plan))
    plan["runner_up"] = {"template_id": "design+build", "why_not": "x"}
    assert lint.validate(plan) == []
```

- [ ] **Step 2: 跑测试验证失败**

Run: `python3 -m pytest plugins/kdev-team/tests/test_router_lint.py -v`
Expected: FAIL —— ModuleNotFoundError: kdev_team.lint（roster 用例仍 PASS）

- [ ] **Step 3: 写最小实现**

```python
# plugins/kdev-team/kdev_team/lint.py
"""delivery-plan.yml 语义校验器：drive 前硬校验，不过禁进确认屏。

逐字段对 staff.yml / node-table gate_specs / handoff 契约硬校验。
返回空 list = 合法；非空 = 拒进、逐条人读。
"""
from kdev_team import lifecycle, roster, delivery_plan

_VALID_REVIEWERS = {"reviewer-expert", "self"}
_VALID_HUMAN_GATES = {"after-req", "after-dev", "after-test"}


def validate(plan, staff=None) -> list:
    errs = delivery_plan.structural_errors(plan)
    if errs:
        return errs   # 结构不全，不跑语义（防 KeyError）
    if staff is None:
        staff = roster.load_staff()

    # 1. template
    if plan["template_id"] not in lifecycle.list_templates():
        errs.append(f"unknown template_id: {plan['template_id']!r}")

    on_stages = [s for s in plan["stages"] if s.get("on")]
    seen_emps = []   # 按顺序的 on:true emp，用于 handoff 前向引用校验

    for i, s in enumerate(on_stages):
        emp, flow = s.get("emp"), s.get("flow")
        # 2. emp flow-owner + flow 合法
        if not roster.is_flow_owner(emp, staff):
            errs.append(f"stage emp {emp!r} is not a flow-owner")
        elif flow not in roster.flows_for(emp, staff):
            errs.append(f"stage emp {emp!r} has no flow {flow!r}")
        # 3. handoff_from
        hf = s.get("handoff_from")
        if hf:
            if "@" not in hf:
                errs.append(f"handoff_from must be '<emp>@<node>', got {hf!r}")
            else:
                up_emp, up_node = hf.split("@", 1)
                if up_emp not in seen_emps:
                    errs.append(
                        f"handoff_from {hf!r}: {up_emp!r} must be an earlier on stage")
                else:
                    up_flow = next(x["flow"] for x in on_stages if x["emp"] == up_emp)
                    try:
                        dn = roster.delivery_node(up_emp, up_flow, staff)
                    except roster.RosterError:
                        dn = None
                    if up_node != dn:
                        errs.append(
                            f"handoff_from {hf!r}: node must be {up_emp!r} delivery_node "
                            f"{dn!r}, got {up_node!r}")
        seen_emps.append(emp)

    # 4. review_overrides
    on_emp_set = {s["emp"] for s in on_stages}
    for emp, gates in (plan.get("review_overrides") or {}).items():
        if emp not in on_emp_set:
            errs.append(f"review_override emp {emp!r} not in on stages")
            continue
        flow = next(s["flow"] for s in on_stages if s["emp"] == emp)
        try:
            specs = roster.gate_specs(emp, flow, staff)
        except roster.RosterError:
            specs = {}
        for gid, val in (gates or {}).items():
            if gid not in specs:
                errs.append(f"review_override {emp}.{gid}: unknown gate")
            if val not in _VALID_REVIEWERS:
                errs.append(
                    f"review_override {emp}.{gid}={val!r}: must be one of {_VALID_REVIEWERS}")

    # 5. human_gates
    for g in (plan.get("human_gates") or []):
        if g not in _VALID_HUMAN_GATES:
            errs.append(f"unknown human_gate: {g!r}")

    # 6. low confidence → runner_up required
    if float(plan["confidence"]) < 0.6:
        ru = plan.get("runner_up")
        if not (isinstance(ru, dict) and ru.get("template_id")):
            errs.append("confidence<0.6 requires runner_up with template_id")

    return errs
```

- [ ] **Step 4: 跑测试验证通过**

Run: `python3 -m pytest plugins/kdev-team/tests/test_router_lint.py -v`
Expected: PASS（roster 4 + lint 10 = 14 passed）

- [ ] **Step 5: 跑 kdev-team 全量回归**

Run: `python3 -m pytest plugins/kdev-team/tests -q`
Expected: PASS（原 84 + 新增全部）

- [ ] **Step 6: Commit**

```bash
git add plugins/kdev-team/kdev_team/lint.py plugins/kdev-team/tests/test_router_lint.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-team): delivery-plan lint 语义校验器（template/emp/flow/handoff/review/human-gate/confidence）"
```

> **本任务建议走 superpowers:requesting-code-review（opus）**——lint 是 WP2 TDD 核心、校验逻辑分支多，评审揪边界遗漏。

---

# WP3 — kdev-team：/kdev-team skill（confirm/drive 纯函数 + authoring）

### Task 3.1: 确认屏渲染（confirm.render_screen）

**Files:**
- Create: `plugins/kdev-team/kdev_team/confirm.py`
- Test: `plugins/kdev-team/tests/test_router_confirm.py`（新建）

**Interfaces:**
- Consumes: Task 2.3 `roster.gate_specs`。
- Produces:
  - `confirm.render_screen(plan, staff=None) -> str`：渲染「编排结论·待确认」一屏。每 on:true stage 列 emp display + flow + 评审项（从该 emp flow 的 gate_specs 真实生成，review_overrides 覆盖后显示最终 reviewer，被覆盖的标 `⚠(已调整)`）+ handoff 来源。含：slug / 归类(template_id) / confidence / reasoning / human_gates / runner_up（有则）/ `⚠️ 串行非并发` 提示 / `confidence<0.6 或丢段时禁一键 Enter` 二次确认提示。
  - `confirm.review_items(emp, flow, overrides, staff=None) -> list[dict]`：辅助——`[{gate, reviewer, overridden:bool}]`，仅 `kind==review` 的 gate（decision/acceptance 非人选评审项不列）。

- [ ] **Step 1: 写失败测试**

```python
# plugins/kdev-team/tests/test_router_confirm.py
from kdev_team import confirm, delivery_plan as dp

# 内联合法 plan 文本（禁跨 test 文件导入，见 plan 测试基建约定）
GOOD = """
template_id: full-delivery
slug: user-auth
goal: "做用户认证"
confidence: 0.86
reasoning: r
stages:
  - {emp: req-architect, flow: design-flow, on: true, handoff_from: null}
  - {emp: dev-engineer, flow: coding-flow, on: true, handoff_from: req-architect@n8-merge}
  - {emp: test-engineer, flow: test-design-flow, on: true, handoff_from: req-architect@n8-merge}
review_overrides:
  dev-engineer: {g-sec-review: reviewer-expert}
human_gates: [after-req]
runner_up: {template_id: design+build, why_not: "含'功能'隐含可交付"}
"""


def test_review_items_from_gate_specs_only_review_kind():
    items = confirm.review_items("dev-engineer", "coding-flow", {})
    gates = {it["gate"] for it in items}
    assert {"g-plan-review", "g-code-review", "g-sec-review"} <= gates
    assert "g-relevance" not in gates       # decision kind 不列
    assert "g-e2e" not in gates             # acceptance kind 不列


def test_review_items_reflect_override():
    items = confirm.review_items(
        "dev-engineer", "coding-flow", {"g-sec-review": "self"})
    sec = next(it for it in items if it["gate"] == "g-sec-review")
    assert sec["reviewer"] == "self" and sec["overridden"] is True


def test_render_screen_contains_key_sections():
    s = confirm.render_screen(dp.parse(GOOD))
    assert "user-auth" in s                 # slug
    assert "full-delivery" in s             # 归类
    assert "0.86" in s                      # confidence
    assert "需求架构师" in s and "开发工程师" in s and "测试工程师" in s
    assert "串行" in s                       # 非并发提示
    assert "after-req" in s                  # human gate
    assert "design+build" in s               # runner_up
    assert "req-architect@n8-merge" in s     # handoff 来源


def test_render_screen_low_confidence_forces_second_confirm():
    plan = dp.parse(GOOD)
    plan["confidence"] = 0.4
    plan["runner_up"] = {"template_id": "design+build", "why_not": "x"}
    s = confirm.render_screen(plan)
    assert "禁" in s or "二次确认" in s      # 禁一键 Enter 提示
```

- [ ] **Step 2: 跑测试验证失败**

Run: `python3 -m pytest plugins/kdev-team/tests/test_router_confirm.py -v`
Expected: FAIL —— ModuleNotFoundError: kdev_team.confirm

- [ ] **Step 3: 写最小实现**

```python
# plugins/kdev-team/kdev_team/confirm.py
"""确认屏渲染 + 结构化编辑（纯函数；副作用/交互留 SKILL.md 主会话）。"""
from kdev_team import roster

_DISPLAY = {
    "req-architect": "需求架构师", "dev-engineer": "开发工程师",
    "test-engineer": "测试工程师",
}


def review_items(emp, flow, overrides, staff=None) -> list:
    specs = roster.gate_specs(emp, flow, staff)
    ov = overrides or {}
    out = []
    for gid, spec in specs.items():
        if spec.get("kind") != "review":
            continue
        reviewer = ov.get(gid, spec.get("reviewer"))
        out.append({"gate": gid, "reviewer": reviewer,
                    "overridden": gid in ov})
    return out


def render_screen(plan, staff=None) -> str:
    L = []
    slug = plan.get("slug")
    L.append(f"━━━ kdev 编排结论 · 待你确认  slug: {slug} ━━━")
    L.append(f"目标：{plan.get('goal')}")
    L.append(f"归类：{plan.get('template_id')}   置信度 {plan.get('confidence')}")
    L.append(f"理由：{plan.get('reasoning')}")
    L.append("⚠️ 按依赖串行驱动，非真并发（各段不同时跑）")
    L.append("")
    L.append("拟派流水线（同 slug 串联）：")
    on_stages = [s for s in plan.get("stages", []) if s.get("on")]
    overrides = plan.get("review_overrides") or {}
    for i, s in enumerate(on_stages, 1):
        emp, flow = s["emp"], s["flow"]
        disp = _DISPLAY.get(emp, emp)
        src = f"  ◄── 读 {s['handoff_from']}" if s.get("handoff_from") else ""
        L.append(f"  [{i}] ✓ {disp}  {emp} · {flow}{src}")
        items = review_items(emp, flow, overrides.get(emp), staff)
        if items:
            parts = []
            for it in items:
                tag = " ⚠(已调整)" if it["overridden"] else ""
                parts.append(f"{it['gate']}={it['reviewer']}{tag}")
            L.append("        评审： " + "   ".join(parts))
    L.append("")
    hg = plan.get("human_gates") or []
    L.append(f"人介入闸（停人）：{' · '.join(hg) if hg else '无'}  + 评审3次不过升你裁决")
    ru = plan.get("runner_up")
    if ru:
        L.append(f"次选：{ru.get('template_id')} —— {ru.get('why_not', '')}")
    L.append("")
    low = float(plan.get("confidence", 1)) < 0.6
    if low:
        L.append("🔴 置信度<0.6：禁一键 Enter，请二次确认或换模板/微调后再派。")
    L.append("你可以：[Enter] 照此派发  [d N] 关段  [r <emp> <gate>=<self|reviewer-expert>] 调评审"
             "  [g +after-test] 加停人闸  [t <template>] 换模板  [s <slug>] 改 slug")
    return "\n".join(L)
```

- [ ] **Step 4: 跑测试验证通过**

Run: `python3 -m pytest plugins/kdev-team/tests/test_router_confirm.py -v`
Expected: PASS（4 passed）

- [ ] **Step 5: Commit**

```bash
git add plugins/kdev-team/kdev_team/confirm.py plugins/kdev-team/tests/test_router_confirm.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-team): 确认屏渲染 render_screen + review_items（评审项从 gate_specs 真实生成）"
```

---

### Task 3.2: 确认屏结构化编辑（confirm.apply_edit）

**Files:**
- Modify: `plugins/kdev-team/kdev_team/confirm.py`（加 `apply_edit`）
- Test: `plugins/kdev-team/tests/test_router_confirm.py`（追加）

**Interfaces:**
- Produces: `confirm.apply_edit(plan, command) -> dict`：返回**新 plan dict**（不原地改）。支持命令：
  - `d N`：关掉第 N 条 on stage（置 `on=False`）
  - `r <emp> <gate>=<reviewer>`：设 `review_overrides[emp][gate]=reviewer`
  - `g +<gate>` / `g -<gate>`：增/删 human_gate
  - `t <template_id>`：换 `template_id`（不重渲 stages —— MVP 只换标签，stages 由 LLM 重出；测试只验字段变更）
  - `s <slug>`：改 slug
  - 未知命令 → 抛 `confirm.EditError`

- [ ] **Step 1: 写失败测试（追加）**

```python
import pytest
from kdev_team import confirm


def test_drop_stage_sets_on_false():
    plan = dp.parse(GOOD)
    out = confirm.apply_edit(plan, "d 3")
    assert out["stages"][2]["on"] is False
    assert plan["stages"][2]["on"] is True      # 原 plan 不被改


def test_review_override_edit():
    out = confirm.apply_edit(dp.parse(GOOD), "r dev-engineer g-sec-review=self")
    assert out["review_overrides"]["dev-engineer"]["g-sec-review"] == "self"


def test_add_and_remove_human_gate():
    out = confirm.apply_edit(dp.parse(GOOD), "g +after-test")
    assert "after-test" in out["human_gates"]
    out2 = confirm.apply_edit(out, "g -after-req")
    assert "after-req" not in out2["human_gates"]


def test_swap_template_and_slug():
    out = confirm.apply_edit(dp.parse(GOOD), "t design+build")
    assert out["template_id"] == "design+build"
    out2 = confirm.apply_edit(out, "s my-feature")
    assert out2["slug"] == "my-feature"


def test_unknown_command_raises():
    with pytest.raises(confirm.EditError):
        confirm.apply_edit(dp.parse(GOOD), "frobnicate 7")
```

- [ ] **Step 2: 跑测试验证失败**

Run: `python3 -m pytest plugins/kdev-team/tests/test_router_confirm.py -v`
Expected: FAIL —— `AttributeError: ... has no attribute 'apply_edit'` / `EditError`

- [ ] **Step 3: 写最小实现（加到 confirm.py）**

```python
import copy


class EditError(Exception):
    pass


def apply_edit(plan, command) -> dict:
    plan = copy.deepcopy(plan)
    parts = command.strip().split()
    if not parts:
        raise EditError("empty command")
    op = parts[0]
    if op == "d" and len(parts) == 2 and parts[1].isdigit():
        on = [s for s in plan["stages"] if s.get("on")]
        idx = int(parts[1]) - 1
        if not (0 <= idx < len(on)):
            raise EditError(f"no stage #{parts[1]}")
        on[idx]["on"] = False
        return plan
    if op == "r" and len(parts) == 3 and "=" in parts[2]:
        emp = parts[1]
        gate, reviewer = parts[2].split("=", 1)
        plan.setdefault("review_overrides", {}).setdefault(emp, {})[gate] = reviewer
        return plan
    if op == "g" and len(parts) == 2 and parts[1][:1] in "+-":
        sign, gate = parts[1][0], parts[1][1:]
        hg = plan.setdefault("human_gates", [])
        if sign == "+" and gate not in hg:
            hg.append(gate)
        elif sign == "-" and gate in hg:
            hg.remove(gate)
        return plan
    if op == "t" and len(parts) == 2:
        plan["template_id"] = parts[1]
        return plan
    if op == "s" and len(parts) == 2:
        plan["slug"] = parts[1]
        return plan
    raise EditError(f"unknown edit command: {command!r}")
```

- [ ] **Step 4: 跑测试验证通过**

Run: `python3 -m pytest plugins/kdev-team/tests/test_router_confirm.py -v`
Expected: PASS（9 passed）

- [ ] **Step 5: Commit**

```bash
git add plugins/kdev-team/kdev_team/confirm.py plugins/kdev-team/tests/test_router_confirm.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-team): 确认屏结构化编辑 apply_edit（d/r/g/t/s 命令 → 重出 plan dict）"
```

---

### Task 3.3: drive 序列构建（drive.build_sequence）

**Files:**
- Create: `plugins/kdev-team/kdev_team/drive.py`
- Test: `plugins/kdev-team/tests/test_router_drive.py`（新建）

**Interfaces:**
- Produces: `drive.build_sequence(plan) -> list[dict]`：把冻结 plan 的 on:true stages 转成有序执行步骤。每步：
  ```python
  {
    "stage_index": 1,
    "emp": "req-architect",
    "flow": "design-flow",
    "dispatch_id": "user-auth#1-req-architect",
    "handoff_from": None,                       # 或 "req-architect@n8-merge"
    "driver_cmd": "/kdev-flow-driver req-architect --task <slug> --slug user-auth",
    "dispatch_start_cmd": ["dispatch-start", "design-flow", "user-auth", "--emp", "req-architect", "--dispatch-id", "user-auth#1-req-architect", "--stage-index", "1"],
    "dispatch_done_cmd": ["dispatch-done", "design-flow", "user-auth", "--emp", "req-architect", "--dispatch-id", "user-auth#1-req-architect", "--status", "done"],
    "human_gate_after": "after-req" or None,    # 该 stage 之后是否停人
  }
  ```
  `human_gate_after` 映射：req-architect→after-req、dev-engineer→after-dev、test-engineer→after-test，仅当该闸在 `plan["human_gates"]` 才置值。

- [ ] **Step 1: 写失败测试**

```python
# plugins/kdev-team/tests/test_router_drive.py
from kdev_team import drive, delivery_plan as dp

# 内联合法 plan 文本（禁跨 test 文件导入，见 plan 测试基建约定）
GOOD = """
template_id: full-delivery
slug: user-auth
goal: "做用户认证"
confidence: 0.86
reasoning: r
stages:
  - {emp: req-architect, flow: design-flow, on: true, handoff_from: null}
  - {emp: dev-engineer, flow: coding-flow, on: true, handoff_from: req-architect@n8-merge}
  - {emp: test-engineer, flow: test-design-flow, on: true, handoff_from: req-architect@n8-merge}
human_gates: [after-req]
"""


def test_sequence_length_and_order():
    seq = drive.build_sequence(dp.parse(GOOD))
    assert [s["emp"] for s in seq] == ["req-architect", "dev-engineer", "test-engineer"]
    assert [s["stage_index"] for s in seq] == [1, 2, 3]


def test_dispatch_ids_unique_and_formatted():
    seq = drive.build_sequence(dp.parse(GOOD))
    assert seq[0]["dispatch_id"] == "user-auth#1-req-architect"
    assert len({s["dispatch_id"] for s in seq}) == 3


def test_driver_cmd_carries_slug():
    seq = drive.build_sequence(dp.parse(GOOD))
    assert "--slug user-auth" in seq[1]["driver_cmd"]
    assert "dev-engineer" in seq[1]["driver_cmd"]


def test_human_gate_after_only_when_in_plan():
    seq = drive.build_sequence(dp.parse(GOOD))   # human_gates: [after-req]
    assert seq[0]["human_gate_after"] == "after-req"
    assert seq[1]["human_gate_after"] is None


def test_dropped_stage_excluded():
    plan = dp.parse(GOOD)
    plan["stages"][2]["on"] = False
    seq = drive.build_sequence(plan)
    assert [s["emp"] for s in seq] == ["req-architect", "dev-engineer"]


def test_dispatch_cmds_are_cli_argv_lists():
    seq = drive.build_sequence(dp.parse(GOOD))
    s0 = seq[0]
    assert s0["dispatch_start_cmd"][0] == "dispatch-start"
    assert "--dispatch-id" in s0["dispatch_start_cmd"]
    assert s0["dispatch_done_cmd"][0] == "dispatch-done"
```

- [ ] **Step 2: 跑测试验证失败**

Run: `python3 -m pytest plugins/kdev-team/tests/test_router_drive.py -v`
Expected: FAIL —— ModuleNotFoundError: kdev_team.drive

- [ ] **Step 3: 写最小实现**

```python
# plugins/kdev-team/kdev_team/drive.py
"""把冻结 delivery-plan 转成有序 drive 步骤（纯函数；真调 flow-driver/写事件留 SKILL.md）。"""

_HUMAN_GATE = {
    "req-architect": "after-req",
    "dev-engineer": "after-dev",
    "test-engineer": "after-test",
}


def build_sequence(plan) -> list:
    slug = plan["slug"]
    human_gates = set(plan.get("human_gates") or [])
    out = []
    for i, s in enumerate([s for s in plan["stages"] if s.get("on")], 1):
        emp, flow = s["emp"], s["flow"]
        did = f"{slug}#{i}-{emp}"
        gate = _HUMAN_GATE.get(emp)
        out.append({
            "stage_index": i,
            "emp": emp,
            "flow": flow,
            "dispatch_id": did,
            "handoff_from": s.get("handoff_from"),
            "driver_cmd": f"/kdev-flow-driver {emp} --task {slug} --slug {slug}",
            "dispatch_start_cmd": [
                "dispatch-start", flow, slug, "--emp", emp,
                "--dispatch-id", did, "--stage-index", str(i)],
            "dispatch_done_cmd": [
                "dispatch-done", flow, slug, "--emp", emp,
                "--dispatch-id", did, "--status", "done"],
            "human_gate_after": gate if gate in human_gates else None,
        })
    return out
```

- [ ] **Step 4: 跑测试验证通过**

Run: `python3 -m pytest plugins/kdev-team/tests/test_router_drive.py -v`
Expected: PASS（6 passed）

- [ ] **Step 5: Commit**

```bash
git add plugins/kdev-team/kdev_team/drive.py plugins/kdev-team/tests/test_router_drive.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-team): drive.build_sequence —— 冻结 plan → 有序 flow-driver 调用 + dispatch 事件 argv + 停人闸"
```

---

### Task 3.4: `/kdev-team` skill + command（authoring）

**Files:**
- Create: `plugins/kdev-team/skills/kdev-team/SKILL.md`
- Create: `plugins/kdev-team/commands/kdev-team.md`
- Test: `plugins/kdev-team/tests/test_router_skill_manifest.py`（新建）

**Interfaces:**
- Consumes: 全 WP2/WP3 模块（lifecycle/lint/delivery_plan/confirm/drive）+ WP1 CLI dispatch。
- Produces: 主会话三段 loop 的 authoring 文档。可测部分 = manifest/frontmatter 结构 + 关键约束文字在场（参照既有 `test_skill_slimmed.py` / `test_plugin_manifest.py` 风格）。

- [ ] **Step 1: 写失败测试**

```python
# plugins/kdev-team/tests/test_router_skill_manifest.py
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
SKILL = ROOT / "skills/kdev-team/SKILL.md"
CMD = ROOT / "commands/kdev-team.md"


def test_skill_and_command_files_exist():
    assert SKILL.exists()
    assert CMD.exists()


def test_skill_frontmatter_has_name_and_description():
    txt = SKILL.read_text(encoding="utf-8")
    assert txt.startswith("---")
    assert "name:" in txt.split("---", 2)[1]
    assert "description:" in txt.split("---", 2)[1]


def test_skill_documents_three_phase_loop_and_hard_constraint():
    txt = SKILL.read_text(encoding="utf-8")
    for kw in ("plan", "confirm", "drive"):
        assert kw in txt
    # 硬约束：子 agent 不能再开子 agent / flow-driver 跑主会话
    assert "子 agent" in txt and "主会话" in txt
    # 复用现有资产、不开 CEO subagent
    assert "kdev-flow-driver" in txt
    # 诚实债明示
    assert "review-mode" in txt or "评审开关" in txt
    assert "不可恢复" in txt or "断点续跑" in txt


def test_command_frontmatter_argument_hint():
    txt = CMD.read_text(encoding="utf-8")
    assert "argument-hint:" in txt
    assert "kdev-team" in txt or "kdev-ceo" in txt
```

- [ ] **Step 2: 跑测试验证失败**

Run: `python3 -m pytest plugins/kdev-team/tests/test_router_skill_manifest.py -v`
Expected: FAIL —— 文件不存在

- [ ] **Step 3: 写实现（authoring）**

创建 `plugins/kdev-team/commands/kdev-team.md`（frontmatter + 用法，照 kdev-flow-driver.md 风格）：

```markdown
---
description: CEO 总编排——把一个高层目标 LLM 路由到命名生命周期模板，渲染一屏编排结论人确认，再主会话顺序链式调 /kdev-flow-driver 跑通跨员工流水线（MVP：full-delivery 三段）
argument-hint: <高层目标> （别名 /kdev-ceo）
---

# /kdev-team

把"一个高层目标"端到端编排成跨数字员工的交付流水线。调用本命令即加载 kdev-team skill。

## 用法

```
/kdev-team 做用户认证功能
/kdev-team 给 X 项目加一个导出 CSV 的功能
```

## 三段
1. **plan**：LLM 把目标对号入座到 lifecycles 模板，产 delivery-plan（封闭 schema）。
2. **confirm**：lint 校验过 → 渲染一屏编排结论，人确认/微调。
3. **drive**：冻结 delivery-plan，主会话顺序调 /kdev-flow-driver 跑各段（同 slug 接力 + 停人闸）。
```

创建 `plugins/kdev-team/skills/kdev-team/SKILL.md`，frontmatter + 正文覆盖：
- frontmatter：`name: kdev-team`、`description:`（含触发语 "总编排 / CEO / 一个目标跑通需求到测试"）。
- **§0 硬约束**：子 agent 不能再开子 agent → 总编排只能主会话跑；drive 段在主会话顺序调 `/kdev-flow-driver`（它也跑主会话、内部才派 capability agent）；**绝不**把 flow-driver 当 `Agent()` 派出去。
- **§1 plan 段**：读 `lifecycles/*.yml`（`kdev_team.lifecycle.list_templates/load_template`）+ `staff.yml`，LLM 按各模板 `when` 把目标对号入座 → 产 delivery-plan dict（schema 见 `kdev_team.delivery_plan`）。confidence<0.6 或贴两模板 → 必填 runner_up。
- **§2 confirm 段**：`python -c` 调 `kdev_team.lint.validate(plan)`；非空 → 据错修正重出，**不进确认屏**。空 → `kdev_team.confirm.render_screen(plan)` 打印一屏。读人输入 → `kdev_team.confirm.apply_edit(plan, cmd)` 重跑 lint + 重渲，直到人 Enter。confidence<0.6 或丢段 → 禁一键 Enter，强制二次确认。
- **§3 drive 段**：`kdev_team.delivery_plan.write(workspace, plan)` 冻结落 `features/<slug>/delivery-plan.yml`。`kdev_team.drive.build_sequence(plan)` 取有序步骤；逐步：① `python -m kdev_core <dispatch_start_cmd>` 写派单事件 → ② 主会话调 `driver_cmd`（`/kdev-flow-driver`）跑该员工到 terminal/BLOCKED → ③ `python -m kdev_core <dispatch_done_cmd>`（usage 能观测多少填多少，缺则 null）→ ④ 若 `human_gate_after` 非 null，停下向人汇报该段产物、等人确认再进下一步。段间 handoff 零新增：上游 flow-driver 已在交付节点 `handoff-write`，下游 bootstrap 同 slug `handoff-read`（缺失回退裸任务）。
- **§4 评审发函（不碰）**：reviewer gate 由各 flow-driver 内部按 node-table `gate_specs.reviewer` 触发 `reviewer-orchestrator`（硬规：只派 orchestrator）。CEO 不直接发评审函。
- **§5 诚实债（必须对用户明示）**：① 评审开关 per-gate 自动化引擎未建——MVP 确认屏 per-gate 专家/自评仅意图展示，落地靠 `--review-mode {ai,both,human}` 三档 + 手改 node-table；② 跨员工链级进度活主会话内存、无 `delivery-resume` 断点续跑——崩了/换 session 不可恢复，**不得宣称达 L3**。
- **§6 HUD**：delivery-plan.yml + dispatch 事件落 `features/<slug>/` 后，HUD 自动多渲链进度/派单流（`python -m kdev_hud render`）。

- [ ] **Step 4: 跑测试验证通过**

Run: `python3 -m pytest plugins/kdev-team/tests/test_router_skill_manifest.py -v`
Expected: PASS（4 passed）

- [ ] **Step 5: 跑 kdev-team 全量回归**

Run: `python3 -m pytest plugins/kdev-team/tests -q`
Expected: PASS（全绿）

- [ ] **Step 6: Commit**

```bash
git add plugins/kdev-team/skills/kdev-team/SKILL.md plugins/kdev-team/commands/kdev-team.md plugins/kdev-team/tests/test_router_skill_manifest.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-team): /kdev-team skill + command（plan/confirm/drive 三段 + 硬约束 + 诚实债明示）"
```

> **本任务建议走 superpowers:requesting-code-review（opus）**——skill authoring 是 CEO 编排接缝、硬约束/诚实债措辞要准，评审防"宣称达 L3"等越界表述。

---

# WP4 — kdev-hud：派单可视化 + 自包含钻入

### Task 4.1: datasource —— 读 delivery-plan + 派生 dispatch 视图

**Files:**
- Modify: `plugins/kdev-hud/kdev_hud/datasource.py`
- Test: `plugins/kdev-hud/tests/test_dispatch_datasource.py`（新建）

**Interfaces:**
- Produces:
  - `datasource.read_delivery_plan(workspace, slug) -> dict | None`（guarded `import yaml`；缺 PyYAML / 缺文件 / 坏 → None）
  - `datasource._dispatch_views(events) -> list[dict]`：把 dispatch 事件按 `dispatch_id` 配对 → `[{dispatch_id, emp, flow, stage_index, status, started_at, done_at, subagent_tokens, tool_uses, duration_s, running:bool}]`（有 start 无 done → running=True、status="running"）。

- [ ] **Step 1: 写失败测试**

```python
# plugins/kdev-hud/tests/test_dispatch_datasource.py
from kdev_hud import datasource


def _ev(**kw):
    base = {"type": "dispatch", "slug": "s", "flow": "design-flow",
            "emp": "req-architect", "dispatch_id": "s#1-req-architect"}
    base.update(kw)
    return base


def test_dispatch_views_pairs_start_and_done():
    evs = [
        _ev(phase="start", stage_index=1, ts="2026-01-01T00:00:00+00:00"),
        _ev(phase="done", status="done", subagent_tokens=500,
            ts="2026-01-01T00:05:00+00:00"),
    ]
    views = datasource._dispatch_views(evs)
    assert len(views) == 1
    v = views[0]
    assert v["status"] == "done" and v["running"] is False
    assert v["subagent_tokens"] == 500
    assert v["started_at"] == "2026-01-01T00:00:00+00:00"
    assert v["done_at"] == "2026-01-01T00:05:00+00:00"


def test_dispatch_view_running_when_no_done():
    views = datasource._dispatch_views([_ev(phase="start", stage_index=1)])
    assert views[0]["running"] is True
    assert views[0]["status"] == "running"


def test_dispatch_views_ignores_non_dispatch():
    evs = [{"type": "gate", "verdict": "PASS"}, _ev(phase="start", stage_index=1)]
    assert len(datasource._dispatch_views(evs)) == 1


def test_read_delivery_plan_missing_returns_none(tmp_path):
    assert datasource.read_delivery_plan(str(tmp_path), "nope") is None


def test_read_delivery_plan_parses(tmp_path):
    d = tmp_path / ".kdev" / "features" / "s"
    d.mkdir(parents=True)
    (d / "delivery-plan.yml").write_text(
        "template_id: full-delivery\nslug: s\nstages:\n"
        "  - {emp: req-architect, flow: design-flow, on: true}\n",
        encoding="utf-8")
    plan = datasource.read_delivery_plan(str(tmp_path), "s")
    assert plan["template_id"] == "full-delivery"
    assert plan["stages"][0]["emp"] == "req-architect"
```

- [ ] **Step 2: 跑测试验证失败**

Run: `python3 -m pytest plugins/kdev-hud/tests/test_dispatch_datasource.py -v`
Expected: FAIL —— `AttributeError: ... has no attribute '_dispatch_views' / 'read_delivery_plan'`

- [ ] **Step 3: 写最小实现（加到 datasource.py）**

```python
# —— datasource.py 顶部 import 区加 ——
try:
    import yaml as _yaml
except ImportError:        # HUD 自包含：缺 PyYAML 不崩，delivery-plan 视为缺失
    _yaml = None


def read_delivery_plan(workspace, slug):
    """读 features/<slug>/delivery-plan.yml → dict；缺 yaml/缺文件/坏 → None。"""
    if _yaml is None:
        return None
    path = _feature_dir(workspace, slug) / "delivery-plan.yml"
    if not path.exists():
        return None
    try:
        return _yaml.safe_load(path.read_text(encoding="utf-8"))
    except (_yaml.YAMLError, OSError):
        return None


def _dispatch_views(events):
    """dispatch 事件按 dispatch_id 配对 start↔done → 渲染用摘要。"""
    by_id = {}
    order = []
    for e in events:
        if e.get("type") != "dispatch":
            continue
        did = e.get("dispatch_id")
        if did not in by_id:
            by_id[did] = {
                "dispatch_id": did, "emp": e.get("emp"), "flow": e.get("flow"),
                "stage_index": e.get("stage_index"), "status": "running",
                "started_at": None, "done_at": None, "running": True,
                "subagent_tokens": None, "tool_uses": None, "duration_s": None,
            }
            order.append(did)
        v = by_id[did]
        if e.get("phase") == "start":
            v["started_at"] = e.get("ts")
            if e.get("stage_index") is not None:
                v["stage_index"] = e.get("stage_index")
        elif e.get("phase") == "done":
            v["done_at"] = e.get("ts")
            v["status"] = e.get("status") or "done"
            v["running"] = False
            for k in ("subagent_tokens", "tool_uses", "duration_s"):
                if e.get(k) is not None:
                    v[k] = e.get(k)
    return [by_id[d] for d in order]
```

- [ ] **Step 4: 跑测试验证通过**

Run: `python3 -m pytest plugins/kdev-hud/tests/test_dispatch_datasource.py -v`
Expected: PASS（5 passed）

- [ ] **Step 5: Commit**

```bash
git add plugins/kdev-hud/kdev_hud/datasource.py plugins/kdev-hud/tests/test_dispatch_datasource.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-hud): datasource 读 delivery-plan（guarded yaml）+ dispatch 事件配对派生"
```

---

### Task 4.2: datasource —— build_feature_view 接链进度/派单/忙闲

**Files:**
- Modify: `plugins/kdev-hud/kdev_hud/datasource.py`（扩 `build_feature_view`）
- Test: `plugins/kdev-hud/tests/test_dispatch_datasource.py`（追加）

**Interfaces:**
- Produces：`build_feature_view` 返回新增字段：
  - `delivery`：`{template_id, slug, goal, stages:[{emp,flow,on,handoff_from}], total_on, done_count, progress_label}`（无 delivery-plan → None）；`done_count` = dispatch_views 里 status=="done" 数；`progress_label` = `"链进度 {done}/{total}"`。
  - `dispatches`：`_dispatch_views(events)`。
  - `employee_activity`：`[{emp, busy:bool, dispatch_id}]`——dispatch running 的 emp busy=True。

- [ ] **Step 1: 写失败测试（追加）**

```python
import json


def _seed(tmp_path, plan_yaml, events):
    d = tmp_path / ".kdev" / "features" / "auth"
    d.mkdir(parents=True)
    (d / "flow-state.json").write_text(json.dumps({"slug": "auth", "stories": []}),
                                       encoding="utf-8")
    (d / "delivery-plan.yml").write_text(plan_yaml, encoding="utf-8")
    with (d / "events.jsonl").open("w", encoding="utf-8") as f:
        for e in events:
            f.write(json.dumps(e) + "\n")
    return str(tmp_path)


PLAN = ("template_id: full-delivery\nslug: auth\ngoal: g\nstages:\n"
        "  - {emp: req-architect, flow: design-flow, on: true, handoff_from: null}\n"
        "  - {emp: dev-engineer, flow: coding-flow, on: true, handoff_from: req-architect@n8-merge}\n"
        "  - {emp: test-engineer, flow: test-design-flow, on: true, handoff_from: req-architect@n8-merge}\n")


def test_feature_view_has_delivery_progress(tmp_path):
    ws = _seed(tmp_path, PLAN, [
        _ev(phase="start", emp="req-architect", dispatch_id="auth#1-req-architect", stage_index=1, slug="auth"),
        _ev(phase="done", status="done", emp="req-architect", dispatch_id="auth#1-req-architect", slug="auth"),
        _ev(phase="start", emp="dev-engineer", flow="coding-flow", dispatch_id="auth#2-dev-engineer", stage_index=2, slug="auth"),
    ])
    v = datasource.build_feature_view(ws, "auth")
    assert v["delivery"]["total_on"] == 3
    assert v["delivery"]["done_count"] == 1
    assert v["delivery"]["progress_label"] == "链进度 1/3"
    assert len(v["dispatches"]) == 2


def test_employee_activity_marks_running_busy(tmp_path):
    ws = _seed(tmp_path, PLAN, [
        _ev(phase="start", emp="dev-engineer", flow="coding-flow", dispatch_id="auth#2-dev-engineer", stage_index=2, slug="auth"),
    ])
    v = datasource.build_feature_view(ws, "auth")
    busy = {a["emp"]: a["busy"] for a in v["employee_activity"]}
    assert busy.get("dev-engineer") is True


def test_feature_view_no_delivery_plan_is_none(tmp_path):
    d = tmp_path / ".kdev" / "features" / "bare"
    d.mkdir(parents=True)
    (d / "flow-state.json").write_text(json.dumps({"slug": "bare", "stories": []}),
                                       encoding="utf-8")
    v = datasource.build_feature_view(str(tmp_path), "bare")
    assert v["delivery"] is None
    assert v["dispatches"] == []
```

- [ ] **Step 2: 跑测试验证失败**

Run: `python3 -m pytest plugins/kdev-hud/tests/test_dispatch_datasource.py -v`
Expected: FAIL —— `KeyError: 'delivery'`

- [ ] **Step 3: 写最小实现（改 build_feature_view）**

在 `build_feature_view` 末尾、`return {...}` 前加派生，并把新字段加入返回 dict：

```python
    # —— 派单/链进度派生（dispatch 事件 + delivery-plan）——
    dispatches = _dispatch_views(events)
    plan = read_delivery_plan(workspace, slug)
    if plan:
        on_stages = [s for s in (plan.get("stages") or []) if s.get("on")]
        done_count = sum(1 for dv in dispatches if dv["status"] == "done")
        delivery = {
            "template_id": plan.get("template_id"),
            "slug": plan.get("slug", slug),
            "goal": plan.get("goal"),
            "stages": on_stages,
            "total_on": len(on_stages),
            "done_count": done_count,
            "progress_label": f"链进度 {done_count}/{len(on_stages)}",
        }
    else:
        delivery = None
    employee_activity = [
        {"emp": dv["emp"], "busy": dv["running"], "dispatch_id": dv["dispatch_id"]}
        for dv in dispatches
    ]
```

返回 dict 追加三键：

```python
        "delivery": delivery,
        "dispatches": dispatches,
        "employee_activity": employee_activity,
```

- [ ] **Step 4: 跑测试验证通过 + kdev-hud 全量回归**

Run: `python3 -m pytest plugins/kdev-hud/tests -q`
Expected: PASS（原 36 + 新增；既有 build_feature_view 用例不受影响——只增键不改旧键）

- [ ] **Step 5: Commit**

```bash
git add plugins/kdev-hud/kdev_hud/datasource.py plugins/kdev-hud/tests/test_dispatch_datasource.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-hud): build_feature_view 接链进度/派单视图/员工忙闲"
```

---

### Task 4.3: dashboard —— 渲染链进度 + 派单流 + 员工忙闲

**Files:**
- Modify: `plugins/kdev-hud/kdev_hud/dashboard.py`
- Test: `plugins/kdev-hud/tests/test_dispatch_dashboard.py`（新建）

**Interfaces:**
- Consumes: Task 4.2 view-model 新字段（`primary.delivery` / `primary.dispatches` / `primary.employee_activity`）。
- Produces: `dashboard.render` 在 primary 有 `delivery` 时新增三块：① 链进度磁贴/条（progress_label + 各 stage 圈选）；② 派单流 section（每 dispatch：emp + flow + status + usage，running 显示忙绿灯）；③ 员工忙闲条。无 delivery → 不渲染这些块（向后兼容旧快照）。同时把既有「员工级派单待 P-B」那条 note 删除/改为已落地说明（避免 R-009 文档漂移）。

- [ ] **Step 1: 写失败测试**

```python
# plugins/kdev-hud/tests/test_dispatch_dashboard.py
from kdev_hud import dashboard


def _model_with_delivery():
    primary = {
        "slug": "auth", "display_name": "用户认证", "feature_status": "active",
        "stories": [], "stories_done": 0, "stories_total": 0, "completion_pct": 0,
        "active": None, "runs": [], "gates": [], "alerts": [], "alert_count": 0,
        "events": [], "updated_at": "2026-01-01T00:00:00+00:00",
        "delivery": {
            "template_id": "full-delivery", "slug": "auth", "goal": "做认证",
            "stages": [
                {"emp": "req-architect", "flow": "design-flow", "on": True, "handoff_from": None},
                {"emp": "dev-engineer", "flow": "coding-flow", "on": True, "handoff_from": "req-architect@n8-merge"},
                {"emp": "test-engineer", "flow": "test-design-flow", "on": True, "handoff_from": "req-architect@n8-merge"},
            ],
            "total_on": 3, "done_count": 1, "progress_label": "链进度 1/3",
        },
        "dispatches": [
            {"dispatch_id": "auth#1-req-architect", "emp": "req-architect", "flow": "design-flow",
             "stage_index": 1, "status": "done", "started_at": "t0", "done_at": "t1",
             "subagent_tokens": 500, "tool_uses": 8, "duration_s": 60, "running": False},
            {"dispatch_id": "auth#2-dev-engineer", "emp": "dev-engineer", "flow": "coding-flow",
             "stage_index": 2, "status": "running", "started_at": "t2", "done_at": None,
             "subagent_tokens": None, "tool_uses": None, "duration_s": None, "running": True},
        ],
        "employee_activity": [
            {"emp": "req-architect", "busy": False, "dispatch_id": "auth#1-req-architect"},
            {"emp": "dev-engineer", "busy": True, "dispatch_id": "auth#2-dev-engineer"},
        ],
    }
    return {"features": [primary], "feature_count": 1, "primary": primary}


def test_render_shows_chain_progress():
    html = dashboard.render(_model_with_delivery(), generated_at="now")
    assert "链进度 1/3" in html
    assert "full-delivery" in html


def test_render_shows_dispatch_flow_with_emp_and_status():
    html = dashboard.render(_model_with_delivery(), generated_at="now")
    assert "开发工程师" in html or "dev-engineer" in html
    assert "running" in html or "进行中" in html
    assert "500" in html        # usage tokens 渲染


def test_render_marks_busy_employee():
    html = dashboard.render(_model_with_delivery(), generated_at="now")
    # 忙闲条：dev busy
    assert "忙" in html or "busy" in html.lower()


def test_render_without_delivery_omits_chain_block():
    model = _model_with_delivery()
    model["primary"]["delivery"] = None
    model["primary"]["dispatches"] = []
    model["primary"]["employee_activity"] = []
    html = dashboard.render(model, generated_at="now")
    assert "链进度" not in html       # 无 delivery 不渲染
```

- [ ] **Step 2: 跑测试验证失败**

Run: `python3 -m pytest plugins/kdev-hud/tests/test_dispatch_dashboard.py -v`
Expected: FAIL —— "链进度" 不在输出

- [ ] **Step 3: 写最小实现（改 dashboard.render）**

在 `render` 内、组装 `inner` 之前构造派单块（仅 `primary.get("delivery")` 时）；`_DISPLAY` 复用一份 emp→中文映射（dashboard 内定义）。示例片段：

```python
_EMP_DISPLAY = {"req-architect": "需求架构师", "dev-engineer": "开发工程师",
                "test-engineer": "测试工程师"}


def _render_delivery(primary):
    d = primary.get("delivery")
    if not d:
        return ""
    # 链进度条
    steps = "".join(
        f'<span class="gate {"pass" if i < d["done_count"] else ""}">'
        f'{i+1}. {escape(_EMP_DISPLAY.get(s["emp"], s["emp"]))}</span>'
        for i, s in enumerate(d["stages"]))
    # 派单流
    disp_rows = ""
    for dv in primary.get("dispatches", []):
        emp = escape(_EMP_DISPLAY.get(dv["emp"], dv["emp"] or "?"))
        st = "进行中" if dv["running"] else escape(str(dv["status"]))
        usage = (f' · {dv["subagent_tokens"]} tok' if dv.get("subagent_tokens") else "")
        usage += (f' · {dv["tool_uses"]} tools' if dv.get("tool_uses") else "")
        usage += (f' · {dv["duration_s"]}s' if dv.get("duration_s") else "")
        dot = '<i style="color:var(--green)">●</i> ' if dv["running"] else ""
        disp_rows += (f'<div class="story"><span>{dot}{emp} · {escape(dv["flow"] or "")}</span>'
                      f'<span class="muted">{st}{usage}</span></div>')
    # 忙闲
    busy_rows = "".join(
        f'<span class="gate {"fail" if a["busy"] else ""}">'
        f'{escape(_EMP_DISPLAY.get(a["emp"], a["emp"]))}：{"忙" if a["busy"] else "闲"}</span>'
        for a in primary.get("employee_activity", []))
    return (
        f'<div class="sec"><div class="sh t">🧭 交付链 · {escape(d["template_id"])} '
        f'（{escape(d["progress_label"])}）</div><div class="sb">{steps}</div></div>'
        f'<div class="sec"><div class="sh">📡 派单流</div><div class="sb">'
        f'{disp_rows or "<span class=muted>暂无派单</span>"}</div></div>'
        f'<div class="sec"><div class="sh">👤 员工忙闲</div><div class="sb">{busy_rows}</div></div>'
    )
```

把 `_render_delivery(primary)` 插入 `inner`（建议放在「当前活动」section 之后），并删除既有 `act += '<div class="note">注：员工级派单/忙闲事件待 P-B handoff 落地...'` 那行 note（改为：当无 delivery 时才提示「本需求非 CEO 编排链」可选）。

- [ ] **Step 4: 跑测试验证通过 + kdev-hud 全量回归**

Run: `python3 -m pytest plugins/kdev-hud/tests -q`
Expected: PASS（既有 dashboard 用例不破 + 新 4 passed）

- [ ] **Step 5: Commit**

```bash
git add plugins/kdev-hud/kdev_hud/dashboard.py plugins/kdev-hud/tests/test_dispatch_dashboard.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-hud): dashboard 渲染交付链进度 + 派单流 + 员工忙闲"
```

---

### Task 4.4: 自包含 hud.html 钻入（JS 展开 + location.hash 扛刷新）

**Files:**
- Modify: `plugins/kdev-hud/kdev_hud/dashboard.py`（派单流每行可点开详情面板 + JS + hash 恢复）
- Test: `plugins/kdev-hud/tests/test_drilldown_html.py`（新建）

**Interfaces:**
- Consumes: Task 4.3 派单流。
- Produces: 派单流每个 dispatch 行可点击展开「单派单详情面板」（内联 stage_index / dispatch_id / handoff / usage 全字段），用纯 JS（无外链）`toggle` + `:target`/`location.hash` 让展开态在 2s `location.reload()` 后由 hash 恢复。`_page` 注入的 reload 脚本改为「reload 后按 `location.hash` 重新展开对应面板」。

- [ ] **Step 1: 写失败测试**

```python
# plugins/kdev-hud/tests/test_drilldown_html.py
from kdev_hud import dashboard


def _model_with_delivery():
    """内联夹具（禁跨 test 文件导入，见 plan 测试基建约定）——与 test_dispatch_dashboard 同形。"""
    primary = {
        "slug": "auth", "display_name": "用户认证", "feature_status": "active",
        "stories": [], "stories_done": 0, "stories_total": 0, "completion_pct": 0,
        "active": None, "runs": [], "gates": [], "alerts": [], "alert_count": 0,
        "events": [], "updated_at": "2026-01-01T00:00:00+00:00",
        "delivery": {
            "template_id": "full-delivery", "slug": "auth", "goal": "做认证",
            "stages": [
                {"emp": "req-architect", "flow": "design-flow", "on": True, "handoff_from": None},
                {"emp": "dev-engineer", "flow": "coding-flow", "on": True, "handoff_from": "req-architect@n8-merge"},
                {"emp": "test-engineer", "flow": "test-design-flow", "on": True, "handoff_from": "req-architect@n8-merge"},
            ],
            "total_on": 3, "done_count": 1, "progress_label": "链进度 1/3",
        },
        "dispatches": [
            {"dispatch_id": "auth#1-req-architect", "emp": "req-architect", "flow": "design-flow",
             "stage_index": 1, "status": "done", "started_at": "t0", "done_at": "t1",
             "subagent_tokens": 500, "tool_uses": 8, "duration_s": 60, "running": False},
            {"dispatch_id": "auth#2-dev-engineer", "emp": "dev-engineer", "flow": "coding-flow",
             "stage_index": 2, "status": "running", "started_at": "t2", "done_at": None,
             "subagent_tokens": None, "tool_uses": None, "duration_s": None, "running": True},
        ],
        "employee_activity": [
            {"emp": "req-architect", "busy": False, "dispatch_id": "auth#1-req-architect"},
            {"emp": "dev-engineer", "busy": True, "dispatch_id": "auth#2-dev-engineer"},
        ],
    }
    return {"features": [primary], "feature_count": 1, "primary": primary}


def test_dispatch_rows_have_detail_panels_with_anchor_ids():
    html = dashboard.render(_model_with_delivery(), generated_at="now")
    # 每 dispatch 一个可锚定的详情面板（id 含 dispatch_id 派生）
    assert 'id="d-auth' in html or 'id="dispatch-auth' in html
    # 详情面板内含全字段
    assert "auth#2-dev-engineer" in html
    assert "req-architect@n8-merge" in html or "handoff" in html.lower()


def test_drilldown_uses_inline_js_no_external():
    html = dashboard.render(_model_with_delivery(), generated_at="now")
    assert "http://" not in html and "https://" not in html   # 零外链
    assert "<script" in html


def test_reload_preserves_open_panel_via_hash():
    html = dashboard.render(_model_with_delivery(), generated_at="now")
    # reload 脚本必须把当前 location.hash 带回（扛 2s 自动刷新）
    assert "location.hash" in html
    assert "location.reload" in html


def test_panels_collapsed_by_default():
    html = dashboard.render(_model_with_delivery(), generated_at="now")
    # 详情面板默认折叠（CSS：未 :target 时 display:none）
    assert ":target" in html or "hud-panel" in html
```

- [ ] **Step 2: 跑测试验证失败**

Run: `python3 -m pytest plugins/kdev-hud/tests/test_drilldown_html.py -v`
Expected: FAIL —— 锚定面板 / location.hash 不在输出

- [ ] **Step 3: 写最小实现**

① 在 `_CSS` 末尾追加钻入面板样式（默认折叠、`:target` 展开）：

```python
.hud-panel{display:none;border:1px dashed var(--line);border-radius:6px;
margin:4px 0 8px;padding:8px 10px;font-size:11px;color:var(--mut);background:#0e1d31}
.hud-panel:target{display:block}
.drill{color:var(--blue);text-decoration:none;font-size:10px;margin-left:6px}
```

② `_render_delivery` 的派单行改为带「详情」锚 + 紧随的折叠面板（`id` 用 dispatch_id 做 DOM-safe 派生：替换 `#`→`-`）：

```python
        pid = "d-" + (dv["dispatch_id"] or "x").replace("#", "-").replace(":", "-")
        disp_rows += (
            f'<div class="story"><span>{dot}{emp} · {escape(dv["flow"] or "")}'
            f'<a class="drill" href="#{pid}">详情▾</a></span>'
            f'<span class="muted">{st}{usage}</span></div>'
            f'<div class="hud-panel" id="{pid}">'
            f'派单 {escape(dv["dispatch_id"] or "")} · 阶段 {dv.get("stage_index")}<br>'
            f'状态 {st} · tokens {dv.get("subagent_tokens")} · tools {dv.get("tool_uses")}'
            f' · {dv.get("duration_s")}s<br>'
            f'起 {escape(str(dv.get("started_at")))} · 止 {escape(str(dv.get("done_at")))}'
            f'</div>')
```

③ `_page` 的 reload 脚本改为带 hash 回跳（展开态在 2s 刷新后由 `:target` 恢复）：

```python
        f"<script>setTimeout(function(){{"
        f"var h=location.hash;location.reload();"
        f"if(h){{location.hash=h;}}"
        f"}},{_RELOAD_MS});</script>"
```

> 说明：`location.reload()` 默认保留 URL fragment，浏览器刷新后 `:target` 自动复原展开面板；脚本里显式重设 `location.hash` 是双保险（部分内嵌预览器刷新后丢 hash）。零 JS 事件绑定、纯 `:target` CSS 驱动 → 自包含、无外链。

- [ ] **Step 4: 跑测试验证通过 + kdev-hud 全量回归**

Run: `python3 -m pytest plugins/kdev-hud/tests -q`
Expected: PASS（全绿）

- [ ] **Step 5: 活体验证（生成真 hud.html 看渲染）**

Run:
```bash
cd /home/lyadmin/Projects/kdev-router-mvp
python3 - <<'PY'
import sys, json, tempfile, pathlib
sys.path.insert(0, "plugins/kdev-hud")
from kdev_hud import dashboard
# 复用测试夹具构造 model
sys.path.insert(0, "plugins/kdev-hud/tests")
from test_dispatch_dashboard import _model_with_delivery
html = dashboard.render(_model_with_delivery(), generated_at="2026-06-17T00:00:00+00:00")
out = pathlib.Path("/tmp/router-hud-preview.html"); out.write_text(html, encoding="utf-8")
print("wrote", out, len(html), "bytes")
assert "链进度 1/3" in html and "location.hash" in html
print("OK chain + hash present")
PY
```
Expected: 打印 `OK chain + hash present`；产物可 file:// 打开人眼核对（截图存 screenshots/ 不进 git）。

- [ ] **Step 6: Commit**

```bash
git add plugins/kdev-hud/kdev_hud/dashboard.py plugins/kdev-hud/tests/test_drilldown_html.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-hud): 自包含 hud.html 派单钻入（:target 折叠面板 + location.hash 扛 2s 自动刷新）"
```

> **本任务建议走 superpowers:requesting-code-review（opus）**——HUD 是观测层、自包含/降级/零外链是铁律，评审防 XSS（escape 覆盖）+ 刷新丢 hash 边界。

---

# 集成验收 + 收尾

### Task 5.1: 端到端数据流集成测试

**Files:**
- Test: `plugins/kdev-team/tests/test_router_e2e_dataflow.py`（新建）

**Interfaces:**
- Consumes: 全栈（lifecycle→lint→delivery_plan.write→kdev_core dispatch CLI→kdev_hud build_feature_view）。模拟 full-delivery 链：分类得 plan → lint 过 → 冻结 → 写 dispatch start/done（req done + dev start）→ HUD 渲染链进度 1/3。

- [ ] **Step 1: 写集成测试**

```python
# plugins/kdev-team/tests/test_router_e2e_dataflow.py
import sys
from pathlib import Path

# kdev-hud 不在默认 path，显式加
sys.path.insert(0, str(Path(__file__).resolve().parents[2] / "kdev-hud"))

from kdev_team import lifecycle, lint, delivery_plan as dp
from kdev_core import events
from kdev_hud import datasource


def test_full_delivery_dataflow_renders_chain_progress(tmp_path):
    ws = str(tmp_path)
    # 1. 分类：照 full-delivery 模板产 plan
    t = lifecycle.load_template("full-delivery")
    plan = dp.parse(f"""
template_id: full-delivery
slug: e2e-auth
goal: 做认证
confidence: 0.9
reasoning: r
stages:
  - {{emp: req-architect, flow: design-flow, on: true, handoff_from: null}}
  - {{emp: dev-engineer, flow: coding-flow, on: true, handoff_from: req-architect@n8-merge}}
  - {{emp: test-engineer, flow: test-design-flow, on: true, handoff_from: req-architect@n8-merge}}
human_gates: [after-req]
""")
    # 2. lint 过
    assert lint.validate(plan) == []
    # 3. 冻结
    dp.write(ws, plan)
    # 4. 写 dispatch 事件：req done, dev running
    events.append_event(ws, "e2e-auth", events.dispatch_event(
        phase="start", slug="e2e-auth", flow="design-flow", emp="req-architect",
        dispatch_id="e2e-auth#1-req-architect", stage_index=1))
    events.append_event(ws, "e2e-auth", events.dispatch_event(
        phase="done", slug="e2e-auth", flow="design-flow", emp="req-architect",
        dispatch_id="e2e-auth#1-req-architect", status="done"))
    events.append_event(ws, "e2e-auth", events.dispatch_event(
        phase="start", slug="e2e-auth", flow="coding-flow", emp="dev-engineer",
        dispatch_id="e2e-auth#2-dev-engineer", stage_index=2))
    # HUD 需要 flow-state.json 才认 feature
    fs = Path(ws) / ".kdev" / "features" / "e2e-auth" / "flow-state.json"
    fs.write_text('{"slug":"e2e-auth","stories":[]}', encoding="utf-8")
    # 5. HUD 渲染链进度
    v = datasource.build_feature_view(ws, "e2e-auth")
    assert v["delivery"]["progress_label"] == "链进度 1/3"
    busy = {a["emp"]: a["busy"] for a in v["employee_activity"]}
    assert busy["dev-engineer"] is True and busy["req-architect"] is False
```

- [ ] **Step 2: 跑集成测试**

Run: `python3 -m pytest plugins/kdev-team/tests/test_router_e2e_dataflow.py -v`
Expected: PASS（1 passed）

- [ ] **Step 3: 跑三插件全量回归（最终绿）**

> ⚠️ 三套**必须分开跑**——kdev-core/tests 与 kdev-hud/tests 都有 `__init__.py` 形成同名 `tests` 包，合跑会 `ImportPathMismatchError`。

Run:
```bash
cd /home/lyadmin/Projects/kdev-router-mvp
for d in kdev-core kdev-hud kdev-team; do echo "## $d"; python3 -m pytest plugins/$d/tests -q || break; done
```
Expected: 三套各自 PASS（全绿，贴各套真实计数）

- [ ] **Step 4: Commit**

```bash
git add plugins/kdev-team/tests/test_router_e2e_dataflow.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "test(kdev-team): full-delivery 端到端数据流集成测试（lint→冻结→dispatch→HUD 链进度）"
```

---

### Task 5.2: 版本 bump + CHANGELOG（G-004）

**Files:**
- Modify: `plugins/kdev-core/.claude-plugin/plugin.json`（0.3.0 → 0.4.0）+ `plugins/kdev-core/CHANGELOG.md`
- Modify: `plugins/kdev-hud/.claude-plugin/plugin.json`（0.1.0 → 0.2.0）+ `plugins/kdev-hud/CHANGELOG.md`
- Modify: `plugins/kdev-team/.claude-plugin/plugin.json`（0.8.1 → 0.9.0）+ `plugins/kdev-team/CHANGELOG.md`

**说明：** 三插件都加了新能力（minor bump）。kdev-core 加 dispatch 事件+CLI；kdev-hud 加派单可视化+钻入；kdev-team 加 lifecycles+lint+/kdev-team skill。各 CHANGELOG 顶部加一节，注明本 MVP 改动 + G-004「须刷 marketplace+重启」。

- [ ] **Step 1: 三处 plugin.json version 改新值**（minor bump）

- [ ] **Step 2: 三处 CHANGELOG.md 顶部加节**（日期 2026-06-17，列本插件改动 + 关联本 plan + Q 20260617-182851 编排路由器决策）

- [ ] **Step 3: 跑全量回归确认 manifest 测试仍绿**（三套分开跑，理由同上）

Run: `for d in kdev-core kdev-hud kdev-team; do echo "## $d"; python3 -m pytest plugins/$d/tests -q || break; done`
Expected: PASS（含各 test_plugin_manifest 读到新 version）

- [ ] **Step 4: Commit**

```bash
git add plugins/kdev-core/.claude-plugin/plugin.json plugins/kdev-core/CHANGELOG.md \
        plugins/kdev-hud/.claude-plugin/plugin.json plugins/kdev-hud/CHANGELOG.md \
        plugins/kdev-team/.claude-plugin/plugin.json plugins/kdev-team/CHANGELOG.md
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "chore: bump kdev-core 0.4.0 / kdev-hud 0.2.0 / kdev-team 0.9.0（编排路由器 MVP，G-004 须刷 marketplace+重启）"
```

---

## Self-Review（plan vs spec 核对）

**Spec 覆盖：**
- §1/§2 三段主 loop + 纯主会话 skill → WP3 Task 3.4 SKILL.md（§0 硬约束、§1/2/3 三段）。✅
- §3 LLM 决策契约 + 模板目录 → WP2 Task 2.2 lifecycle + full-delivery.yml；LLM 分类是 authoring（Task 3.4 §1）。✅
- §3 输出 schema + 校验器钉死 reviews 值域/同 slug → WP2 Task 2.4 schema + Task 2.5 lint。✅
- §4 确认屏（评审项从 gate_specs 真实生成、禁一键 Enter） → WP3 Task 3.1 render_screen + Task 3.2 apply_edit。✅
- §5 派发执行（不外层 init、handoff 零新增、评审发函路由器不碰、停人）→ Task 3.3 build_sequence + Task 3.4 §3/§4。✅
- §5 loop 落点 C（HUD 当前台、delivery-plan 丢 features/ 让 HUD 多渲链进度）→ WP4 全部 + delivery_plan.write。✅
- §5/§7 诚实债 1（评审开关降级 review-mode）+ 诚实债 2（链级不可恢复、不宣称 L3）→ Task 3.4 §5 明示 + Global Constraints OUT OF SCOPE。✅
- §6 复用零改 + 新建最小四件 → 全 plan 守 Global Constraints。✅
- WP1 dispatch 事件（派单写 + 完成回填 usage）→ Task 1.1/1.2。✅
- WP4 自包含 hud.html 钻入（内联详情 + JS 展开 + location.hash 扛刷新 + 大产物 file:// 不内联）→ Task 4.4（大产物本 MVP 无产物链接，钻入面板只放元数据，符合"不内联大产物"）。✅
- delivery-plan.yml 归属 → Global Constraints + 数据契约：kdev-team 写/校验，kdev-hud 只读，kdev-core 不感知。✅

**占位符扫描：** 无 TBD/TODO；每 code step 给真代码；每 test step 给真断言。✅

**类型一致性：** `dispatch_event` 签名（events.py）↔ CLI 参数（cli.py）↔ `_dispatch_views` 消费字段（datasource）↔ dashboard 渲染字段，逐一对齐：`dispatch_id/phase/emp/flow/stage_index/status/subagent_tokens/tool_uses/duration_s/handoff_from`。`build_sequence` 输出键 ↔ SKILL.md §3 消费一致。`delivery` view 键（total_on/done_count/progress_label/stages）↔ dashboard `_render_delivery` 消费一致。✅

**OUT OF SCOPE 守界：** per-gate merge / delivery-resume / 其余 4 模板 均未进任何 task。✅
