# 数字员工整体设计路线图 Roadmap v1.0

| 项 | 值 |
|---|---|
| 文档状态 | **整体设计路线图 v1.0**（集群开发的单一真相源：跨阶段排序 + live 进度；承 Q-004 起步对齐扩展而来）|
| lifecycle | design |
| 日期 | 2026-06-06 |
| 范围 | 数字员工集群从起步到北极星的整体设计路线图 + live 执行进度（§1.5 为单一真相源）。起源：回答 Q-004『从低起步』怎么落到可动手；现扩展为全阶段排序追踪 |
| 关联决策 | [Q-004 从低起步](../../../.kdev/memory/决策日志.md) · [Q-007 抽共性底座](../../../.kdev/memory/决策日志.md) · [Q-008 底座组成](../../../.kdev/memory/决策日志.md) · [Q-009 记忆 git 托管](../../../.kdev/memory/决策日志.md) |
| 配套 | [底座设计总纲 v1.0](./_archive/2026-06-05-01-kdev-core底座设计总纲-v1.0.md) · [员工能力专项 v1.5](./_archive/2026-05-28-01-KDev数字员工架构-员工能力专项-v1.5.md) · [记忆架构 v0.1](./2026-06-10-05-KDev数字员工-记忆底座-合稿-v1.0.md) · [概念模型 v0.5](./_archive/2026-06-02-01-KDev数字员工概念模型-双轴+等级阶梯-v0.1.md) |
| 参考（reference-only）| [X1 群组 vs X3 矩阵对比（已 superseded）](./_archive/2026-05-27-02-KDev多智能体集群-X1群组-vs-X3矩阵对比.md) · X3 spike：`/home/lyadmin/Projects/kdev-agents-x3/plugins/kdev-cluster-x3/` |

---

## 0. 一句话

把 Q-004「从低起步」落成一条 **walking-skeleton roadmap**：**底座先行**（kdev-core 核心三件 R1/R2/R3 + git 托管前置），但**不把全部记忆基建压在第一个员工之前**——**开发工程师（coding-flow）在 lean 底座上端到端跑通一个真实任务（史上首次实测）**，再按真实需求长记忆 scope / 第二个员工 / 评审专家 / 框架层，逐级爬向 L4。

---

## 1. 决策摘要（本轮拍板）

| # | 决策点 | 拍板 | 依据 |
|---|---|---|---|
| 1 | Roadmap 排法 | **B — walking-skeleton**（底座先行，记忆基建按需长，非全串行）| 集群至今**零实测**，首次端到端证据越早越好；底座按真实需求 right-size 防过度抽象 |
| 2 | 第一个员工 | **开发工程师 / coding-flow**，自主度 **L2 协同** | 价值密度最高（真产出软件）+ 一口气压测 R3 三类 gate + Auto Mode；接底座白捡 resume |
| 3 | X1/X3 去向 | **重建，只参考 X3**（X1 出局；二者均未端到端实测）| canonical v1.5 已倒向 X3 矩阵；X1/X3 仅单测脚手架，非可信成品 |
| 4 | 记忆分层时机 | 阶段1 用 **flat 单 scope**（够用）；并发员工出现（阶段2）才上 **scope + JSONL 操作层** | scope/JSONL 是"有并发员工才需要"的，单员工 flat 足够（[记忆架构 §4](./2026-06-10-05-KDev数字员工-记忆底座-合稿-v1.0.md) 已写明 flat→scoped 迁移路径）|
| 5 | git 托管时机 | **Q-009 nested-repo + 自举 hook 提前到阶段0 前置** | 便宜 + 保命；同时保护"当前正在写的设计记忆"（现 machine-local 未托管）|
| 6 | dogfood 任务 | **UED 6.0 改造 benchmark**（RuoYi-Vue3-FastAPI 视觉改造，materials 在 `测试题目/`）· **两 pass**（Pass1 小切片验底座 / Pass2 全量原样拿可比分）| 真实可比 benchmark；闭 X1/X3 时代 sop_test0518 实测的洞 |

---

## 1.5 执行进度（live · 更新于 2026-06-10）

> 本节随实施推进更新——每完成一块回写状态 + 凭据。详细轨迹见 `.kdev/memory/执行日志.md` Step main-33~48。

| 阶段 | 块 | 状态 | 凭据 |
|---|---|---|---|
| **阶段0** | R1 flow-state | ✅ merged + push | [P1 plan](../../superpowers/plans/2026-06-06-kdev-core-R1-flow-state.md) · 22 测试 · Step main-36 |
| **阶段0** | R2 node machine | ✅ merged + push | [P2 plan](../../superpowers/plans/2026-06-06-kdev-core-R2-node-machine.md) · 25 测试 · Step main-37 |
| **阶段0** | R3 三类 gate | ✅ merged + push | [P3 plan](../../superpowers/plans/2026-06-06-kdev-core-R3-gates.md) · 19 测试 · Step main-38 |
| **阶段0** | P4 git 托管 (Q-009) | ✅ 机制 merged · ✅ 激活生效 | [P4 plan](../../superpowers/plans/2026-06-06-kdev-memory-P4-git-sync.md) · 15 测试 · Step main-39 · 记忆仓 `KDevSec/kdev-agents-memory`(private) · 激活 commit `93fb679`（用户亲跑 bootstrap；SessionStart hook 未自触发=**plugin cache 版本键控 stale**，已 bump 0.13.0 修复见 [G-004](../../../.kdev/memory/踩坑日志.md)，待刷 marketplace 生效）· 护栏见 [G-003](../../../.kdev/memory/踩坑日志.md) |
| **阶段1** | coding-flow 接底座 + UED benchmark dogfood（两 pass）| ✅ 框架侧 done · ✅ **Pass1 跑通** | [P5 plan](../../superpowers/plans/2026-06-06-阶段1-coding-flow接底座.md) · [接入设计](../../superpowers/specs/2026-06-06-阶段1-coding-flow接底座-design.md) · CLI+node-table(16节点)+7persona+SKILL接口节 · kdev-core 76 + coding-flow 8 测试绿 · 分支 feat/p5-coding-flow-kdev-core(merged main) · **Pass1**：底座驱动 13 节点端到端(9 gate · R3 三类全跑 · FAIL→reflow→重评 · resume-across-process · complete 终结) · 6 业务 Agent 派单 · T0+T1 restyle `build:prod` RC=0 · 登录页对版 login.png · dogfood `~/Projects/kdev-dogfood-ued6`(分支 feat/ued6-restyle 8 commit) · Step main-45 · 限制：无 MySQL→真鉴权/T0 布局截图 env-blocked、插画 TODO、gitee→GitHub 镜像 |
| **阶段2** | 第二员工(需求架构师) + 记忆 scope 分离 + 员工集中 kdev-team | 🔨 设计 done · ✅ **P-0 done**(员工集中 kdev-team) · ✅ **P-C1 done**(kdev-memory scope 分离 v0.14.0；P-A geblock 已解) · 数字员工轨 ✅ **P-Core-FF done**(kdev-core v0.2.0 feature-first) · ✅ **P-A done**(req-architect 接底座，kdev-team/design-flow v0.3.0) · ✅ **kdev-hud done**(v0.1.0 观测层) · P-B ⏳ → M2 · kdev-memory减痛轨 ✅ **P-C0.5+P-C1a done**(v0.15.0) · P-C1b ⏳ defer · 详见 §1.5.1/§1.5.2 | [接入设计](../../superpowers/specs/2026-06-07-阶段2-第二员工+记忆scope-design.md) · [P-0 plan](../../superpowers/plans/2026-06-07-阶段2-P0-kdev-team.md) · Q-011 · 序 **P-0**→**P-C1**(kdev-memory scope opt-in)→**P-A**(需求架构师 kdev-design-flow 接底座)→**P-B**(SR/AR→coding-flow handoff) · [kdev-memory污染治理+评分可配 spec v0.2](../../superpowers/specs/2026-06-09-kdev-memory会话污染治理+评分可配-design.md)(减痛轨 P-C0.5/1a/1b，与 P-C1 并行) · **P-0 实测**：新建 `plugins/kdev-team/`（plugin.json v0.1.0 + marketplace 注册）· 7 dev-engineer-* persona→真 CC frontmatter agent（canonical id，可 `Agent({subagent_type})` 直派，除阶段1「主控兼演编排」降级根因）· node-table→`orchestration/dev-engineer.node-table.yml` · `staff.yml` 花名册 · kdev-coding-flow SKILL 回归纯方法论（编排指令归 `dev-engineer-orchestrator` agent）+ 建 plugin.json v0.2.0 · 测试 **kdev-team 11 + kdev-core 76 绿**、coding-flow 旧 persona/orchestration 测试清理 · subagent-driven 6 task + final review **READY-TO-MERGE** · 分支 `stage2-p0-kdev-team`(6 commit) · Step main-47 · ⚠️ [G-004](../../../.kdev/memory/踩坑日志.md) 待用户刷 marketplace+重启 session 才激活 subagent_type 直派 · defer P-C2 JSONL/P-C3 并发锁 |
| 阶段3 | 框架层 + 评审专家 + 协作（L3 起步）| ⏳ | — |
| 阶段4 | CQO + 自演进 + 跨IDE（L4 北极星）| ⏳ defer | — |

> **里程碑 M0 ✅**：底座核心三件 **R1+R2+R3 = 66 测试全绿、main 已 push** + **git 托管已激活生效**（`.kdev/` = nested repo，已 push 记忆仓）。`plugins/kdev-core/` = 完整 lean 底座核心。
>
> **里程碑 M1 ★✅ —— 集群史上首次端到端实测达成**：开发工程师 coding-flow 经 kdev-core 底座（薄 CLI 驱动 R1/R2/R3）端到端跑通 UED6 改造 Pass1（T0+T1）。底座真在驱动（非 prompt 演戏）的硬证据：13 节点经 ~25 次独立 CLI 进程流转 + flow-state.json 全程持久续接（resume-across-process）· 9 gate 覆盖 R3 三类 · 一次 **FAIL(完成验证)→reflow→修→重评 PASS** 活体闭环 · `complete` 终结后 resume 正确拒续 · 自评 vs 第三方评审(deferred) reviewer 绑定生效 · 6 业务 Agent 在对应节点派单 · 真产出 `build:prod` RC=0 + 登录页对版原型。下一步 **Pass2**（整题原样全量，需后端环境）/ 阶段2。

---

### 1.5.1 阶段2 两轨展开（更新于 2026-06-10）

阶段2 = 两条并行轨，**P-0 已 done**：

```
┌─ 数字员工轨（推进集群 · 关键路径）
│   P-C1 记忆 scope 分离 ✅(kdev-memory v0.14.0) → P-A 需求架构师接底座 ✅(2026-06-12，kdev-team v0.3.0 + kdev-design-flow v0.3.0) → P-B 跨员工 handoff → 轻 dogfood 验证
│
└─ kdev-memory 减痛轨（本轮新增 · 不在员工关键路径）
    ✅ P-C0.5 评分可配 + ✅ P-C1a brief分级/subagent精简（v0.15.0，合并一会话/分支）→ P-C1b Step落盘 transcript溯源 ⏳ defer（独立·带实现风险）
    └─ ✅ **实现 done(v0.15.0)**：subagent-driven 7 task TDD + 最终 holistic review READY-TO-MERGE，全量 323 绿；P-C0.5+P-C1a 因共享 4 文件+有依赖故合并，P-C1b 因 transcript 路径机制待验证故隔离 defer
```

- **交叉点 ⚠️**：`P-C1`(scope) 与 `P-C1b`(transcript 溯源) 都改 step 落盘 / step_id——无依赖但有合并冲突风险，注意先后顺序（详见 [污染治理 spec §7](../../superpowers/specs/2026-06-09-kdev-memory会话污染治理+评分可配-design.md)）。
- **排序建议**：**数字员工轨优先**——`P-C1` 是第二员工(`P-A`)的 geblocker（第二员工建在 flat 记忆上 → 归属乱 + flat→scoped 返工，[阶段2 spec §2.1](../../superpowers/specs/2026-06-07-阶段2-第二员工+记忆scope-design.md)）；减痛轨 `P-C0.5`（4-6h 快赢，消除评分打断 + 44 半残 nag）可在任意空档插。
- **凭据**：[阶段2接入设计 spec](../../superpowers/specs/2026-06-07-阶段2-第二员工+记忆scope-design.md) · [kdev-memory污染治理 spec v0.2](../../superpowers/specs/2026-06-09-kdev-memory会话污染治理+评分可配-design.md)
- **defer**：他评智能体（移入后续 roadmap）· P-C2 JSONL · P-C3 并发锁

---

### 1.5.2 前向序：M2 之后的主线（更新于 2026-06-11）

```
[减痛轨 ✅ P-C0.5+P-C1a(v0.15.0) → P-C1b ⏳ defer]   ← P-C1b 待 transcript 机制验证（kdev-memory 基建，支线）
        ↓
✅ P-Core-FF done（kdev-core v0.2.0，2026-06-11）  ★ Q-013 提前：P-A 与 HUD 共同前置
   交付：features/<slug>/ 路径翻转 · flow-state 台账(runs[]/stories[]/active{}) · events.jsonl 流水 · python -m kdev_core migrate
        ↓
✅ P-A 需求架构师接底座（接最终契约，2026-06-12）‖ ✅ kdev-hud 观测层（v0.1.0 通道①②，2026-06-12）
        ↓
P-B 跨员工 handoff ⏳ ← 下一步 → 轻 dogfood 验证   ═══► M2（第二员工并发 + scope/handoff 上线）
        ↓
阶段3（L2 完整 → L3 起步）
  ├─ kdev-core 框架层：派单 / 产物总线（events.jsonl 已在 P-Core-FF 落地）
  ├─ 评审专家（搬 X3 的 checklist + 冲突仲裁）
  └─ 跨员工直接发函硬规            ═══► M3（评审专家 + 协作 = L3 起步）
        ↓
阶段4（L4 北极星，defer）：CQO 元监督 + 自演进 + 跨 IDE

正交支线：Pass2（UED6 整题全量竞赛分，需后端环境，随时可机会性插入）
```

- **M2 定义**：第二员工经 kdev-core 驱动跑通 + scope 分离落对位置 + handoff 接线通 + P-0 合规（[阶段2 spec §7](../../superpowers/specs/2026-06-07-阶段2-第二员工+记忆scope-design.md)）。
- **M3 定义**：评审专家上线（X3 的 checklist + 冲突仲裁）+ 跨员工协作 = L3 起步。
- **P-A 前置（Q-013 已拍板，P-Core-FF 已落地）**：P-A 现可直接接最终契约（`features/<slug>/` + events.jsonl，[编排底座合稿](./2026-06-10-03-KDev数字员工-编排底座-合稿-v1.0.md)），不再建旧布局。见 §1.5.4。
- **✅ P-A done（2026-06-12，kdev-team v0.3.0 + kdev-design-flow v0.3.0）**：第二员工「需求架构师 req-architect」接 kdev-core 底座。`orchestration/req-architect.node-table.yml`（IR→SR→AR→原型→方案 5 阶段 + 3 self 评审 gate，复刻 design-flow 3 闸门）+ 6 `req-architect-*` agent（1 编排 + 5 业务）+ `staff.yml` 词条 + `kdev-flow-driver/references` 追加路由/判据段（**通用 driver 本体不改**，kdev-core 零改）。kdev-design-flow 自带 flow_state 退役（迁 kdev-core feature-first），SKILL 回归方法论参考。测试 **kdev-team 22 / design-flow 29 / kdev-core 126 全绿**，design-flow 触发契约保全。分支 `feat/p-a-req-architect`。⚠️ G-004：bump version 后需用户刷 marketplace + 重启 session 才激活 `req-architect-*` subagent_type 直派。下一步 **P-B**（SR/AR → coding-flow 跨员工 handoff 协议）。

---

### 1.5.3 kdev-hud 观测层计划（第 4 个 plugin · 阶段3 实施）

- **定位**：补全 4-plugin 分工——干活(kdev-team) / 调度(kdev-core) / 记忆(kdev-memory) / **观测(kdev-hud)**。账本的**纯只读消费者**：不记账、不流转、不判断，只把底座记下的渲染成人看得懂的视图。
- **设计状态**：✅ **CANONICAL 设计 done**（[kdev-hud 设计 v1.0](./2026-06-10-04-KDev数字员工-HUD驾驶舱-kdev-hud-设计-v1.0.md)）+ 演讲示意；✅ **通道①② 实施 done**（2026-06-12，`plugins/kdev-hud/` v0.1.0，分支 feat/kdev-hud）——纯只读消费 feature-first 账本，CLI `python3 -m kdev_hud {statusline,render}`，34 测试全绿（fixture 锚定 kdev_core 真实写 API）。通道③ 仍归 CEO/kdev-team。
- **三通道**：① 命令行状态栏 ② 网页实时仪表盘 `hud.html`（①② 归 kdev-hud）③ CEO 对话播报（归 CEO/kdev-team，**非** HUD 组件）。kdev-hud 只做 ①②。
- **数据源 / 依赖**：只读 Q-012 的 `features/<slug>/`（flow-state.json + events.jsonl）——契约 = 文件格式，零额外接口，底座可 headless 单装无反向依赖。
- ✅ **数据源已就绪（2026-06-11）**：P-Core-FF 已完成（kdev-core v0.2.0）——kdev-core 现已实现 feature-first 存储（`features/<slug>/flow-state.json` + `events.jsonl`，含 `stories[]`/`runs[]`/`active{}`）。HUD 可基于已落地数据源启动实施。
- **实施序**：架构上 HUD 可越过阶段3 其余（评审专家/协作/CQO）提前做，数据源（feature-first + events.jsonl）已在 P-Core-FF 落地，前置已解锁。

---

### 1.5.4 排序考量：feature-first 存储 + events.jsonl 提前（HUD 与 P-A 的共同前置 · ✅ Q-013 已拍板）

**背景（核实于 2026-06-11）**：kdev-core 当时仍是阶段0 lean 核心（R1/R2/R3），flow-state 在旧布局 `.kdev/flows/<flow>/<slug>/`、无 `stories[]`、无 `events.jsonl`。**Q-012 feature-first 只在合稿 v1.0 设计、代码未实现**。

**两件事撞同一个前置**：
- **P-A 需求架构师**：建旧布局 → Q-012 落地后返工；应接最终 feature-first 契约
- **kdev-hud 早做**：整个契约 = feature-first 布局 + events.jsonl，数据源不存在就无从读起

**决策**：把 **kdev-core 的 feature-first 存储迁移 + events.jsonl 实现**（原列阶段3 框架层）**提前为一个独立底座块（P-Core-FF），置于 P-A 之前**。一次落地解锁三件：① P-A 接最终契约不返工 ② kdev-hud 数据源就绪、可越过阶段3 其余早做 ③ 阶段3 框架层提前消化一部分。

```
✅ P-Core-FF done（kdev-core v0.2.0，2026-06-11）
   ↓
P-A 需求架构师（接最终契约） ‖ kdev-hud（数据源已就绪，可并行/紧随）
   ↓
P-B handoff → dogfood ═► M2
```

**✅ 已拍板且已实现（[Q-013](../../../.kdev/memory/决策日志.md)，2026-06-11）**：采纳 P-Core-FF 提前，实施序已回写 §1.5.2。**P-Core-FF 已于 2026-06-11 完成（kdev-core v0.2.0，126 测试绿）**：R1 存储层 refit（路径翻转 `features/<slug>/` · `active{}` 对象 · history→events.jsonl 虹吸 · `runs[]/stories[]` 台账 · slug mint `origin/relates_to`）+ `python -m kdev_core migrate` 迁移脚本（幂等）。R2/R3 纯函数不动（adapter 模式）。`handoffs/<员工>/` 目录约定已建，handoff 协议待 P-B。

### 1.5.5 P-Core-FF follow-up（待办 · 2026-06-11 收尾标记）

P-Core-FF 主体（R1 存储 refit）已 done；以下三项**刻意留作 follow-up**（守"本步只动 R1 存储层"红线，未在 P-Core-FF 内闭环）：

| # | follow-up | 范围 / 动作 | 触发 / 依赖 | 状态 |
|---|---|---|---|---|
| **FF-1** | **dogfood 仓数据迁移** | 跑 `python -m kdev_core migrate` 把 `~/Projects/kdev-dogfood-ued6/.kdev/flows/coding-flow/ued6-restyle/` → `features/`（迁移脚本就绪，已对该文件副本实测 23 events 成功；非破坏，先 `--dry-run` 给用户看 report 再实跑）| 一次性；需用户确认后执行（独立仓，不在框架仓 commit 范围）| ⏳ 待用户确认执行 |
| **FF-2** | **集群一键装**（[Q-014](../../../.kdev/memory/决策日志.md) 落地切分）| ① kdev-team `plugin.json` 加 `dependencies: kdev-core` ② 写 `scripts/setup-kdev-team.sh`（仿 `setup-kdev-codegraph.sh` 一键装整簇）③ kdev-flow-driver `PYTHONPATH` 从 `find ~ -name kdev-agents` clone 路径改指向插件 cache 安装位（`${CLAUDE_PLUGIN_ROOT}`）| 动 kdev-team（越"只动 R1"红线，故 defer）；**这步才让 marketplace-装的用户真跑得起 `python -m kdev_core`** | ⏳ defer，专项 |
| **FF-3** | **kdev-hud score 字段提示** | gate 事件当前带 `verdict`(PASS/FAIL)+`issues`，**无 `score`**（计划内 deferred，events 行结构前向兼容——加 `score` 一个 key、不改 shape）| kdev-hud 实施时（§1.5.3）：先按 PASS/FAIL 渲染评审流水，score 作未来加字段 | ✅ 已落实（kdev-hud v0.1.0 按 PASS/FAIL 渲染评审流水；score 作未来加字段，events 行前向兼容） |

> FF-2 是 Q-014「框架分发形态 = CC marketplace + 插件依赖（非 OMC clone）」的落地半截：P-Core-FF Task 9 已把 kdev-core 升为 marketplace 插件（plugin.json + 注册 + CHANGELOG），FF-2 补上「kdev-team 声明依赖 + 一键装 + cache 路径」闭环。

---

## 2. 三工作流 + 5 阶段

**三条工作流**：**A** = kdev-core 底座（R1–R7 + 框架）· **B** = kdev-memory 进化（git / scope / JSONL / rollup）· **C** = 员工层（在底座上重建、参考 X3）。

```
阶段0  kdev-core 核心三件 + git 托管前置
        A: R1(白捡 golden seed) → R2 节点机 → R3 结构化三类 gate
        B: Q-009 nested-repo + 自举 hook + kdev-sync.yml（与 A 并行，不阻塞）
          ▼
阶段1  ★ 开发工程师端到端 dogfood —— 史上首次真实实测 ★   [L2 协同]
        C: coding-flow 13 节点接底座（R1 新接/R2 node-table/R3 gate 映射）
        记忆暂用 flat 单 scope；评审由 AI 内联判（decider:ai），不引入评审专家
          ▼  验收: R3 三类各跑通 + 有界回流 + resume 可续 + 真任务出可运行代码
阶段2  记忆 scope/JSONL + 第二个员工（需求架构师，喂上游 SR/AR）
        B: scope 维度 + JSONL 操作层 + per-scope counter
          ▼
阶段3  框架层 + 评审专家 + 跨员工协作              [L2 完整 → L3 起步]
        kdev-core 框架层: events.jsonl/派单/产物总线 · kdev-hud(新): 三通道观测(只读)
        C: 评审专家（搬 X3 的 10 checklist + 冲突仲裁）+ 直接发函硬规
          ▼
阶段4  CQO 元监督 + 自演进 + 跨 IDE              [L4 自治 · 北极星 · defer]
```

> 自主度阶梯：阶段1 = **L2 协同**（员工跑完整 flow、主控在 gate 确认）→ 阶段3 = L3 起步 → 阶段4 = L4。**L1 不作为起点**——它是单步助手，装不下"端到端跑通一条 flow"，从 L2 起步是承 [Q-006「当前≈L2」](../../../.kdev/memory/决策日志.md) 的自然结论。

---

## 3. 阶段0 — kdev-core 核心三件 + git 托管前置

### 3.1 A 轨：底座核心三件（纯底座，TDD）

| 件 | 范围 | 来源 | 验收 |
|---|---|---|---|
| **R1 状态** | flow-state.json + 原子写(tempfile+os.replace) + active/_meta stale guard + resume | 泛化 [kdev-design-flow/lib/flow_state.py](../../../plugins/kdev-design-flow/lib/flow_state.py)（🔴 最高 ROI，白捡）| 原子写 / 损坏恢复 / resume 单测过 |
| **R2 节点机** | node-table loader（merge L0+L1 编排）+ advance（邻接表 + 守卫 guard(state)→str\|null）+ 有界回流 + 幂等豁免(retry++) | 借 OMC Team Pipeline 范本，不 fork | 顺进 / 回流 / 幂等豁免 / 完成 单测过 |
| **R3 三类 gate** | GateResult(request_id/iter/verdict 结构化 JSON) + record_gate 三类统一解析 + iter≥max→escalate BLOCKED（不 force-accept）+ 双重计数 | [底座总纲 §4.3](./_archive/2026-06-05-01-kdev-core底座设计总纲-v1.0.md)；**X3 反证：gate 逻辑不能埋 prompt 文本** | review/decision/acceptance 各 PASS+FAIL + 超限 escalate 单测过 |

> **R3 一次做全三类**（不止 review）——因为消费者 coding-flow 一上来就用全三类（review 代码/质量 · decision Gate-A/B · acceptance E2E）。这逼底座 R3 从第一天就完整、且被真实消费者验证。
>
> **TDD 推进**，测试设计参考 X3 的 smoke 模式（full-lifecycle + resume-after-interrupt，见 X3 `tests/test_smoke_handoff_dry_run.py`）。**Python 函数非 class**（守底座总纲 §4.1；X3 用 `StateMd` class 是反面参考）。

### 3.2 B 轨：git 托管前置（Q-009，与 A 并行）

- **实现**：kdev-memory 的 **自举 hook**（SessionStart 无 `.kdev/.git` → 从 `kdev-sync.yml` clone；有 → pull；SessionEnd/rollup → commit+push）+ `kdev-sync.yml` schema + [§9.4 托管提醒](./2026-06-10-05-KDev数字员工-记忆底座-合稿-v1.0.md)。
- **立即收益**：把**当前这套设计记忆**（`.kdev/memory/`，现 machine-local 未托管）设为 nested repo，先保命。
- **不阻塞 A**：git 托管是 kdev-memory 独立特性，与底座 TDD 互不依赖，可并行。

---

## 4. 阶段1 — 开发工程师 walking skeleton ★（首次真实实测）

### 4.1 refit 内容

把 [kdev-coding-flow](../../../plugins/kdev-coding-flow/) 的 13 节点 SOP 接上 kdev-core 底座：

- **R1 状态**：coding-flow 现**无**状态持久化 → 新接底座 R1，**白捡 resume**。
- **R2 节点机**：声明 coding-flow 的 13-node-table（节点 + kind + gate）。
- **R3 gate 映射**：Gate-A/B/C + E2E 映射到底座三类 —— 代码/质量评审→**review**、Gate-A/B 路线判断→**decision**、E2E/验收→**acceptance**。**精确 node/gate 映射在 writing-plans 阶段细化。**
- **Auto Mode 留 flow 侧**（守 [Q-008「执行留 flow」](../../../.kdev/memory/决策日志.md)）：底座只提供 R3 的 `awaiting_human` 停靠点，Auto Mode 的"自己往下跑"逻辑留在 coding-flow prompt。

### 4.2 边界（保持骨架薄）

- **记忆**：flat 单 scope（[记忆架构 §4](./2026-06-10-05-KDev数字员工-记忆底座-合稿-v1.0.md) flat 态），不上 scope/JSONL。
- **评审**：开发工程师 solo，gate 由 **AI 内联判**（R3 review-kind，decider:ai 默认，跑 UED §10 自检清单）——**不引入评审专家**（阶段3）、不引入跨员工协作。**竞赛分由外部 benchmark grader 出**，不是我们的 flow。
- **dogfood 任务**：**UED 6.0 改造 benchmark**（RuoYi-Vue3-FastAPI 视觉改造）。题目 + materials 在 `测试题目/`：[EXAM-PROMPT.md](../../../测试题目/EXAM-PROMPT.md) + `01-pic/`（3 原型）+ `02-ued6.0/`（UED 硬约束 + design-tokens + tailwind preset + css）。后端不允许改，纯前端改造。
- **两 pass**（用户定）：
  - **Pass 1 验底座**：砍成小切片（T0 全局主题 + T1 登录页，或 T0 + 用户管理标杆页），**不追分**，目的把 kdev-core R1/R2/R3 + 开发工程师端到端抠通。
  - **Pass 2 真 benchmark**：底座稳后，**整题原样不改**跑全量（T0–T4 全菜单），拿可比竞赛分（题目要求"原样发给每个参赛智能体"，砍题即不可比）。
- **TDD 适配**：视觉改造主体**非 TDD 可驱动** → R3 acceptance 走 **build 通过 + UED §10 自检 + 视觉 diff + 功能冒烟**（登录→首页→用户管理 CRUD）；coding-flow 接底座时需一条「前端/视觉」验证支路，不假设处处 TDD 红绿。

### 4.3 验收标准（tracer-bullet 级，不镀金）

**A. 底座机制类**（证明 R1/R2/R3 真在驱动，不是 prompt 演戏）
1. **R1 真驱动**：flow-state.json 全程结构化记 `current_node` + `history[GateResult]`；中途 kill → `resume` 从上次 node 正确续（非从头）。
2. **R2 真流转**：13 节点按邻接表推进；至少一次 gate 不过的**有界回流**（retry++ 记状态、可查），有上限不死循环。
3. **R3 三类各跑通一次**：review(代码/质量) · decision(Gate-A/B 选分支) · acceptance(E2E) 各至少触发一次正确流转；其中至少一次 **FAIL→修→重评**闭环；GateResult 是结构化 JSON（带 request_id/iter/verdict），下游不靠正则解析。
4. **escalate 不 force-accept**：构造一次评审超限(≥max)，验证转 BLOCKED 升人工、非自动降级接受。

**B. 端到端交付类**（证明它是能干活的员工，不只机制 demo）

5. **真任务端到端**：环境对齐→实施计划→（视觉改造主体非 TDD）→**build 通过 + 功能冒烟**（登录→首页→用户管理 CRUD）跑完，产出可运行前端 + RuoYi 既有功能不破。
6. **L2 画像成立**：员工自跑完整 flow，主控只在 gate 确认/决策，不替员工写代码。

**C. 工程质量类**

7. **底座 R1/R2/R3 单测齐全且过** + 接 coding-flow 后的集成 smoke（参考 X3 full-lifecycle+resume 模式）。

**UED 合规（已确认启用，dogfood 是前端任务）**：**UED §10 自检清单**作为一道 R3 review 闸——禁裸 hex / 8px 网格 / 按钮圆角 2px / 表头 44px / chart palette 定序 / 字体白名单 等，**客观可机检**（grep + 人工抽查）。Pass2 全量时还叠加题目自带的"遍历全菜单逐页截图"评审。

**明确非目标（防镀金）**：不要评审专家(阶段3)、scope/JSONL 记忆(阶段2)、CQO(阶段4)、多员工协作、HUD 完整体(阶段3)；dogfood 求打通不求规模。

---

## 5. 阶段2–4（概要）

| 阶段 | 内容 | 自主度 |
|---|---|---|
| **阶段2** | 上第二个员工（需求架构师，喂上游 SR/AR）→ 出现并发写 → 建 kdev-memory **scope + JSONL 操作层 + per-scope counter**（[记忆架构 §9.6 #3/#5](./2026-06-10-05-KDev数字员工-记忆底座-合稿-v1.0.md)）| L2 |
| **阶段3** | 框架层（kdev-core: events.jsonl/派单/产物总线 + 独立 **kdev-hud** 观测，[设计 v1.0 ✅](./2026-06-10-04-KDev数字员工-HUD驾驶舱-kdev-hud-设计-v1.0.md) 实施⏳，详见 §1.5.3）+ **评审专家**（搬 X3 的 10 checklist + 冲突仲裁 SOP）+ 跨员工直接发函硬规（v1.5 硬规 2/7）| **L2 完整 → L3 起步** |
| **阶段4** | CQO 元监督 + 自演进 + 跨 IDE | **L4 自治（北极星，defer）** |

---

## 6. X3 参考映射（reference-only）

X3 最大价值 = **反向验证 kdev-core 设计方向**：X3 赶工自造 runtime 踩的坑，恰好是 lean 底座要修的点（同一问题两次相遇）。

**搬（成熟设计，照搬当参考）**：12 评审节点表（R/D/T/F + 阻断/告警 + D2 并行）· 10 评审员 checklist（代码评审最细）· 冲突仲裁 3 步 SOP · COMPLETE.md 聚合 manifest · 轻组长 4 职责分离 · agent frontmatter + lint 规则 · smoke 测试模式 · kdev-start-feature 入口结构。

**别学（赶工/技术债/与 canonical 冲突）**：

| X3 做法 | 为何别学 / 底座正解 |
|---|---|
| 状态用 markdown + regex + 全量覆写 + class | R1 必须 **JSON + 原子写 + 函数非 class**（并发会撞）|
| **gate 逻辑埋 prompt 文本**，retry 计数只在 events.log 文字 | R3 必须**结构化 GateResult**，否则没法程序化查重试轮次、resume 全靠正则猜 |
| events.log 单文件 + tab 分隔 + 全量读 | 操作层必须 **per-scope JSONL**（跨 feature 无法 grep、加字段破格式）|
| `AGENT_GROUP`/`AGENT_TO_LEAD` 双份硬编码路由表 | **单一数据源**（已有漂移风险）|
| on-blocked hook 触发条件过宽 + stdout 注入 hack | hook framework **标准化 Blocked 信号** |
| handoffs 只有 COMPLETE.md 无中间 checkpoint | 中间节点失败靠文本推断 → 需结构化 checkpoint |

---

## 7. 风险 + 里程碑

### 7.1 风险

| 风险 | 缓解 |
|---|---|
| coding-flow 最重（13 节点）、新代码最多 → 阶段1 比最薄骨架长 | R1 白捡 golden seed + R3 被真实消费者一次验全；接受"首跑证明硬的"换更强证据 |
| 记忆 flat→scoped 一次迁移（阶段1→2）| [记忆架构 §4](./2026-06-10-05-KDev数字员工-记忆底座-合稿-v1.0.md) 已写明迁移路径；flat 内容归 `memory/shared/` |
| dogfood 是大题（全菜单改造）→ 在大题上 debug 全新底座很痛 | **两 pass**：Pass1 小切片验底座、Pass2 才全量原样；UED 条件项已确认启用（前端任务）|
| 视觉改造非 TDD → coding-flow 的 TDD 节点不契合 | acceptance 改走 build+UED自检+视觉diff+冒烟；coding-flow 加「前端/视觉」验证支路（refit 时细化）|
| 环境重：全栈 standup（RuoYi 前端+FastAPI+DB）+ 双分辨率截图 | 环境对齐员（coding-flow 节点0）扛；截图用 playwright MCP（已连）|

### 7.2 里程碑

- **M0**：kdev-core R1/R2/R3 单测全绿 + git 托管生效（当前设计记忆已 nested-repo）。
- **M1 ★**：开发工程师在真任务上端到端跑通 + 阶段1 验收 7 条全过 = **集群史上首次实测**。
- **M2**：第二个员工并发跑 + 记忆 scope/JSONL 上线。
- **M3**：评审专家 + 跨员工协作 = L3 起步。

---

## 8. 回填 Q-004 待定项

[Q-004](../../../.kdev/memory/决策日志.md) 原三条待定，本稿给出答案：

| Q-004 待定 | 本稿答案 |
|---|---|
| L1 起步态砍哪个维度（员工数 / 每员工能力数 / 流程节点 / 自治程度）| **砍员工数（→1，开发工程师）** + 后续员工/协作/评审/框架 defer；**自主度不砍（L2）**，节点不砍（coding-flow 13 节点全留以压测底座）|
| 等级阶梯如何分层 | **5 阶段 = L2→L2完整→L3→L4 阶梯**（§2）|
| 3 个【…】定稿 + 生命周期 4 宏观期 + HTML 重画（挂起项）| 起步等级已定 → **可解挂**，但属阶段3+ 工作，不在阶段0/1 关键路径 |

---

## 9. 下一步

1. 本稿用户复核 → 转 **superpowers:writing-plans** 起**阶段0（kdev-core R1/R2/R3 + git 托管）**实施计划。
2. 决策回写：[决策日志](../../../.kdev/memory/决策日志.md) Q-004 补「细化」段 + status 推进。
3. dogfood 已锁（UED 改造 benchmark，两 pass）；Pass1 小切片范围（T0+T1 vs T0+用户管理标杆页）在阶段1 启动时定。

---

## 10. 引用

- [底座设计总纲 v1.0](./_archive/2026-06-05-01-kdev-core底座设计总纲-v1.0.md)（R1–R7 + §8 落地路径 C-1~C-8）
- [员工能力专项 v1.5](./_archive/2026-05-28-01-KDev数字员工架构-员工能力专项-v1.5.md)（6 人团队终态蓝图）
- [记忆架构 v0.1](./2026-06-10-05-KDev数字员工-记忆底座-合稿-v1.0.md)（三层 + scope + git 托管 §9）
- [概念模型 v0.5](./_archive/2026-06-02-01-KDev数字员工概念模型-双轴+等级阶梯-v0.1.md)（自主度 L1–L4）
- 金种子：[kdev-design-flow/lib/flow_state.py](../../../plugins/kdev-design-flow/lib/flow_state.py)
- X3 spike（reference-only）：`/home/lyadmin/Projects/kdev-agents-x3/plugins/kdev-cluster-x3/`
</content>
</invoke>
