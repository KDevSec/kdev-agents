# 安全模式 Linker Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 kdev-code-graph 的 ingestor 加一个零 LLM、确定性的"安全规则→代码"模式 linker，把规则节点连到使用了规则点名 API 的代码节点（`related` 边），让 trace/impact 有真边可查。

**Architecture:** 扩展 `security_rules.py` 从规则"适用场景"段抽 pattern token；新增 `linkers/` 子包（`Linker` 协议 + 路径索引 helper + `PatternLinker`）；`cli.py` 加 `link` 子命令编排 `upsert_edge` 落盘；折进 `/kdev-codegraph-build`。设计来源：[安全模式 linker 设计 spec](2026-05-22-安全模式linker设计.md)。

**Tech Stack:** Python 3.11+（stdlib only）、pytest、现有 `kdev_ingestor` 包。

> **提交身份**：本仓库 commit 受 block-unattributed-commit hook 校验，所有 commit 必须用 AI 身份字面值覆盖：
> `git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "..."`
>
> **示例 token 说明**：本计划及测试 fixture 一律用**中性合成 token**（`flagged_call` / `risky_run` / `tainted_load`）演示——它们不含任何真实危险 API 子串，既能完整测试"抽 token→匹配→连边"机制，又不会触发仓库安全提醒 hook。真实 token 在运行时从 kdev-secure-coding 规则文件抽取，机制与 token 是否真实无关。

---

## File Structure

| 文件 | 责任 | 动作 |
|---|---|---|
| `plugins/kdev-code-graph/ingestor/kdev_ingestor/security_rules.py` | 加 `patterns` 字段 + `_extract_patterns` | 改 |
| `plugins/kdev-code-graph/ingestor/tests/fixtures/sample-rules/01-input-validation.md` | 给 3.1.1 加"适用场景"含合成 token | 改 |
| `plugins/kdev-code-graph/ingestor/tests/test_security_rules.py` | 加 pattern 提取测试 | 改 |
| `plugins/kdev-code-graph/ingestor/kdev_ingestor/linkers/__init__.py` | 子包初始化 | 新 |
| `plugins/kdev-code-graph/ingestor/kdev_ingestor/linkers/base.py` | `Linker` 协议 + `build_path_index` + `resolve_node_id` + `make_security_edge` | 新 |
| `plugins/kdev-code-graph/ingestor/kdev_ingestor/linkers/pattern_linker.py` | `PatternLinker` | 新 |
| `plugins/kdev-code-graph/ingestor/tests/test_linkers.py` | base helper + PatternLinker 测试 | 新 |
| `plugins/kdev-code-graph/ingestor/kdev_ingestor/cli.py` | `link` 子命令 | 改 |
| `plugins/kdev-code-graph/ingestor/tests/test_cli.py` | link 端到端 + 幂等测试 | 改 |
| `plugins/kdev-code-graph/skills/kdev-codegraph-build/SKILL.md` | 加 link 步骤 | 改 |
| `plugins/kdev-code-graph/skills/kdev-codegraph-trace/SKILL.md` | 报告 related 边 | 改 |

所有命令在 `plugins/kdev-code-graph/ingestor/` 目录下运行（该目录有 pyproject.toml + 已装 dev venv）。测试运行：`python3 -m pytest <path> -v`。

---

## Task 1: 规则 pattern 提取

**Files:**
- Modify: `plugins/kdev-code-graph/ingestor/kdev_ingestor/security_rules.py`
- Modify: `plugins/kdev-code-graph/ingestor/tests/fixtures/sample-rules/01-input-validation.md`
- Test: `plugins/kdev-code-graph/ingestor/tests/test_security_rules.py`

- [ ] **Step 1: 给 fixture 的 3.1.1 加"适用场景"段（合成 token）**

把 `tests/fixtures/sample-rules/01-input-validation.md` 改为（只在 3.1.1 下加 `### 适用场景`，3.1.2 不加，用于测"无适用场景→空 patterns"）：

```markdown
# 3.1 输入验证与表示

> 概述：输入验证和表示问题通常由元字符、交替编码和数字表示引起，必须对所有输入进行合法性校验。

## 3.1.1 命令操作安全

### 规则
- 避免拼接外部数据进入命令调用，应当使用结构化 API 并对参数进行白名单限制。
- 应避免外部控制系统设置以防止意外的应用程序行为。

### 适用场景
代码中出现 `flagged_call` / `risky_run` / `tainted_load` 等调用时。

## 3.1.2 SQL 操作安全

### 规则
- 必须使用参数化查询，禁止使用字符串拼接或 f-string 构造 SQL。
- ORM 查询应避免 raw 接口接收用户输入。
```

- [ ] **Step 2: 写失败测试**

在 `tests/test_security_rules.py` 末尾追加：

```python
def test_parse_rule_file_extracts_patterns(sample_rules_dir: Path):
    rules = parse_rule_file(sample_rules_dir / "01-input-validation.md")
    assert rules[0].patterns == ["flagged_call", "risky_run", "tainted_load"]
    # 3.1.2 没有"适用场景"段 → 空 patterns
    assert rules[1].patterns == []


def test_security_rule_patterns_default_empty():
    rule = SecurityRule(
        rule_id="3.1.1", title="x", summary="y",
        category="input_validation", source_file=Path("/tmp/x.md"),
    )
    assert rule.patterns == []
```

- [ ] **Step 3: 运行测试确认失败**

Run: `python3 -m pytest tests/test_security_rules.py::test_parse_rule_file_extracts_patterns -v`
Expected: FAIL（`AttributeError: 'SecurityRule' object has no attribute 'patterns'`）

- [ ] **Step 4: 实现 pattern 提取**

在 `security_rules.py`：

第 11 行 `from dataclasses import dataclass` 改为：
```python
from dataclasses import dataclass, field
```

在文件顶部正则区（`_FILENAME_SLUG_RE` 之后）加：
```python
# Matches `### 适用场景` (Chinese applicability section)
_SCENARIO_SECTION_RE = re.compile(r"^###\s+适用场景\s*$", re.MULTILINE)
# Backtick-quoted spans
_BACKTICK_RE = re.compile(r"`([^`]+)`")
# Valid API pattern token: identifier, optionally dotted (e.g. mod.func)
_PATTERN_TOKEN_RE = re.compile(r"^[A-Za-z_][A-Za-z0-9_.]*$")
```

`SecurityRule` dataclass 加字段（放在 `source_file` 之后）：
```python
    patterns: list[str] = field(default_factory=list)
```

加提取函数（放在 `_extract_summary` 之后）：
```python
def _extract_patterns(body: str) -> list[str]:
    """Extract backtick-quoted API tokens from the `### 适用场景` section."""
    m = _SCENARIO_SECTION_RE.search(body)
    if not m:
        return []
    after = body[m.end():]
    nxt = re.search(r"^###\s+", after, re.MULTILINE)
    section = after[: nxt.start()] if nxt else after
    seen: dict[str, None] = {}
    for tok in _BACKTICK_RE.findall(section):
        tok = tok.strip()
        if _PATTERN_TOKEN_RE.match(tok) and tok not in seen:
            seen[tok] = None
    return list(seen.keys())
```

在 `parse_rule_file` 里构造 `SecurityRule` 处（`summary = _extract_summary(body)` 之后、`rules.append(...)` 处）加 `patterns`：
```python
        summary = _extract_summary(body)
        if not summary:
            continue
        rules.append(SecurityRule(
            rule_id=m.group("id"),
            title=m.group("title").strip(),
            summary=summary,
            category=category,
            source_file=path,
            patterns=_extract_patterns(body),
        ))
```

- [ ] **Step 5: 运行测试确认通过 + 全量回归**

Run: `python3 -m pytest tests/test_security_rules.py -v`
Expected: 全部 PASS（新 2 个 + 原有不回归）

- [ ] **Step 6: 提交**

```bash
cd /home/lyadmin/Projects/kdev-agents
git add plugins/kdev-code-graph/ingestor/kdev_ingestor/security_rules.py plugins/kdev-code-graph/ingestor/tests/test_security_rules.py "plugins/kdev-code-graph/ingestor/tests/fixtures/sample-rules/01-input-validation.md"
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-code-graph): ingestor 从规则适用场景段提取 pattern token"
```

---

## Task 2: linkers 子包 —— 协议 + 路径索引 helper + 边构造

**Files:**
- Create: `plugins/kdev-code-graph/ingestor/kdev_ingestor/linkers/__init__.py`
- Create: `plugins/kdev-code-graph/ingestor/kdev_ingestor/linkers/base.py`
- Test: `plugins/kdev-code-graph/ingestor/tests/test_linkers.py`

- [ ] **Step 1: 写失败测试**

新建 `tests/test_linkers.py`：

```python
from kdev_ingestor.graph_io import KnowledgeGraph
from kdev_ingestor.linkers.base import (
    build_path_index,
    resolve_node_id,
    make_security_edge,
)


def _graph_with(nodes):
    return KnowledgeGraph(version="1", project={}, nodes=list(nodes))


def test_resolve_function_level():
    g = _graph_with([
        {"id": "file:a.py", "type": "file", "filePath": "a.py"},
        {"id": "function:a.py:f", "type": "function", "filePath": "a.py",
         "lineRange": [10, 25]},
    ])
    idx = build_path_index(g)
    # line inside the function range → function node
    assert resolve_node_id(idx, "a.py", 15) == "function:a.py:f"


def test_resolve_file_fallback():
    g = _graph_with([
        {"id": "file:a.py", "type": "file", "filePath": "a.py"},
        {"id": "function:a.py:f", "type": "function", "filePath": "a.py",
         "lineRange": [10, 25]},
    ])
    idx = build_path_index(g)
    # line outside any function range → file node
    assert resolve_node_id(idx, "a.py", 3) == "file:a.py"


def test_resolve_missing_path_returns_none():
    g = _graph_with([{"id": "file:a.py", "type": "file", "filePath": "a.py"}])
    idx = build_path_index(g)
    assert resolve_node_id(idx, "other.py", 1) is None


def test_make_security_edge_shape():
    e = make_security_edge("kdev-sec:rule:3.1.1", "function:a.py:f")
    assert e == {
        "source": "kdev-sec:rule:3.1.1",
        "target": "function:a.py:f",
        "type": "related",
        "direction": "forward",
        "weight": 0.6,
    }
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python3 -m pytest tests/test_linkers.py -v`
Expected: FAIL（`ModuleNotFoundError: No module named 'kdev_ingestor.linkers'`）

- [ ] **Step 3: 实现 base.py + 包初始化**

新建 `kdev_ingestor/linkers/__init__.py`（空文件即可）：
```python
```

新建 `kdev_ingestor/linkers/base.py`：
```python
"""Linker family: connect kdev rule nodes to code nodes in a UA graph.

Member 1 is PatternLinker (deterministic, pattern-based). Member 2 (a future
LLM-based SemanticLinker) shares the `Linker` protocol below.
"""

from __future__ import annotations

from pathlib import Path
from typing import Protocol

from kdev_ingestor.graph_io import KnowledgeGraph
from kdev_ingestor.security_rules import SecurityRule

SECURITY_EDGE_TYPE = "related"
SECURITY_EDGE_WEIGHT = 0.6


class Linker(Protocol):
    def link(
        self,
        rules: list[SecurityRule],
        graph: KnowledgeGraph,
        source_root: Path,
    ) -> list[dict]:
        """Return edge dicts (rule node -> code node). Pure; does not persist."""
        ...


def build_path_index(graph: KnowledgeGraph) -> dict[str, dict]:
    """Index graph nodes by filePath for O(1)-ish hit resolution.

    Returns {filePath: {"file": <file_node_id|None>,
                         "funcs": [(start, end, func_node_id), ...]}}
    """
    idx: dict[str, dict] = {}
    for n in graph.nodes:
        fp = n.get("filePath")
        if not fp:
            continue
        entry = idx.setdefault(fp, {"file": None, "funcs": []})
        if n.get("type") == "file":
            entry["file"] = n["id"]
        elif n.get("type") == "function":
            lr = n.get("lineRange")
            if isinstance(lr, (list, tuple)) and len(lr) == 2:
                entry["funcs"].append((lr[0], lr[1], n["id"]))
    return idx


def resolve_node_id(
    path_index: dict[str, dict], rel_path: str, line_no: int
) -> str | None:
    """Map a (file, line) hit to the enclosing function node, else the file node."""
    entry = path_index.get(rel_path)
    if entry is None:
        return None
    for start, end, node_id in entry["funcs"]:
        if start <= line_no <= end:
            return node_id
    return entry["file"]


def make_security_edge(rule_node_id: str, code_node_id: str) -> dict:
    return {
        "source": rule_node_id,
        "target": code_node_id,
        "type": SECURITY_EDGE_TYPE,
        "direction": "forward",
        "weight": SECURITY_EDGE_WEIGHT,
    }
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python3 -m pytest tests/test_linkers.py -v`
Expected: 4 个 PASS

- [ ] **Step 5: 提交**

```bash
cd /home/lyadmin/Projects/kdev-agents
git add plugins/kdev-code-graph/ingestor/kdev_ingestor/linkers/ plugins/kdev-code-graph/ingestor/tests/test_linkers.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-code-graph): 新增 linkers 子包（Linker 协议 + 路径索引 + 边构造）"
```

---

## Task 3: PatternLinker

**Files:**
- Create: `plugins/kdev-code-graph/ingestor/kdev_ingestor/linkers/pattern_linker.py`
- Test: `plugins/kdev-code-graph/ingestor/tests/test_linkers.py`（追加）

- [ ] **Step 1: 写失败测试**

在 `tests/test_linkers.py` 末尾追加：

```python
from pathlib import Path

from kdev_ingestor.security_rules import SecurityRule
from kdev_ingestor.graph_io import upsert_node
from kdev_ingestor.linkers.pattern_linker import PatternLinker


def _rule(rule_id, patterns):
    return SecurityRule(
        rule_id=rule_id, title="t", summary="s",
        category="input_validation", source_file=Path("/tmp/x.md"),
        patterns=patterns,
    )


def _full(n):
    # ensure node passes upsert_node validation
    base = {"name": n["id"], "summary": "s", "tags": [], "complexity": "simple"}
    base.update(n)
    return base


def _graph_with_rule(nodes, rule_node_id):
    g = KnowledgeGraph(version="1", project={}, nodes=[])
    for n in nodes:
        upsert_node(g, _full(n))
    upsert_node(g, {
        "id": rule_node_id, "type": "concept", "name": "r",
        "summary": "s", "tags": ["kdev:security_rule"], "complexity": "moderate",
    })
    return g


def test_pattern_linker_function_level(tmp_path):
    (tmp_path / "a.py").write_text(
        "def f():\n    flagged_call('x')\n", encoding="utf-8"
    )
    g = _graph_with_rule(
        [
            {"id": "file:a.py", "type": "file", "filePath": "a.py"},
            {"id": "function:a.py:f", "type": "function", "filePath": "a.py",
             "lineRange": [1, 2]},
        ],
        "kdev-sec:rule:3.1.1",
    )
    edges = PatternLinker().link([_rule("3.1.1", ["flagged_call"])], g, tmp_path)
    assert edges == [{
        "source": "kdev-sec:rule:3.1.1", "target": "function:a.py:f",
        "type": "related", "direction": "forward", "weight": 0.6,
    }]


def test_pattern_linker_file_fallback(tmp_path):
    # hit on line 1, but function starts at line 5 → falls back to file node
    (tmp_path / "a.py").write_text("flagged_call('top')\n", encoding="utf-8")
    g = _graph_with_rule(
        [
            {"id": "file:a.py", "type": "file", "filePath": "a.py"},
            {"id": "function:a.py:f", "type": "function", "filePath": "a.py",
             "lineRange": [5, 9]},
        ],
        "kdev-sec:rule:3.1.1",
    )
    edges = PatternLinker().link([_rule("3.1.1", ["flagged_call"])], g, tmp_path)
    assert edges[0]["target"] == "file:a.py"


def test_pattern_linker_no_patterns_no_edges(tmp_path):
    (tmp_path / "a.py").write_text("flagged_call('x')\n", encoding="utf-8")
    g = _graph_with_rule(
        [{"id": "file:a.py", "type": "file", "filePath": "a.py"}],
        "kdev-sec:rule:3.1.1",
    )
    assert PatternLinker().link([_rule("3.1.1", [])], g, tmp_path) == []


def test_pattern_linker_skips_rule_not_in_graph(tmp_path):
    (tmp_path / "a.py").write_text("flagged_call('x')\n", encoding="utf-8")
    g = KnowledgeGraph(version="1", project={}, nodes=[])
    upsert_node(g, _full({"id": "file:a.py", "type": "file", "filePath": "a.py"}))
    # rule node NOT injected → no edges
    assert PatternLinker().link([_rule("3.1.1", ["flagged_call"])], g, tmp_path) == []


def test_pattern_linker_dedupes_multiple_hits(tmp_path):
    (tmp_path / "a.py").write_text(
        "flagged_call(1)\nflagged_call(2)\n", encoding="utf-8"
    )
    g = _graph_with_rule(
        [
            {"id": "file:a.py", "type": "file", "filePath": "a.py"},
            {"id": "function:a.py:f", "type": "function", "filePath": "a.py",
             "lineRange": [1, 2]},
        ],
        "kdev-sec:rule:3.1.1",
    )
    edges = PatternLinker().link([_rule("3.1.1", ["flagged_call"])], g, tmp_path)
    assert len(edges) == 1
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python3 -m pytest tests/test_linkers.py -k pattern_linker -v`
Expected: FAIL（`ModuleNotFoundError: ...pattern_linker`）

- [ ] **Step 3: 实现 PatternLinker**

新建 `kdev_ingestor/linkers/pattern_linker.py`：
```python
"""PatternLinker: deterministic rule->code linking via API token matching."""

from __future__ import annotations

import re
from pathlib import Path

from kdev_ingestor.graph_io import KnowledgeGraph
from kdev_ingestor.security_rules import SecurityRule
from kdev_ingestor.linkers.base import (
    build_path_index,
    make_security_edge,
    resolve_node_id,
)


class PatternLinker:
    def link(
        self,
        rules: list[SecurityRule],
        graph: KnowledgeGraph,
        source_root: Path,
    ) -> list[dict]:
        source_root = Path(source_root)

        # (compiled regex, rule_node_id) — only for rules whose node exists.
        compiled: list[tuple[re.Pattern, str]] = []
        for rule in rules:
            rule_node_id = f"kdev-sec:rule:{rule.rule_id}"
            if rule_node_id not in graph._node_id_to_index:
                continue
            for pat in rule.patterns:
                rx = re.compile(r"\b" + re.escape(pat) + r"\b")
                compiled.append((rx, rule_node_id))
        if not compiled:
            return []

        path_index = build_path_index(graph)
        edges: dict[tuple[str, str], dict] = {}
        for rel_path in path_index:
            try:
                text = (source_root / rel_path).read_text(encoding="utf-8")
            except (OSError, UnicodeDecodeError):
                continue
            for line_no, line in enumerate(text.splitlines(), start=1):
                for rx, rule_node_id in compiled:
                    if rx.search(line):
                        node_id = resolve_node_id(path_index, rel_path, line_no)
                        if node_id is None:
                            continue
                        key = (rule_node_id, node_id)
                        if key not in edges:
                            edges[key] = make_security_edge(rule_node_id, node_id)
        return list(edges.values())
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python3 -m pytest tests/test_linkers.py -v`
Expected: 全部 PASS（base 4 + pattern 5）

- [ ] **Step 5: 提交**

```bash
cd /home/lyadmin/Projects/kdev-agents
git add plugins/kdev-code-graph/ingestor/kdev_ingestor/linkers/pattern_linker.py plugins/kdev-code-graph/ingestor/tests/test_linkers.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-code-graph): PatternLinker 实现规则→代码模式关联"
```

---

## Task 4: CLI `link` 子命令

**Files:**
- Modify: `plugins/kdev-code-graph/ingestor/kdev_ingestor/cli.py`
- Test: `plugins/kdev-code-graph/ingestor/tests/test_cli.py`

- [ ] **Step 1: 写失败测试**

先看现有 `tests/test_cli.py` 的 import 与风格（它已测 inject / list-tags，复用其 `main` 调用方式）。在文件末尾追加：

```python
def test_link_creates_related_edges(tmp_path, sample_rules_dir):
    import json
    from kdev_ingestor.cli import main

    # source file using a synthetic flagged token
    src = tmp_path / "src"
    src.mkdir()
    (src / "a.py").write_text("def f():\n    flagged_call('x')\n", encoding="utf-8")

    # minimal UA-shaped graph referencing a.py
    graph_path = tmp_path / "knowledge-graph.json"
    graph_path.write_text(json.dumps({
        "version": "1", "project": {}, "layers": [], "tour": [],
        "nodes": [
            {"id": "file:a.py", "type": "file", "name": "a.py",
             "filePath": "a.py", "summary": "s", "tags": [], "complexity": "simple"},
            {"id": "function:a.py:f", "type": "function", "name": "f",
             "filePath": "a.py", "lineRange": [1, 2], "summary": "s",
             "tags": [], "complexity": "simple"},
        ],
        "edges": [],
    }, ensure_ascii=False), encoding="utf-8")

    # inject rule nodes first, then link
    assert main(["inject", "--rules-dir", str(sample_rules_dir),
                 "--graph", str(graph_path)]) == 0
    assert main(["link", "--rules-dir", str(sample_rules_dir),
                 "--graph", str(graph_path), "--source-root", str(src)]) == 0

    g = json.loads(graph_path.read_text(encoding="utf-8"))
    related = [e for e in g["edges"] if e["type"] == "related"]
    assert any(e["source"] == "kdev-sec:rule:3.1.1"
               and e["target"] == "function:a.py:f" for e in related)


def test_link_idempotent(tmp_path, sample_rules_dir):
    import json
    from kdev_ingestor.cli import main

    src = tmp_path / "src"; src.mkdir()
    (src / "a.py").write_text("flagged_call('x')\n", encoding="utf-8")
    graph_path = tmp_path / "kg.json"
    graph_path.write_text(json.dumps({
        "version": "1", "project": {}, "layers": [], "tour": [],
        "nodes": [{"id": "file:a.py", "type": "file", "name": "a.py",
                   "filePath": "a.py", "summary": "s", "tags": [],
                   "complexity": "simple"}],
        "edges": [],
    }), encoding="utf-8")
    main(["inject", "--rules-dir", str(sample_rules_dir), "--graph", str(graph_path)])
    main(["link", "--rules-dir", str(sample_rules_dir), "--graph", str(graph_path),
          "--source-root", str(src)])
    n1 = len(json.loads(graph_path.read_text())["edges"])
    main(["link", "--rules-dir", str(sample_rules_dir), "--graph", str(graph_path),
          "--source-root", str(src)])
    n2 = len(json.loads(graph_path.read_text())["edges"])
    assert n1 == n2


def test_link_missing_source_root_errors(tmp_path, sample_rules_dir, capsys):
    import json
    from kdev_ingestor.cli import main
    graph_path = tmp_path / "kg.json"
    graph_path.write_text(json.dumps({
        "version": "1", "project": {}, "layers": [], "tour": [],
        "nodes": [], "edges": [],
    }), encoding="utf-8")
    rc = main(["link", "--rules-dir", str(sample_rules_dir),
               "--graph", str(graph_path), "--source-root", str(tmp_path / "nope")])
    assert rc == 2
```

> 注：`sample_rules_dir` fixture 已在 `conftest.py` 提供；其 3.1.1 的 patterns 含 `flagged_call`（Task 1 已加）。

- [ ] **Step 2: 运行测试确认失败**

Run: `python3 -m pytest tests/test_cli.py -k link -v`
Expected: FAIL（`link` 子命令不存在，argparse 报错 / SystemExit）

- [ ] **Step 3: 实现 link 子命令**

在 `cli.py`：

import 区把 `upsert_node` 那行补上 `upsert_edge`：
```python
from kdev_ingestor.graph_io import (
    GraphIOError,
    load_graph,
    save_graph,
    upsert_edge,
    upsert_node,
)
```
顶部加：
```python
from kdev_ingestor.linkers.pattern_linker import PatternLinker
```

加命令函数（放在 `_cmd_list_tags` 之后）：
```python
def _cmd_link(args: argparse.Namespace) -> int:
    graph_path = Path(args.graph)
    source_root = Path(args.source_root)
    if not source_root.exists():
        print(f"error: source root not found: {source_root}", file=sys.stderr)
        return 2
    try:
        graph = load_graph(graph_path)
        rules = parse_rules_dir(Path(args.rules_dir))
    except (GraphIOError, RuleParseError) as exc:
        print(f"error: {exc}", file=sys.stderr)
        return 2

    edges = PatternLinker().link(rules, graph, source_root)
    linked = 0
    for edge in edges:
        try:
            upsert_edge(graph, edge)
            linked += 1
        except GraphIOError:
            continue

    try:
        save_graph(graph, graph_path)
    except GraphIOError as exc:
        print(f"error: {exc}", file=sys.stderr)
        return 2

    print(f"linked {linked} security edge(s) into {graph_path}")
    return 0
```

在 `main()` 里 `p_list` 注册之后、`args = parser.parse_args(argv)` 之前加：
```python
    p_link = sub.add_parser("link", help="link security rules to code by pattern")
    p_link.add_argument("--rules-dir", required=True)
    p_link.add_argument("--graph", required=True)
    p_link.add_argument("--source-root", required=True)
    p_link.set_defaults(func=_cmd_link)
```

- [ ] **Step 4: 运行测试确认通过 + 全量回归**

Run: `python3 -m pytest tests/test_cli.py -v`
Expected: 全部 PASS

- [ ] **Step 5: 提交**

```bash
cd /home/lyadmin/Projects/kdev-agents
git add plugins/kdev-code-graph/ingestor/kdev_ingestor/cli.py plugins/kdev-code-graph/ingestor/tests/test_cli.py
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "feat(kdev-code-graph): cli 新增 link 子命令编排模式关联落盘"
```

---

## Task 5: 更新 build / trace 两个 SKILL.md

**Files:**
- Modify: `plugins/kdev-code-graph/skills/kdev-codegraph-build/SKILL.md`
- Modify: `plugins/kdev-code-graph/skills/kdev-codegraph-trace/SKILL.md`

> 本任务无单测（doc 改动）；通过全量 pytest 确认未破坏，并人工核对措辞。

- [ ] **Step 1: build SKILL —— 加 link 步骤**

在 `kdev-codegraph-build/SKILL.md` 的 `## Step 3：灌入 kdev 安全规范节点` 的 Bash 代码块**之后**、`## Step 4` 之前，新增一段：

````markdown
## Step 3.5：连边（安全规则 → 代码）

inject 只灌规则节点，需再跑 `link` 把规则连到使用了其点名 API 的代码节点（产 `related` 边）。

**Bash / macOS / Linux:**

```bash
python3 "$INGESTOR" link \
    --rules-dir "$KDEV_RULES_DIR" \
    --graph .understand-anything/knowledge-graph.json \
    --source-root .
```

**PowerShell / Windows:**

```powershell
& py -3 $Ingestor link --rules-dir $KdevRulesDir --graph .understand-anything\knowledge-graph.json --source-root .
```

预期：`linked N security edge(s) into ...`。N=0 不是错误（可能项目没用到规则点名的 API，或规则无 pattern）。
````

并在 `## Step 5：报告` 的 markdown 表格里加一行：
```markdown
| 安全关联边(related) | <N> |
```

- [ ] **Step 2: trace SKILL —— 报告 related 边**

在 `kdev-codegraph-trace/SKILL.md` 的「模式 1：规范 → 代码」步骤 2 列表里，`tested_by` 边那条之后加一条：
```markdown
   - `related` 边（source 是该 rule node）→ ⚠️ 疑似涉及（模式命中，weight=0.6）
```

并在该模式的报告模板里，「测试覆盖」表之后加一节：
```markdown
## 疑似涉及（模式命中，需人工确认是否违规）

| 文件 | 函数/类 | 命中说明 |
|---|---|---|
| app/web/api.py | run_cmd | 使用了该规则点名的 API（related） |

> 模式命中 ≠ 一定违规——表示该规则适用于此处，请对照规则的正例/反例人工核对。
```

在「模式 2：代码 → 规范」步骤 2 里，把"邻边 source 端是 concept + kdev:security_rule"扩展为同时认 `related` 邻居：
```markdown
2. 找邻边：邻居是带 `kdev:security_rule` tag 的 concept 节点（经 `documents` 或 `related` 边相连）→ 关联/疑似涉及的规范。
```

- [ ] **Step 3: 全量回归**

Run: `cd plugins/kdev-code-graph/ingestor && python3 -m pytest -q`
Expected: 全部 PASS

- [ ] **Step 4: 提交**

```bash
cd /home/lyadmin/Projects/kdev-agents
git add plugins/kdev-code-graph/skills/kdev-codegraph-build/SKILL.md plugins/kdev-code-graph/skills/kdev-codegraph-trace/SKILL.md
git -c user.name=ly-AI -c user.email=ly1989abc@126.com commit -m "docs(kdev-code-graph): build 加 link 步骤、trace 报告 related 疑似涉及边"
```

---

## Task 6: 全量验证（orchestrator 手动）

- [ ] **Step 1: ingestor 全量测试**

Run: `cd plugins/kdev-code-graph/ingestor && python3 -m pytest -v`
Expected: 全绿，含新增 Task 1-4 的测试。

- [ ] **Step 2: contract 测试不回归**

Run: `cd plugins/kdev-code-graph && python3 -m pytest tests/contract -v`
Expected: 全绿（`related` 在 UA 白名单内，无 schema 破坏）。

- [ ] **Step 3: lineRange 真实性核对（spec §7.1 首步）**

如手边有真实 UA 产出的 `.understand-anything/knowledge-graph.json`，确认 function 节点字段名为 `lineRange`（list[2]）。若字段名不同（如 `line`/`startLine`），回到 `linkers/base.py:build_path_index` 加兼容分支并补测试。无真实图谱则记为待办，留到首次真实 build 时核对。

---

## Self-Review（plan 作者自检结论）

- **Spec 覆盖**：§3 架构→Task 2/3；§5 边编码→Task 2 `make_security_edge`+Task 3；§6 模式提取→Task 1；§7 匹配+映射→Task 2/3；§8 CLI/build→Task 4/5；§9 错误处理→Task 4（source 不存在/dangling skip）+Task 3（无 pattern/rule 不在图）；§10 测试→各 Task 的测试步；§11 trace/impact→Task 5（impact 不改，符合 spec）。无遗漏。
- **占位符**：无 TBD；所有 code step 含完整代码。
- **类型一致**：`Linker.link(rules, graph, source_root)` 全程一致；`build_path_index`/`resolve_node_id`/`make_security_edge` 签名在 Task 2 定义、Task 3/4 调用一致；边形状（related/forward/0.6）在 spec、Task 2、Task 3、Task 4 测试四处一致。
